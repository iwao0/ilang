# Capabilities (FFI / std bindings) — see crates/ilang-cli/src/manifest.rs.
capabilities = ["file", "os", "ffi", "net"]

[package]
name = "gui-win32"

[deps]
windows = "../../../bindings/windows"
wgpu29 = "../../../bindings/wgpu29"
