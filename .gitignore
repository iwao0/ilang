/target
/.claude/worktrees/
/.claude/scheduled_tasks.lock
/.claude/settings.local.json
.DS_Store

# Prebuilt wgpu-native binaries/headers — fetched via
# third_party/wgpu/fetch.sh, not committed (large). Ignore every
# fetched <os>-<arch>/ dir + zips; keep the script (a plain file).
/third_party/wgpu/*.zip
/third_party/wgpu/*/
