# 攻撃面カバレッジ索引(全 fixture カタログ)

バグあぶり出しラウンドが **どの攻撃面を確認済みか** を引くための索引。
`crates/ilang-cli/tests/programs/` 配下の全 fixture を、各ファイル先頭の
ヘッダコメント(§10 で「何を pin しているか書く」と定めたもの)を**逐語転記**して並べる。

**役割分担**: 本書は索引(攻撃面 → 確認済みか)、[HANDOFF.md](HANDOFF.md) は履歴と詳細、
[BUG_HUNTING.md](BUG_HUNTING.md) は手順。

## 使い方

- **ラウンド開始時**: [BUG_HUNTING.md](BUG_HUNTING.md) §6 で狙う交点を決めたら、本書を grep して
  既に近い fixture があるか見る。あれば確認済みなので別の交点へ移る(同じ場所を再 probe しない)。
- **fixture を追加したら**: `scripts/gen_bug_coverage.sh` を実行して本書を再生成する。
  fixture のヘッダが唯一の真実なので索引は自動で実態に追従する(手動編集しない)。

> このファイルは自動生成物。直接編集せず `scripts/gen_bug_coverage.sh` で再生成すること。

## 01_basics (40)

| fixture | pin している内容 |
|---|---|
| [arith_float.il](crates/ilang-cli/tests/programs/01_basics/arith_float.il) | Float arithmetic. `%` is intentionally not supported on floats. |
| [arith_int.il](crates/ilang-cli/tests/programs/01_basics/arith_int.il) | Integer arithmetic  precedence  parens  unary ops. |
| [array_literal_dynamic_inference.il](crates/ilang-cli/tests/programs/01_basics/array_literal_dynamic_inference.il) | An array literal with no annotation infers as a DYNAMIC array (user rule: `let xs = [...]` is `T[]`; `T[N]` arises only from a declared type). So `push` works on a bare literal bi… |
| [bitwise.il](crates/ilang-cli/tests/programs/01_basics/bitwise.il) | Bitwise operators: & \| ^ ~ << >> on integers. |
| [closures.il](crates/ilang-cli/tests/programs/01_basics/closures.il) | Closures capture outer variables by value at creation time (interpreter  JIT — Stage C handles all capture types). |
| [closures_heap.il](crates/ilang-cli/tests/programs/01_basics/closures_heap.il) | Closures capturing heap-typed values (string / array / object). Stage C: ARC-managed closures retain captures on construct and release them when the last reference drops. |
| [closures_jit.il](crates/ilang-cli/tests/programs/01_basics/closures_jit.il) | Closures with i64 / f64 / bool captures — runs on both interpreter and JIT (Stage A). Heap-typed captures (string, objects, arrays) are tested separately in `closures.il` with `//… |
| [comments_and_asi.il](crates/ilang-cli/tests/programs/01_basics/comments_and_asi.il) | Line comments are ignored. |
| [comparison.il](crates/ilang-cli/tests/programs/01_basics/comparison.il) | All comparison operators, on ints / floats / strings / bools. |
| [default_arg_heap_and_reeval.il](crates/ilang-cli/tests/programs/01_basics/default_arg_heap_and_reeval.il) | Default arguments have two subtle behaviours the existing default-arg fixtures don't pin: a HEAP default value is freshly built per call and must be released (no leak) when the pa… |
| [default_args.il](crates/ilang-cli/tests/programs/01_basics/default_args.il) | Default-value parameters: trailing parameters can supply a default value that's substituted at call sites where the caller omits the argument. |
| [destructure_let_underscore.il](crates/ilang-cli/tests/programs/01_basics/destructure_let_underscore.il) | `let (a, _, c) = tuple` — `_` slot ignores the bound value. Per syntax.md §3 "Destructuring let". |
| [float_assoc_constants.il](crates/ilang-cli/tests/programs/01_basics/float_assoc_constants.il) | `f32` / `f64` associated constants  `.isNaN()` / `.isFinite()` predicates, per syntax.md §3. |
| [for_in_array.il](crates/ilang-cli/tests/programs/01_basics/for_in_array.il) | for-in over an array. Both primitive and string elements. |
| [for_in_range.il](crates/ilang-cli/tests/programs/01_basics/for_in_range.il) | Rust 風の `for i in a..b` (排他) と `for i in a..=b` (包含). |
| [if_elif_else.il](crates/ilang-cli/tests/programs/01_basics/if_elif_else.il) | `if` is an expression. Forms: bare if, if/else, if/elif/.../else. |
| [int_assoc_constants.il](crates/ilang-cli/tests/programs/01_basics/int_assoc_constants.il) | Per-width Min / Max associated constants on every signed and unsigned integer type, per syntax.md §3 "Integer associated constants". |
| [let_and_assign.il](crates/ilang-cli/tests/programs/01_basics/let_and_assign.il) | `let` binding, plain reassignment, compound assignments, shadowing. |
| [literals.il](crates/ilang-cli/tests/programs/01_basics/literals.il) | All literal forms documented in syntax.md §1. |
| [logical.il](crates/ilang-cli/tests/programs/01_basics/logical.il) | && / \|\| / !  with short-circuit evaluation. |
| [loop_break_value.il](crates/ilang-cli/tests/programs/01_basics/loop_break_value.il) | `loop` evaluates to a `break v` value. |
| [loops.il](crates/ilang-cli/tests/programs/01_basics/loops.il) | while / loop with break / continue, including nested loops. |
| [match_basic.il](crates/ilang-cli/tests/programs/01_basics/match_basic.il) | match on enum — unit variants, payload variants, short variant form. |
| [match_int_range.il](crates/ilang-cli/tests/programs/01_basics/match_int_range.il) | Integer range patterns in `match`: exclusive `..`, inclusive `..=`, and the half-open forms `..N`, `..=N`, `N..`. Per syntax.md §11 "Matching on primitives". |
| [match_optional.il](crates/ilang-cli/tests/programs/01_basics/match_optional.il) | match on Optional — `some(x)` / `none` / wildcard arms, scalar and heap payloads, value- and statement-position uses, and `none` arm with early `return`. |
| [numeric_conversions.il](crates/ilang-cli/tests/programs/01_basics/numeric_conversions.il) | Implicit widening / narrowing within same sign  explicit `as` casts. |
| [operator_precedence.il](crates/ilang-cli/tests/programs/01_basics/operator_precedence.il) | Operator-precedence boundary cases — shift / bitwise / arithmetic combinations where the wrong grouping would give a different answer than the spec says. |
| [primitive_hash_code.il](crates/ilang-cli/tests/programs/01_basics/primitive_hash_code.il) | `.hashCode(): i64` is available on every numeric primitive and `bool`. Ints widen by their declared signedness; bool yields 0 / 1; floats bit-cast so distinct NaN payloads stay di… |
| [primitive_to_string.il](crates/ilang-cli/tests/programs/01_basics/primitive_to_string.il) | `.toString()` on primitives — integers, floats, bool. Float formatting matches `console.log` (integral values print as `N.0`). |
| [return_paths.il](crates/ilang-cli/tests/programs/01_basics/return_paths.il) | Early `return`, value `return`, value-less Unit `return`, and the implicit "block tail = return value" path. |
| [shift_signedness.il](crates/ilang-cli/tests/programs/01_basics/shift_signedness.il) | Right shift picks its opcode by the operand's signedness: a SIGNED shift is arithmetic (sign-extends the high bits), an UNSIGNED shift is logical (zero-fills). `bitwise.il` exerci… |
| [string_encode_utf16.il](crates/ilang-cli/tests/programs/01_basics/string_encode_utf16.il) | `string.encodeUtf16(nulTerminated?)` — UTF-16 code units, with optional trailing 0x0000. The default is NUL-terminated so the result can be handed straight to Win32 W-suffix APIs … |
| [string_from_utf16.il](crates/ilang-cli/tests/programs/01_basics/string_from_utf16.il) | `string.fromUtf16(units: u16[]): string` — UTF-16 → UTF-8. The whole buffer is consumed; invalid sequences (unpaired surrogates) become U+FFFD. |
| [string_hash_code.il](crates/ilang-cli/tests/programs/01_basics/string_hash_code.il) | `"foo".hashCode(): i64` — FNV-1a 64-bit over the bytes. Stable across runs so `@derive(Hash)` on a string-bearing class can rely on the value for bucketing. |
| [string_ops.il](crates/ilang-cli/tests/programs/01_basics/string_ops.il) | String concat, equality, and every built-in method documented in §4. |
| [template_literal.il](crates/ilang-cli/tests/programs/01_basics/template_literal.il) | Template literals — backtick strings with `${...}` interpolation and the full backslash-escape set. Per syntax.md §4. |
| [u64_signedness.il](crates/ilang-cli/tests/programs/01_basics/u64_signedness.il) | u64 values with the top bit set (≥ 2^63) must be treated as UNSIGNED everywhere — division / modulo / right-shift by an integer literal, and every formatting path (toString, conso… |
| [unit_fn_discards_tail.il](crates/ilang-cli/tests/programs/01_basics/unit_fn_discards_tail.il) | A unit-return fn (`fn f() { ... }` — no `:` return annotation) silently discards its trailing expression value. Same ergonomics as a top-level expression statement: the body is ev… |
| [unit_type_and_indirect_call.il](crates/ilang-cli/tests/programs/01_basics/unit_type_and_indirect_call.il) | The unit type `()` is writable in type position (e.g. an explicit void return annotation), and a call can target an arbitrary callee expression — `arr[0]()`, `(f)(x)`, `mk()()` — … |
| [unsigned_literal_and_cast.il](crates/ilang-cli/tests/programs/01_basics/unsigned_literal_and_cast.il) | Clean-round pin for unsigned-integer edges around round 132: - u64 compared against boundary literals (the literal adopts u64, so   the comparison is unsigned even when the litera… |

## 02_classes (35)

| fixture | pin している内容 |
|---|---|
| [arc_aliasing.il](crates/ilang-cli/tests/programs/02_classes/arc_aliasing.il) | `let y = x` creates a new binding that retains `x`'s allocation. The object only dies when ALL strong refs are gone. |
| [arc_array_of_objects.il](crates/ilang-cli/tests/programs/02_classes/arc_array_of_objects.il) | Array of objects: when the array's last reference dies, every element is released — cascading deinits if the elements held heap resources. |
| [arc_field_holds.il](crates/ilang-cli/tests/programs/02_classes/arc_field_holds.il) | An Object-typed field keeps its target alive until the holder dies. When the holder dies, the field is released, possibly cascading. |
| [arc_scope_drop.il](crates/ilang-cli/tests/programs/02_classes/arc_scope_drop.il) | `deinit` must fire when an object's last strong reference goes out of scope. Counted via a shared Counter the tracked objects bump in their deinit. |
| [child_no_init_inherits_parent.il](crates/ilang-cli/tests/programs/02_classes/child_no_init_inherits_parent.il) | A child class with no `init` of its own inherits the parent's init signature. The child's own fields fall back to the type-driven defaults (`0` / `""` / `false` / `none` / `[]` / … |
| [class_basic.il](crates/ilang-cli/tests/programs/02_classes/class_basic.il) | Class fundamentals: fields, init, methods, implicit and explicit `this`. |
| [class_eq_ref.il](crates/ilang-cli/tests/programs/02_classes/class_eq_ref.il) | `==` / `!=` on objects = reference (identity) equality, NOT structural. Mirrors Java's `==`. |
| [class_field_types.il](crates/ilang-cli/tests/programs/02_classes/class_field_types.il) | Fields of varied types — primitive, string, Object, Array, Optional. Verifies that field load/store works for every JIT type kind. |
| [class_method_chain.il](crates/ilang-cli/tests/programs/02_classes/class_method_chain.il) | Methods returning the receiver enable chaining (fluent style). |
| [class_no_init.il](crates/ilang-cli/tests/programs/02_classes/class_no_init.il) | `init` is optional. A class with no init can still be `new`'d without args; fields just start at zero / "" / etc. |
| [derive_enum_field.il](crates/ilang-cli/tests/programs/02_classes/derive_enum_field.il) | `@derive(Eq, Hash)` supports enum-typed fields: the synthesised `equals` / `hashCode` route an enum field through the enum's own structural equality (round 105) and a matching str… |
| [f32_field_from_f64_value.il](crates/ilang-cli/tests/programs/02_classes/f32_field_from_f64_value.il) | Assigning an f64-typed value (a bare float literal, or an f64 expression) directly to an `f32` field must demote to f32 before the store. Object fields live in 8-byte slots; the s… |
| [generic_class.il](crates/ilang-cli/tests/programs/02_classes/generic_class.il) | Generic classes — must be instantiated with explicit type args. Each (Name, args) combo is monomorphized to a distinct concrete class in the JIT. |
| [iflet_heap_release.il](crates/ilang-cli/tests/programs/02_classes/iflet_heap_release.il) | Regression: an `if let some(x) = expr` where `x` is heap-typed must release `x` at the end of the then-branch and the scrutinee allocation itself when the if-let expression finish… |
| [inheritance_basic.il](crates/ilang-cli/tests/programs/02_classes/inheritance_basic.il) | Single inheritance with method override  super dispatch. |
| [inheritance_deinit_chain.il](crates/ilang-cli/tests/programs/02_classes/inheritance_deinit_chain.il) | Deinits chain automatically, Swift-style: when an instance dies, the most derived class's `deinit` fires first, then each ancestor's in turn. There is no `super.deinit()` — explic… |
| [inheritance_ref_equality.il](crates/ilang-cli/tests/programs/02_classes/inheritance_ref_equality.il) | `==` / `!=` on classes is reference equality, even when the receivers are typed as a common ancestor. A child stored in a parent slot still compares by allocation address, not by … |
| [inheritance_super_deinit_error.il](crates/ilang-cli/tests/programs/02_classes/inheritance_super_deinit_error.il) | Deinits chain automatically (derived first, then each ancestor), so an explicit `super.deinit()` would run the parent's hook a second time — rejected at type-check like every othe… |
| [init_default_args.il](crates/ilang-cli/tests/programs/02_classes/init_default_args.il) | `init` and methods accept default-arg parameters too. |
| [interface_basic.il](crates/ilang-cli/tests/programs/02_classes/interface_basic.il) | `interface` declares a method contract; `class C: I, J { ... }` makes C implement every method of every listed interface. Values of class type can flow into a slot typed as the in… |
| [interface_objc_decl.il](crates/ilang-cli/tests/programs/02_classes/interface_objc_decl.il) | `@objc interface` declarations inside `@extern(ObjC) { ... }` register as ordinary ilang interfaces, with extra Objective-C selector metadata that downstream passes consume:    * … |
| [interface_optional.il](crates/ilang-cli/tests/programs/02_classes/interface_optional.il) | Optional interface methods are only meaningful for Objective-C protocols, where the ObjC runtime probes `respondsToSelector:` at the call site. Plain (non-`@objc`) interfaces enfo… |
| [method_implicit_this_fresh_arg.il](crates/ilang-cli/tests/programs/02_classes/method_implicit_this_fresh_arg.il) | Fresh heap args passed through an IMPLICIT `this.method(...)` call (bare `method(...)` inside a method body) follow the same transfer rule as every other call shape: the caller's … |
| [static_field_inherited.il](crates/ilang-cli/tests/programs/02_classes/static_field_inherited.il) | Static methods and static properties were reachable by a subclass's own name (`Derived.make()`, `Derived.version`), but a static FIELD was not — `Derived.count` was rejected ("und… |
| [static_fields.il](crates/ilang-cli/tests/programs/02_classes/static_fields.il) | Class-level mutable storage shared by all instances. |
| [static_method_inherited.il](crates/ilang-cli/tests/programs/02_classes/static_method_inherited.il) | A static method is reachable by a subclass's own name (it resolves to the nearest declaration up the chain), and a subclass may shadow it with its own. A static method can also ca… |
| [static_methods.il](crates/ilang-cli/tests/programs/02_classes/static_methods.il) | Static methods on a class — callable without an instance. |
| [static_string_field.il](crates/ilang-cli/tests/programs/02_classes/static_string_field.il) | String-typed static fields: the slot holds an i64 heap pointer that gets filled in at program startup by a synthetic `AssignField`. Works for both `const` (write-once) and plain `… |
| [static_string_field_arc.il](crates/ilang-cli/tests/programs/02_classes/static_string_field_arc.il) | A heap-typed static field (a string) must take ownership of the value stored into it and drop the previous occupant — the same rule an instance field follows. `StoreStatic` did ne… |
| [struct_literal_on_class_error.il](crates/ilang-cli/tests/programs/02_classes/struct_literal_on_class_error.il) | Struct-literal construction (`Name { f: v }`) is reserved for value types — top-level `struct` / `union`. Pointing it at a regular ARC class must be rejected, otherwise the class'… |
| [typeof_name.il](crates/ilang-cli/tests/programs/02_classes/typeof_name.il) | `typeof(x).name` — RTTI on classes and primitives. Per syntax.md §13a. Both the direct chain and a let-bound `Type` handle resolve to the same string. |
| [typeof_reflection.il](crates/ilang-cli/tests/programs/02_classes/typeof_reflection.il) | `typeof(x).<member>` reflection on a class instance. Per syntax.md §13a. |
| [typeof_reflection_arc.il](crates/ilang-cli/tests/programs/02_classes/typeof_reflection_arc.il) | ARC balance for reflection reads. `typeof(x).name` retains a registry string, `.fields` / `.methods` / `.typeArgs` mint fresh arrays, `.parent` mints a fresh Optional cell, `.kind… |
| [weak_basic.il](crates/ilang-cli/tests/programs/02_classes/weak_basic.il) | Weak references: don't bump strong rc; `.get()` returns T? (Some while target is alive, None after target is freed). |
| [weak_inherited_field.il](crates/ilang-cli/tests/programs/02_classes/weak_inherited_field.il) | A child class inherits its parent's `weak` field and keeps the weak semantics — accessing `.get()` through the inherited slot still produces a live `T?` while the strong owner is … |

## 03_collections (65)

| fixture | pin している内容 |
|---|---|
| [array_basics.il](crates/ilang-cli/tests/programs/03_collections/array_basics.il) | Array literal forms: dynamic, fixed-length, trailing comma, nested. Plus index read/write, .length, and the empty-array annotation rule. |
| [array_buffer_memcpy.il](crates/ilang-cli/tests/programs/03_collections/array_buffer_memcpy.il) | Pass numeric arrays as `void *` buffers to a native extern. `memcpy(dst, src, n)` is the canonical exercise: it writes through the dst pointer and reads through the src pointer. A… |
| [array_fill_arc.il](crates/ilang-cli/tests/programs/03_collections/array_fill_arc.il) | `arr.fill(v)` retains v once per slot (the array fully owns its copies) and releases each old occupant. Two pinned regressions: the lowerer never dropped a fresh fill value's tran… |
| [array_indexof_string_content_eq.il](crates/ilang-cli/tests/programs/03_collections/array_indexof_string_content_eq.il) | `indexOf` / `includes` / `remove` on `string[]` compare by content, matching `==`'s structural equality on strings. The runtime used a raw cell compare (pointer equality), which w… |
| [array_methods_chain.il](crates/ilang-cli/tests/programs/03_collections/array_methods_chain.il) | Every array method documented in §8 — exercised once each, then chained where possible. |
| [array_reverse_independence.il](crates/ilang-cli/tests/programs/03_collections/array_reverse_independence.il) | `arr.reverse()` returns a fresh reversed copy and leaves the receiver untouched. Per syntax.md §8 "Built-in array methods". `array_methods_chain.il` uses reverse inside a method c… |
| [array_search_enum_structural.il](crates/ilang-cli/tests/programs/03_collections/array_search_enum_structural.il) | Array `indexOf` / `includes` / `remove` must compare enum elements STRUCTURALLY (discriminant  payload), matching `==` on enums (round 105). A payload variant like `circle(5)` is … |
| [array_search_structural_containers.il](crates/ilang-cli/tests/programs/03_collections/array_search_structural_containers.il) | Array `indexOf` / `includes` / `remove` compare element STRUCTURALLY for tuple, nested-dynamic-array, and optional element types (the same way round 126 did for enums). Before, th… |
| [array_shift_unshift.il](crates/ilang-cli/tests/programs/03_collections/array_shift_unshift.il) | Front-of-array operations — `shift()` (remove & return the first element) and `unshift(v)` (prepend). Per syntax.md §8. `array_basics.il` covers push/pop/remove/removeAt; this fil… |
| [array_sort_independence.il](crates/ilang-cli/tests/programs/03_collections/array_sort_independence.il) | `arr.sort(cmp)` returns a fresh sorted copy and leaves the receiver untouched (per syntax.md §8: reverse / map / filter / sort all leave the receiver alone). Existing fixtures onl… |
| [class_derive_edge_cases.il](crates/ilang-cli/tests/programs/03_collections/class_derive_edge_cases.il) | Corner cases for the `@derive(Eq, Hash)` synthesis:   - class with no fields (every instance is equal to every     other; hashCode is the seed)   - single-field class (the polynom… |
| [class_derive_float_field.il](crates/ilang-cli/tests/programs/03_collections/class_derive_float_field.il) | `@derive(Eq, Hash)` now handles `f32` / `f64` fields through the primitive `hashCode()` (bit pattern → i64). Two `Vec3f` instances with identical bytes collapse in a Set; differin… |
| [class_derive_mixed.il](crates/ilang-cli/tests/programs/03_collections/class_derive_mixed.il) | `@derive(Eq, Hash)` only synthesises the methods the user didn't write — so deriving one side and hand-rolling the other must compose. Exercises both directions: derived `equals` … |
| [class_derive_nested.il](crates/ilang-cli/tests/programs/03_collections/class_derive_nested.il) | `@derive(Eq, Hash)` walks fields field-by-field; a class-typed field recurses through that field's own `equals` / `hashCode`. Two-level nesting verifies the recursion: `Outer` der… |
| [class_derive_string_field.il](crates/ilang-cli/tests/programs/03_collections/class_derive_string_field.il) | `@derive(Eq, Hash)` now handles string fields by routing through the built-in `string.hashCode()`. Two `Tag` instances with the same `name` should dedup in a Set. |
| [empty_map_literal_brace.il](crates/ilang-cli/tests/programs/03_collections/empty_map_literal_brace.il) | `{}` parses as an empty block (value `()`), so `let m: Map<K,V> = {}` used to fail type-checking ("expected Map<K,V>, got ()") — the only way to make an empty map was `new Map<K,V… |
| [enum_generic.il](crates/ilang-cli/tests/programs/03_collections/enum_generic.il) | User-defined generic enum, instantiated with several type args. JIT monomorphizes per (Name, args) pair. |
| [enum_payload_fixed_array.il](crates/ilang-cli/tests/programs/03_collections/enum_payload_fixed_array.il) | Fixed-length array (`i64[3]`) as an enum variant payload used to return wrong values on match (3, 3, garbage) because the bare array literal `[10, 20, 30]` lowered to the dynamic-… |
| [enum_payload_nested.il](crates/ilang-cli/tests/programs/03_collections/enum_payload_nested.il) | Enums whose payload itself contains another enum / a dynamic array / an Optional. The match arm has to walk one level deeper than the flat `enum_payload_tuple.il` cases, and the A… |
| [enum_payload_struct.il](crates/ilang-cli/tests/programs/03_collections/enum_payload_struct.il) | Enums with named (struct-style) payloads. Patterns can bind by the field name (shorthand) or rename via `field: bound_name`. |
| [enum_payload_tuple.il](crates/ilang-cli/tests/programs/03_collections/enum_payload_tuple.il) | Enums with positional (tuple) payloads. The pattern binds names to the payload fields by position. |
| [enum_unit.il](crates/ilang-cli/tests/programs/03_collections/enum_unit.il) | Unit-only enums (Phase 1 representation: bare i32 tag). |
| [float_array_hof.il](crates/ilang-cli/tests/programs/03_collections/float_array_hof.il) | Higher-order array ops on f32[] / f64[] call the closure through an ABI matching its float parameter / return type. The integer-only call path used to feed the closure garbage (e.… |
| [generic_fn_class_inst.il](crates/ilang-cli/tests/programs/03_collections/generic_fn_class_inst.il) | `fn make<T>(v: T): Box<T> { new Box<T>(v) }` — instantiating a generic class inside a generic fn body. Previously failed at the type-checker with "expected T, got T" because `chec… |
| [generic_fn_enum_inst.il](crates/ilang-cli/tests/programs/03_collections/generic_fn_enum_inst.il) | Generic enum constructor inside a generic fn body — previously failed at MIR ("unknown type: T") because monomorphize_enums only seeded EnumCtors from non-generic fn bodies, and m… |
| [map_basics.il](crates/ilang-cli/tests/programs/03_collections/map_basics.il) | Map<K, V> — explicit constructor  set / has / size / delete plus the indexing forms. |
| [map_clear_primitive_keys.il](crates/ilang-cli/tests/programs/03_collections/map_clear_primitive_keys.il) | `Map<K, V>.clear()` — drop every entry, size goes to zero, all previous keys answer `has(k) == false`. Per syntax.md §9 method table. `map_basics.il` exercises set / get / has / d… |
| [map_entries_primitive_keys.il](crates/ilang-cli/tests/programs/03_collections/map_entries_primitive_keys.il) | `Map<K, V>.entries(): (K, V)[]` — non-string key shapes. Per syntax.md §9. `map_of_class.il` exercises entries() for string keys; this fixture covers `i64` and `bool` keys. |
| [map_float_values.il](crates/ilang-cli/tests/programs/03_collections/map_float_values.il) | A `Map<_, f32>` must store the float at its declared width — a bare literal narrows to f32 instead of staying f64 (whose low 32 bits read back as 0) — and `map.forEach` must hand … |
| [map_foreach_mutation_arc.il](crates/ilang-cli/tests/programs/03_collections/map_foreach_mutation_arc.il) | `Map.forEach` snapshots the entry order at the start of the callback loop, so mutating the map from inside the callback is safe: a value added mid-iteration is freed when the map … |
| [map_get_keys_values.il](crates/ilang-cli/tests/programs/03_collections/map_get_keys_values.il) | Map.get returns V?, Map.keys / values return arrays. |
| [map_index_borrow_aliasing.il](crates/ilang-cli/tests/programs/03_collections/map_index_borrow_aliasing.il) | `m[k]` is a borrowed read (same convention as `ArrayLoad`): the map's slot keeps its share, and consumers that store the value take their own retain. This pins the alias-safety si… |
| [map_index_read_object_key_release.il](crates/ilang-cli/tests/programs/03_collections/map_index_read_object_key_release.il) | Indexing a `Map` with a FRESH object key (`m[new K(1)]`) leaked the lookup key. `__map_get` only borrows the key (hash  equals) and never takes ownership, but the index-read lower… |
| [map_int_keys.il](crates/ilang-cli/tests/programs/03_collections/map_int_keys.il) | Map with non-string key types (i64 / bool). |
| [map_keys_int_widths.il](crates/ilang-cli/tests/programs/03_collections/map_keys_int_widths.il) | Map keys preserve their original integer width through `keys()` — pre-fix the eval `MapKey` collapsed all signed widths to i64 and all unsigned to u64, so `Map<u8, V>.keys()[0]` c… |
| [map_literal.il](crates/ilang-cli/tests/programs/03_collections/map_literal.il) | Map literal `{k: v, ...}`. K is inferred from the first entry's key type; V from its value type. |
| [map_object_key_brace_literal.il](crates/ilang-cli/tests/programs/03_collections/map_object_key_brace_literal.il) | `Map<MyClass, V>` built from the `{}` map-literal shorthand must key on the class's value-equality protocol (`equals` / `hashCode`), exactly like `new Map<MyClass, V>()`. It did n… |
| [map_of_class.il](crates/ilang-cli/tests/programs/03_collections/map_of_class.il) | Map<MyClass, V> with class-typed keys driven by manual `equals` / `hashCode`. Covers the full Map method matrix on a class-typed key so the Object store handles get / set / has / … |
| [map_of_class_derived.il](crates/ilang-cli/tests/programs/03_collections/map_of_class_derived.il) | `@derive(Eq, Hash)` driving `Map<Class, V>`. Parallel of `set_of_class_derived.il` for maps — the synthesised `equals` / `hashCode` should make two distinct `new Pos(...)` instanc… |
| [map_overwrite_releases_prev_object.il](crates/ilang-cli/tests/programs/03_collections/map_overwrite_releases_prev_object.il) | Overwriting the same key in a `Map<K, Object>` must release the previous occupant. `__map_get` used to retain on read while the lowerer classified `m[k]` as a borrow — the binding… |
| [map_set_fresh_needle_arg_release.il](crates/ilang-cli/tests/programs/03_collections/map_set_fresh_needle_arg_release.il) | Fresh needle args to Map / Set lookup methods must drop their transient 1 after the call. The lowerer only released fresh args for `m.set` / `s.add`; `get` / `has` / `delete` leak… |
| [map_set_insertion_order.il](crates/ilang-cli/tests/programs/03_collections/map_set_insertion_order.il) | Map / Set iterate in INSERTION ORDER (JS semantics, user decision): entries()/keys()/values()/forEach and printing all follow first-insert position; overwriting a key keeps its sl… |
| [map_set_order_arc_churn.il](crates/ilang-cli/tests/programs/03_collections/map_set_order_arc_churn.il) | Clean-round pin (no bug fixed — confirms the Map/Set insertion-order machinery added in `a4572ede` keeps ARC exact). The sibling `map_set_insertion_order.il` pins the ORDER semant… |
| [map_value_kinds_churn.il](crates/ilang-cli/tests/programs/03_collections/map_value_kinds_churn.il) | Overwrite churn across the heap value kinds a Map can hold — enum (payload), Optional, array, tuple, closure, nested Map — plus a class-with-Map-field method-tail read. Each loop … |
| [object_key_hash_collision.il](crates/ilang-cli/tests/programs/03_collections/object_key_hash_collision.il) | Set/Map with object keys must consult the user's `equals` when two distinct keys land in the same hash bucket. A coarse `hashCode` forces collisions on purpose. The runtime called… |
| [optional_heap.il](crates/ilang-cli/tests/programs/03_collections/optional_heap.il) | Optional<T> with heap T (string, Object). |
| [optional_iflet.il](crates/ilang-cli/tests/programs/03_collections/optional_iflet.il) | `if let some(v) = ...` — pattern match on Optional outside `match`. |
| [optional_primitive.il](crates/ilang-cli/tests/programs/03_collections/optional_primitive.il) | Optional<T> with primitive T — JIT heap-boxes the payload so 0 can still be a valid Some(0) while None is its own state. |
| [optional_type_propagation.il](crates/ilang-cli/tests/programs/03_collections/optional_type_propagation.il) | An expected `Optional<T>` type flows into a `some(..)` so the wrapped value is built as T — narrowing a float literal to f32 and building a nested tuple with the declared widths —… |
| [packed_array_format.il](crates/ilang-cli/tests/programs/03_collections/packed_array_format.il) | `console.log` and template interpolation read array elements at the header stride, so packed small-int arrays print their real values. Fixed-length `T[N]` arrays are header-less i… |
| [packed_array_ops.il](crates/ilang-cli/tests/programs/03_collections/packed_array_ops.il) | Packed small-element arrays (i8/u8/i16/i32 store at stride 1/2/4) must give the same results as i64 cells across the search, mutate, and higher-order operations — the runtime read… |
| [result_basic.il](crates/ilang-cli/tests/programs/03_collections/result_basic.il) | Built-in Result<T, E>. Variants `ok` / `err`. |
| [result_in_function.il](crates/ilang-cli/tests/programs/03_collections/result_in_function.il) | Functions returning Result; both branches annotated by the fn's declared return type so the typechecker can refine inferred Any. |
| [set_algebra_object_arc.il](crates/ilang-cli/tests/programs/03_collections/set_algebra_object_arc.il) | `set_of_class.il` checks the result SIZES and membership of union / intersection / difference on object-typed sets, but not their ARC: the result sets share element references wit… |
| [set_basics.il](crates/ilang-cli/tests/programs/03_collections/set_basics.il) | Set<T> — construct / add / has / delete / size / forEach plus the set-algebra methods, over both integer and string element kinds. |
| [set_element_types.il](crates/ilang-cli/tests/programs/03_collections/set_element_types.il) | Set<T> across every primitive element type. `set_basics.il` already covers the full method matrix on `Set<i64>` and `Set<string>`; this file repeats every method (add / has / dele… |
| [set_float_bit_pattern.il](crates/ilang-cli/tests/programs/03_collections/set_float_bit_pattern.il) | `Set<f32/f64>` hashes and compares elements by their raw BIT PATTERN (docs: "Floats compare by bit pattern, so distinct NaN payloads stay as separate entries"). `set_element_types… |
| [set_map_payload_enum_key.il](crates/ilang-cli/tests/programs/03_collections/set_map_payload_enum_key.il) | A payload-carrying enum is a first-class `Set` element / `Map` key: dedup and lookup go through the enum's structural equality / hash (round 129), and the object-keyed store retai… |
| [set_of_class.il](crates/ilang-cli/tests/programs/03_collections/set_of_class.il) | Set<MyClass> driven by the value-equality protocol (manual `equals`  `hashCode` methods). Covers the full Set method matrix on a class-typed element so the Object store handles ad… |
| [set_of_class_derived.il](crates/ilang-cli/tests/programs/03_collections/set_of_class_derived.il) | `@derive(Eq, Hash)` should synthesise `equals` / `hashCode` methods structurally over the class's fields, so the resulting behaviour matches the hand-written version in `set_of_cl… |
| [set_of_enum.il](crates/ilang-cli/tests/programs/03_collections/set_of_enum.il) | `Set<MyEnum>` / `Map<MyEnum, V>` work when every variant is unit-payload (or the whole enum is `@flags`). Those compile to a single `i64` tag, so the existing `Int` store keys the… |
| [set_payload_enum_operations.il](crates/ilang-cli/tests/programs/03_collections/set_payload_enum_operations.il) | Clean-round pin for payload-enum Set / Map keys (round 130): the set ALGEBRA (union / intersection / difference), the subset / superset / disjoint predicates, and Map `entries()` … |
| [string_array_join.il](crates/ilang-cli/tests/programs/03_collections/string_array_join.il) | `string[].join(sep)` — concatenate every element with the given separator. Per syntax.md §8 "Built-in array methods". |
| [tuple_array_type.il](crates/ilang-cli/tests/programs/03_collections/tuple_array_type.il) | Postfix type modifiers apply after a tuple / grouped type, so `(i32, f32)[]` (an array of tuples) parses. The tuple elements are built with the annotated widths (f32, not the defa… |
| [tuple_basics.il](crates/ilang-cli/tests/programs/03_collections/tuple_basics.il) | Tuple literal  type annotation  element access via `[N]` (where N is a constant integer literal). Heterogeneous element types and nested tuples should both work; ARC keeps heap el… |

## 04_modules (203)

| fixture | pin している内容 |
|---|---|
| [async_array_let.il](crates/ilang-cli/tests/programs/04_modules/async_array_let.il) | Array literal as an unannotated `let` inside an async fn body. Used to fail at desugar with "couldn't infer this binding's type from the RHS shape". The mini-inferencer now recogn… |
| [async_await_field_method.il](crates/ilang-cli/tests/programs/04_modules/async_await_field_method.il) | The mini-inferencer in the async desugar now handles `obj.field` and `obj.method(...)` on the RHS of `let v = await ...`, so explicit type annotations are no longer required for t… |
| [async_await_in_assignment.il](crates/ilang-cli/tests/programs/04_modules/async_await_in_assignment.il) | `await` in assignment RHS positions. The await-lift pre-pass had no arms for Assign / AssignField / AssignIndex and didn't descend into nested blocks (while / loop / for-in bodies… |
| [async_await_in_if_cond.il](crates/ilang-cli/tests/programs/04_modules/async_await_in_if_cond.il) | `await` inside an `if` condition / `match` scrutinee is now lifted by the sub-expression lift pass:   if await p { A } else { B } becomes   let __await_t0 = await p   if __await_t… |
| [async_await_logical_rhs_error.il](crates/ilang-cli/tests/programs/04_modules/async_await_logical_rhs_error.il) | `await` on the short-circuiting side of `&&` / `\|\|` can't be hoisted (it would run unconditionally), and the state-machine can't reach it in place. This used to be an internal c… |
| [async_await_promise_all.il](crates/ilang-cli/tests/programs/04_modules/async_await_promise_all.il) | `await Promise.all([...])` inside an async fn — the await blocks the fn until every input resolves, then surfaces the aggregated `T[]` directly. The desugar needs an explicit `let… |
| [async_await_rejection_propagates.il](crates/ilang-cli/tests/programs/04_modules/async_await_rejection_propagates.il) | `await` on a REJECTED promise rejects the async fn's own result promise with the same message (JS semantics). The suspend registers a rejection forwarder (`$promise.rejectFollows`… |
| [async_class_method.il](crates/ilang-cli/tests/programs/04_modules/async_class_method.il) | `async fn` inside a `class`. The desugar lifts the state class and the poll fn out next to the containing class as top-level items; the class keeps the original-named method as a … |
| [async_early_return.il](crates/ilang-cli/tests/programs/04_modules/async_early_return.il) | Early `return` inside an `async fn` body. The desugar rewrites every copied `return e` into "settle the result promise with `e`, then a bare `return`" — the poll fn returns `()`, … |
| [async_else_if_chain.il](crates/ilang-cli/tests/programs/04_modules/async_else_if_chain.il) | `elif` chain in tail-position with awaits in each arm. The parser produces else_branch as a bare `If` expr (not a Block); v2 wraps it via coerce_to_block before recursing, so each… |
| [async_for_in_array.il](crates/ilang-cli/tests/programs/04_modules/async_for_in_array.il) | `for v in arr { ...await... }` — non-Range iter desugared to an index-based while loop accessing `arr.length` and `arr[i]`. |
| [async_for_in_half_open.il](crates/ilang-cli/tests/programs/04_modules/async_for_in_half_open.il) | `for v in start.. { ...await...; break }` — half-open range desugars to `while true { ...; v = v  1 }`. Body must exit via break. |
| [async_for_in_range.il](crates/ilang-cli/tests/programs/04_modules/async_for_in_range.il) | `for v in 0..n { ...await... }` — v2 pre-pass rewrites this to the equivalent while loop and the state-machine builder takes over. A heap-typed Counter bound outside forces v2 (le… |
| [async_heap_let_before_await.il](crates/ilang-cli/tests/programs/04_modules/async_heap_let_before_await.il) | Heap-typed let binding introduced BEFORE the first await. Under the legacy class-based lowering this failed because the state class's init has to assign every field, and there's n… |
| [async_heap_return_release.il](crates/ilang-cli/tests/programs/04_modules/async_heap_return_release.il) | A suspended async fn (one that hits an `await`) that returns a heap value used to LEAK that value. The state-machine desugar settles the result promise with `Promise.$promise.sett… |
| [async_if_await.il](crates/ilang-cli/tests/programs/04_modules/async_if_await.il) | `async fn` with `if-else` at the body tail and awaits inside each arm. The BlockBuilder builds one state-block per chunk between awaits and emits a Branch terminator that picks wh… |
| [async_if_branch_heap_let.il](crates/ilang-cli/tests/programs/04_modules/async_if_branch_heap_let.il) | Tail-position if-else with await(s) inside each branch, plus a heap-typed let bound BEFORE the if-else and used inside the branches. Legacy class-based lowering couldn't represent… |
| [async_if_no_else_await.il](crates/ilang-cli/tests/programs/04_modules/async_if_no_else_await.il) | Stmt-position `if cond { ...await... }` with no else. Both arms (the synthesized empty else and the then) converge on an after-segment that picks up the remaining body. |
| [async_loop_with_await.il](crates/ilang-cli/tests/programs/04_modules/async_loop_with_await.il) | `loop { ...await...; break }` — Rust-style infinite loop with awaits. v2 pre-pass rewrites to `while true { ... }`, which the state-machine builder handles via its standard while … |
| [async_match_await.il](crates/ilang-cli/tests/programs/04_modules/async_match_await.il) | `async fn` with `match` at body tail position, awaits inside arm bodies. The BlockBuilder emits a MatchDispatch terminator whose pattern arms save any pattern bindings into state … |
| [async_match_binding_await.il](crates/ilang-cli/tests/programs/04_modules/async_match_binding_await.il) | Tail-position `match` with awaits in arms — including an arm that binds a payload AND awaits inside its body. The v2 MatchT terminator emits a per-arm transition; bindings are sto… |
| [async_match_generic_enum.il](crates/ilang-cli/tests/programs/04_modules/async_match_generic_enum.il) | Generic user enum with type-param payload, awaited inside arm with binding. v2 resolves the variant payload type by substituting the enum's type params with the scrutinee's concre… |
| [async_match_string_binding.il](crates/ilang-cli/tests/programs/04_modules/async_match_string_binding.il) | Pattern binding 型精密化: `T?` の `some(s)` で s が string (heap 型) のケース。v2 の MatchT terminator が variant payload に s を string として宣言し、retain/release が正しく動く。 (legacy / 旧 v2 では I64 placeho… |
| [async_mid_if_await.il](crates/ilang-cli/tests/programs/04_modules/async_mid_if_await.il) | Mid-body `let r = if-else { ... } / match { ... }` whose arms contain awaits. The BlockBuilder allocates a join state, gives each arm a `BodyEnd::AssignAndJump` so they write thei… |
| [async_mid_join_v2.il](crates/ilang-cli/tests/programs/04_modules/async_mid_join_v2.il) | Mid-body if-else / match with awaits and a join point. v2's JumpBind terminator threads each arm's tail value into the join variant's payload field; the rest of the fn body (and a… |
| [async_multi_await.il](crates/ilang-cli/tests/programs/04_modules/async_multi_await.il) | `async fn` with multiple `await` sites — exercises the state-machine lowering. The desugar emits:   - a `__sumThree_State` class with one field per param  one     per `let` bindin… |
| [async_nested_async_calls.il](crates/ilang-cli/tests/programs/04_modules/async_nested_async_calls.il) | async fn `outer` calls another `async fn inner` and awaits it twice in sequence. Each `await inner(...)` suspends `outer` until the inner promise settles; sequential awaits read e… |
| [async_nested_v2.il](crates/ilang-cli/tests/programs/04_modules/async_nested_v2.il) | Phase 2e: nested control flow inside async fn bodies.   - while body containing tail-If with awaits in branches   - while body containing mid-body let-if/match join   - if-arm con… |
| [async_no_await.il](crates/ilang-cli/tests/programs/04_modules/async_no_await.il) | `async fn` with no `await` — the desugar pass wraps the body in `Promise.resolve(...)` and exposes the fn as `Promise<T>`-returning. Multi-state lowering for bodies with awaits is… |
| [async_optional_heap_return.il](crates/ilang-cli/tests/programs/04_modules/async_optional_heap_return.il) | An async fn that returns `Optional<heap>` (`Box?`) — the suspended state machine settles its result promise with a some/none Optional wrapping a heap value. Both the some-path Box… |
| [async_ret_promise_explicit.il](crates/ilang-cli/tests/programs/04_modules/async_ret_promise_explicit.il) | `async fn` whose declared return type already includes the `Promise<...>` wrapper. The desugar must NOT double-wrap to `Promise<Promise<...>>`; it should treat the declared inner … |
| [async_single_await.il](crates/ilang-cli/tests/programs/04_modules/async_single_await.il) | `async fn` with a single `let x = await p` await site. The desugar's mini-inferencer figures out `x: i64` from `p`'s `Promise<i64>` parameter type, so no explicit annotation is re… |
| [async_stored_promise.il](crates/ilang-cli/tests/programs/04_modules/async_stored_promise.il) | Receive a `Promise<T>` into a `let` and await later. Useful for kicking off concurrent work before awaiting any of it. |
| [async_subexpr_await.il](crates/ilang-cli/tests/programs/04_modules/async_subexpr_await.il) | `await` inside sub-expressions — the lifting pre-pass extracts each await into a `let __await_tN = await E` statement above the use site, so the analyser  state-machine synth keep… |
| [async_three_awaits_in_call.il](crates/ilang-cli/tests/programs/04_modules/async_three_awaits_in_call.il) | Three `await` sub-expressions used as arguments to a sync fn — the awaits evaluate left-to-right per syntax.md §13's "Inside a sub-expression, awaits evaluate left-to-right" rule.… |
| [async_while_await.il](crates/ilang-cli/tests/programs/04_modules/async_while_await.il) | `async fn` with a `while` loop whose body contains awaits. The BlockBuilder emits:   - Block 0: pre-loop stmts, Jump to header   - Block 1 (header): Branch(cond, body_first, after… |
| [async_while_break.il](crates/ilang-cli/tests/programs/04_modules/async_while_break.il) | `break` / `continue` inside an async `while` body now jump to the user's logical while — NOT the generated outer `loop { switch state_idx }` driver. The pre-pass rewrites each `br… |
| [async_while_cond_await.il](crates/ilang-cli/tests/programs/04_modules/async_while_cond_await.il) | `while await p { body }`: cond is re-evaluated each iteration via await. v2 pre-pass rewrites to `while true { let cv = await p; if !cv { break }; body }`. A Counter (heap) bound … |
| [async_while_heap_let.il](crates/ilang-cli/tests/programs/04_modules/async_while_heap_let.il) | While-with-await containing a heap-typed let. Legacy class-based lowering can't represent the Counter slot (no zero-value default); v2 with per-variant payloads carries `c` throug… |
| [await_reject_releases_pre_await_heap.il](crates/ilang-cli/tests/programs/04_modules/await_reject_releases_pre_await_heap.il) | Confirmation pin (第38弾): two async ARC properties around the rejection path, complementing the exit-drain fix in 第37弾.    1. When `await p` REJECTS, the async fn abandons the rest… |
| [const_expr.il](crates/ilang-cli/tests/programs/04_modules/const_expr.il) | const can hold a compile-time-evaluable expression. |
| [cross_module_import_assign.il](crates/ilang-cli/tests/programs/04_modules/cross_module_import_assign.il) | Regression: a write to a global imported via `use M { X }` from another module must resolve the same way a read does. The loader qualified `Var` reads to `M.X` but skipped the `As… |
| [cross_module_pub_field_access.il](crates/ilang-cli/tests/programs/04_modules/cross_module_pub_field_access.il) | `pub` on class fields gates cross-module read access. A non-pub field (default — module-private) defined in module `lib_acl` is unreachable from the entry module's code; the type … |
| [cross_module_pub_method_access.il](crates/ilang-cli/tests/programs/04_modules/cross_module_pub_method_access.il) | `pub` on class methods gates cross-module method calls. A non-pub method on a `pub` class is unreachable from outside the declaring module. |
| [cross_module_pub_module_top_stmts.il](crates/ilang-cli/tests/programs/04_modules/cross_module_pub_module_top_stmts.il) | A sub-module's top-level stmts may reference its own non-pub members. The visibility check must judge those accesses from the sub-module's perspective, not the entry's — without t… |
| [cross_module_pub_positive.il](crates/ilang-cli/tests/programs/04_modules/cross_module_pub_positive.il) | Same-class access through `pub` members is reachable from the entry module — sanity counterpart to the `cross_module_pub_*_access` fixtures. |
| [dup_pub_class_same_file.il](crates/ilang-cli/tests/programs/04_modules/dup_pub_class_same_file.il) | Two `pub class` declarations sharing the same name in the same file. The loader's `dup_pub` pass turns the previously- silent shadow into an actionable load-time error so the user… |
| [dup_pub_struct_via_umbrella.il](crates/ilang-cli/tests/programs/04_modules/dup_pub_struct_via_umbrella.il) | Cross-module duplicate: the umbrella `lib_dup_pub_struct.il` re-exports two siblings (`_a.il` / `_b.il`) that both declare `pub struct Shared`. After the loader merges them they b… |
| [embed_basic.il](crates/ilang-cli/tests/programs/04_modules/embed_basic.il) | `@embed("path") const X: T` — embed a file at compile time. The path is resolved relative to **this** source file. `T` must be either `string` (UTF-8 text; invalid UTF-8 = compile… |
| [events_basic.il](crates/ilang-cli/tests/programs/04_modules/events_basic.il) | Built-in `events` module — minimal Node.js-style EventEmitter. Generic over a single payload type; listeners run in registration order. `off` removes by reference equality on the … |
| [events_emit_reentrancy.il](crates/ilang-cli/tests/programs/04_modules/events_emit_reentrancy.il) | std.events: a listener that mutates the listener set DURING `emit` must not corrupt the in-flight walk. `emit` cached `n = arr.length` and indexed the LIVE array, so a listener ca… |
| [extern_addr_of.il](crates/ilang-cli/tests/programs/04_modules/extern_addr_of.il) | `&local` inside @extern(C): take the address of a stack-bound scalar so a C-style out-pointer call can write through it. This replaces the older idiom of allocating a 1-element il… |
| [extern_addr_of_field.il](crates/ilang-cli/tests/programs/04_modules/extern_addr_of_field.il) | `&path` with a field chain: `&obj.field`, `&obj.f1.f2`. The MIR lowerer emits `UseLocal  LoadField*  AddrOfField`. For ARC class fields, `LoadField` returns the inner class pointe… |
| [extern_addr_of_this.il](crates/ilang-cli/tests/programs/04_modules/extern_addr_of_this.il) | `&this.field` inside a method body. The MIR lowerer resolves `this` like a regular Var, then walks the field chain the same way it does for plain `&local.f` — emitting the receive… |
| [extern_alias_msgsend.il](crates/ilang-cli/tests/programs/04_modules/extern_alias_msgsend.il) | Multiple ilang `@symbol("name")` declarations pointing at the same C symbol but with *different* ilang signatures — the shape Cocoa bindings need to express `objc_msgSend(obj, sel… |
| [extern_cstr_array.il](crates/ilang-cli/tests/programs/04_modules/extern_cstr_array.il) | C-side `char**` (NUL-terminated array of NUL-terminated strings) marshals to ilang `string[]` via the `cstrArrayToStrings(p)` helper. The helper walks the input until it hits a NU… |
| [extern_errno_check.il](crates/ilang-cli/tests/programs/04_modules/extern_errno_check.il) | POSIX-style "return < 0 means failure, errno tells you why", expressed via the `errnoCheck(rc)` / `errnoCheckI64(rc)` helpers inside `@extern(C)`. The helper branches on `rc < 0`;… |
| [extern_free_with.il](crates/ilang-cli/tests/programs/04_modules/extern_free_with.il) | Library-specific deallocator. Many libraries ship their own allocator (sqlite3_free, xmlFree, OPENSSL_free, ...) and crash if you mix them with libc::free. The new pattern keeps t… |
| [extern_handle_struct.il](crates/ilang-cli/tests/programs/04_modules/extern_handle_struct.il) | `@handle struct` — opaque pointer-sized handle types for Win32 / libnotify / Wayland-style SDK pointer typedefs. Per syntax.md §13b "@handle struct Name {}". |
| [extern_objc_block.il](crates/ilang-cli/tests/programs/04_modules/extern_objc_block.il) | `@extern(ObjC) { ... }` block with `@objc("selector:") fn` declarations. The parser desugars each @objc fn into a typed `objc_msgSend` alias plus a wrapper that interns the select… |
| [extern_objc_class.il](crates/ilang-cli/tests/programs/04_modules/extern_objc_class.il) | `@objc class X { ... }` inside `@extern(ObjC)` desugars into a regular ilang class with `handle: i64`  `init(h: i64)` and one ilang method per declared `@objc("sel:")` member. Met… |
| [extern_objc_subclass.il](crates/ilang-cli/tests/programs/04_modules/extern_objc_subclass.il) | `@objc class Foo : NSObject` registers Foo with the ObjC runtime via objc_allocateClassPair  objc_registerClassPair on first call to `Foo.register()`. Methods with bodies become I… |
| [extern_objc_subclass_iface.il](crates/ilang-cli/tests/programs/04_modules/extern_objc_subclass_iface.il) | `@objc class Foo : Parent, Iface1, Iface2 { ... }` — extended base-list syntax. The first base name (if any) is the Objective-C parent class; the rest are ilang interfaces the cla… |
| [extern_objc_super.il](crates/ilang-cli/tests/programs/04_modules/extern_objc_super.il) | `super.method(args)` inside an `@objc class : Parent` body dispatches via `objc_msgSendSuper`, so the parent class's implementation runs even though our own IMP would otherwise sh… |
| [extern_slice_return.il](crates/ilang-cli/tests/programs/04_modules/extern_slice_return.il) | C side returns a `{ T* ptr; size_t len; }` 16 B struct, the caller deconstructs via `arrayFromCArray<T>(s.ptr, s.len)`. User code outside the @extern(C) block never sees a raw poi… |
| [extern_struct_bitfield_overwidth_error.il](crates/ilang-cli/tests/programs/04_modules/extern_struct_bitfield_overwidth_error.il) | A `@bits(N)` width must fit the underlying type (`N ≤ 32` for `u32`). The parser only bounds `1..=64` globally; the per-type check lived in the repr-C class path and was skipped f… |
| [extern_struct_bitfield_signed_error.il](crates/ilang-cli/tests/programs/04_modules/extern_struct_bitfield_signed_error.il) | `@bits(N)` is unsigned-only (docs: "unsigned integers only — u8/u16/u32/u64"). The repr-C CLASS path enforced this, but an `@extern(C) { struct ... }` skipped the bitfield validat… |
| [extern_struct_heap_field_error.il](crates/ilang-cli/tests/programs/04_modules/extern_struct_heap_field_error.il) | An `@extern(C)` struct lays its fields out inline (C layout, no ARC), so a heap field — a non-repr-C object, a non-last dynamic array, a tuple, an optional — has no owner and eith… |
| [extern_symbol_alias.il](crates/ilang-cli/tests/programs/04_modules/extern_symbol_alias.il) | `@symbol("c_name")` lets the ilang-side fn name differ from the C symbol resolved at dlsym time. Useful when:   - The C name conflicts with an ilang keyword / module fn   - You wa… |
| [extern_union_heap_field_error.il](crates/ilang-cli/tests/programs/04_modules/extern_union_heap_field_error.il) | An `@extern(C) union` shares offset-0 storage across all fields, so a heap field (object / string / array-of-heap) dangles the moment a sibling field overwrites it — `u.b = new Bo… |
| [extern_variadic_snprintf.il](crates/ilang-cli/tests/programs/04_modules/extern_variadic_snprintf.il) | Variadic native extern: `int snprintf(char *buf, size_t n, const char *fmt, ...)`. The fixed prefix is `(buf, n, fmt)`; the trailing args supply format specifier values. The JIT b… |
| [fn_value_callback_pass_through.il](crates/ilang-cli/tests/programs/04_modules/fn_value_callback_pass_through.il) | Regression: an ilang `fn(...)` value, when received as a function parameter and re-passed to a C `@extern(C)` function expecting that same callback signature, must reach the C sid… |
| [fs_basic.il](crates/ilang-cli/tests/programs/04_modules/fs_basic.il) | Built-in `fs` module — sync filesystem helpers returning `Result<T, fs.FsError>`. Exercises the happy path for read / write / append / size / rm and the ENOENT error path for a mi… |
| [fs_extra.il](crates/ilang-cli/tests/programs/04_modules/fs_extra.il) | Exercises the Node-style extensions to `fs`: stat / lstat / access, copyFile / cp / realpath, chmod / truncate / utimes, symlink / readlink / link, and mkdtemp. Each block leaves … |
| [gui_dock_margin.il](crates/ilang-cli/tests/programs/04_modules/gui_dock_margin.il) | Pure-logic check for the libs/gui layout layer: `Margin`'s four overloaded constructors and the dock placement algorithm.  Cannot `use gui` here — that pulls in `gui_impl`'s nativ… |
| [inner.il](crates/ilang-cli/tests/programs/04_modules/inner.il) | (ヘッダ無し) |
| [lib_acl.il](crates/ilang-cli/tests/programs/04_modules/lib_acl.il) | Sibling module — visibility-test fixture support. The class has an explicit pub init and pub `outer` (the cross-module caller can reach those), and a non-pub `secret` field plus `… |
| [lib_acl_internals.il](crates/ilang-cli/tests/programs/04_modules/lib_acl_internals.il) | Sibling module — exercises a sub-module's own top-level stmt reading a non-pub static field. The cross-module visibility check must permit this because the access happens within t… |
| [lib_circular_log_other.il](crates/ilang-cli/tests/programs/04_modules/lib_circular_log_other.il) | Cycle partner for `use_circular_log.il`. Has no body of its own — its only role is to close the circular `use` graph so the duplication-prevention path in `apply_use` (canon == en… |
| [lib_circular_other.il](crates/ilang-cli/tests/programs/04_modules/lib_circular_other.il) | Other half of the circular `use` pair — see `use_circular_pair.il`. The `use` declaration here is what previously tripped `LoadError::CircularImport`; with the discover  parse spl… |
| [lib_circular_pub_let_consumer.il](crates/ilang-cli/tests/programs/04_modules/lib_circular_pub_let_consumer.il) | Consumer half of the known-limitation pair (see `use_circular_entry_pub_let_unsupported.il`). When this file is the harness entry, the partner is *not* the entry, so its `pub let … |
| [lib_const.il](crates/ilang-cli/tests/programs/04_modules/lib_const.il) | Sibling module exporting consts. The loader inlines const values at every reference site (compile-time substitution). |
| [lib_counter.il](crates/ilang-cli/tests/programs/04_modules/lib_counter.il) | Sibling module exporting a class. |
| [lib_dup_pub_struct.il](crates/ilang-cli/tests/programs/04_modules/lib_dup_pub_struct.il) | Umbrella module that re-exports both siblings. Each sibling declares `pub struct Shared`, so after this umbrella merges them into a single namespace the loader's `dup_pub` pass se… |
| [lib_dup_pub_struct_a.il](crates/ilang-cli/tests/programs/04_modules/lib_dup_pub_struct_a.il) | Sibling fixture for `dup_pub_struct_via_umbrella.il`. Declares `pub struct Shared` so the umbrella re-export can collide it against the other sibling's declaration. |
| [lib_dup_pub_struct_b.il](crates/ilang-cli/tests/programs/04_modules/lib_dup_pub_struct_b.il) | Companion to `lib_dup_pub_struct_a.il` — also declares `pub struct Shared`, so re-exporting both through an umbrella is what the load-time duplicate check must reject. |
| [lib_fallback.il](crates/ilang-cli/tests/programs/04_modules/lib_fallback.il) | Multi-library fallback: try the primary name first, then any additional names. Useful for soname differences (libssl.so.3 vs libssl.so.1.1) and OS variants. We give a deliberately… |
| [lib_helpers.il](crates/ilang-cli/tests/programs/04_modules/lib_helpers.il) | Sibling module for the use_*.il tests. When run standalone (the harness walks every .il), this file just declares fns and a const — no assertions, exits cleanly. |
| [lib_load_error.il](crates/ilang-cli/tests/programs/04_modules/lib_load_error.il) | `os.libLoadError` surfaces the dlopen failure message when an `@lib(...) @optional` library couldn't be loaded. Used for diagnostics; the gating logic still goes through libLoaded. |
| [lib_pub_let.il](crates/ilang-cli/tests/programs/04_modules/lib_pub_let.il) | Sibling module exporting a `pub let`. Used by `use_pub_let_selective.il` to verify selective imports of non-function top-level bindings. |
| [lib_pubchain_core.il](crates/ilang-cli/tests/programs/04_modules/lib_pubchain_core.il) | Leaf of a 3-level `pub use` chain. Reached by callers as `top.mid.core.echo3()`. The dotted prefix is registered by the parser's `renormalize_merged` so each intermediate (`top`, … |
| [lib_pubchain_mid.il](crates/ilang-cli/tests/programs/04_modules/lib_pubchain_mid.il) | Mid hop of the 3-level `pub use` chain. `pub use M as <alias>` is rejected (the umbrella keeps M's canonical name); re-export under the canonical name. |
| [lib_pubchain_top.il](crates/ilang-cli/tests/programs/04_modules/lib_pubchain_top.il) | Top hop of the 3-level `pub use` chain. Re-exports `mid` (which itself re-exports `core`) under its canonical name; aliasing `pub use` is rejected by design. |
| [lib_shapes.il](crates/ilang-cli/tests/programs/04_modules/lib_shapes.il) | Sibling module exporting an enum. |
| [lib_shared_state.il](crates/ilang-cli/tests/programs/04_modules/lib_shared_state.il) | Sibling module exporting a mutable top-level `let`. The write lives in `lib_shared_writer.il` (a different module), so the loader has to qualify the Assign target across the `use`… |
| [lib_shared_writer.il](crates/ilang-cli/tests/programs/04_modules/lib_shared_writer.il) | Reads AND writes a global imported from another module. The loader's rename pass qualifies `Var` reads (`SHARED_COUNT` -> `lib_shared_state.SHARED_COUNT`) but used to skip the `As… |
| [lib_struct_lit_in_method.il](crates/ilang-cli/tests/programs/04_modules/lib_struct_lit_in_method.il) | Sibling module for `struct_lit_in_method_via_dep.il`. Declares a `pub struct`  a `pub class` whose method constructs the struct via a literal. Standalone run is a no-op (no top-le… |
| [libc_assert.il](crates/ilang-cli/tests/programs/04_modules/libc_assert.il) | libc.assert / libc.assertMsg — happy-path no-op. (We can't easily exercise the abort path in a normal fixture, since aborting the process flags the run as a failure.) |
| [libc_full_surface.il](crates/ilang-cli/tests/programs/04_modules/libc_full_surface.il) | Smoke test that the expanded libc umbrella parses  links. Picks one symbol from each new sub-module so a regression in any of them surfaces here before it bites a downstream bindi… |
| [math_sign_and_nan.il](crates/ilang-cli/tests/programs/04_modules/math_sign_and_nan.il) | std.math edge cases:   * `sign(±0.0)` must be `0.0` (JS `Math.sign`), not `1.0`. The     intrinsic mapped to Rust's `f64::signum`, which returns `±1.0`     for `±0.0` — contradict… |
| [method_generics.il](crates/ilang-cli/tests/programs/04_modules/method_generics.il) | Generic methods on classes — both instance and static. Type arguments are inferred from the call-site argument types; the monomorphizer specializes each (class, method, args) tupl… |
| [optional_lib_missing.il](crates/ilang-cli/tests/programs/04_modules/optional_lib_missing.il) | `@lib(...) @optional` allows the JIT to keep going when the library can't be found — calls to its fns turn into a runtime stub that aborts if invoked. `os.libLoaded(...)` lets the… |
| [optional_lib_present.il](crates/ilang-cli/tests/programs/04_modules/optional_lib_present.il) | libc is present everywhere — declaring it `@optional` should produce the same behaviour as a regular `@lib("c") fn`, with `os.libLoaded("c")` reporting true. |
| [os_errno_after_open_fail.il](crates/ilang-cli/tests/programs/04_modules/os_errno_after_open_fail.il) | `os.errno()` reflects the most recent failing libc call. We trigger a deterministic failure by `fopen`-ing a path that can't exist, then verify errno is non-zero (ENOENT = 2 on ma… |
| [os_errno_basic.il](crates/ilang-cli/tests/programs/04_modules/os_errno_basic.il) | Basic `os.errno()` call — runs in both interpreter and JIT. We can't deterministically set errno from pure ilang code (no failing libc call without `@extern("c")`), so we just ver… |
| [os_errno_set_get.il](crates/ilang-cli/tests/programs/04_modules/os_errno_set_get.il) | Round-trip `os.setErrno` then `os.errno()`, plus exercise the named POSIX constants. Runs in both interpreter and JIT — the errno store is per-thread and both backends use the sam… |
| [os_platform.il](crates/ilang-cli/tests/programs/04_modules/os_platform.il) | `os.platform` is a top-level `pub let` populated once at module init from a host helper. Reads as a property — `"macos"` / `"linux"` / `"windows"`, or `"other"` for any target Rus… |
| [out_pointer_posix_memalign.il](crates/ilang-cli/tests/programs/04_modules/out_pointer_posix_memalign.il) | Out-pointer pattern: a C function writes the resulting handle into a caller-provided slot, and the return value is the error code. We declare the slot as `*Buf` (an opaque struct … |
| [outer.il](crates/ilang-cli/tests/programs/04_modules/outer.il) | Umbrella with namespaced re-export of `inner`. |
| [path_basic.il](crates/ilang-cli/tests/programs/04_modules/path_basic.il) | Built-in `path` module — Node.js-inspired POSIX-style helpers (separator is always `/`). Covers the public API:   sep / delimiter / isAbsolute / basename / dirname / extname /   j… |
| [pending_promise_exit_drain_order.il](crates/ilang-cli/tests/programs/04_modules/pending_promise_exit_drain_order.il) | Regression guard: the event-loop drain at program exit must run while the top-level globals are still alive.  A pending Promise continuation can hold a heap object whose `deinit` … |
| [posix_constants.il](crates/ilang-cli/tests/programs/04_modules/posix_constants.il) | POSIX constant values exposed through the `os` module. Verifies the values agree with the syscall layer on this platform — values that differ across Linux/macOS are intentionally … |
| [promise_all.il](crates/ilang-cli/tests/programs/04_modules/promise_all.il) | Promise.all([p1, p2, p3]) → resolves with array of all values once every input promise has resolved. The `.then` fires on the drain pass at main exit. |
| [promise_all_empty.il](crates/ilang-cli/tests/programs/04_modules/promise_all_empty.il) | `Promise.all([])` resolves immediately with an empty array, per the empty-input fast path. Confirms the `n == 0` branch settles agg without going through the pre-scan / pool path. |
| [promise_all_mixed_sync_async.il](crates/ilang-cli/tests/programs/04_modules/promise_all_mixed_sync_async.il) | `Promise.all` with a mix of synchronously-Resolved inputs and an async-Resolved input (via executor). The agg has to wait for the async input to settle before its `.then` fires — … |
| [promise_all_multiple_rejects.il](crates/ilang-cli/tests/programs/04_modules/promise_all_multiple_rejects.il) | `Promise.all` with multiple asynchronously-rejected upstreams — the aggregate catches the FIRST reject to actually settle. With all three executors running on the same pool, the d… |
| [promise_all_object_values.il](crates/ilang-cli/tests/programs/04_modules/promise_all_object_values.il) | `Promise.all` with HEAP element values: the result array takes over the per-slot retains the resolve stubs accumulated — `build_i64_array` copies raw slots without retaining, so t… |
| [promise_all_race_input_arc.il](crates/ilang-cli/tests/programs/04_modules/promise_all_race_input_arc.il) | `Promise.all` / `Promise.race` receive their input array with a transferred 1 (the lowering retains a non-fresh arg, a fresh literal hands its own reference in — the same conventi… |
| [promise_all_rejects_on_first.il](crates/ilang-cli/tests/programs/04_modules/promise_all_rejects_on_first.il) | `Promise.all` rejects with the first rejection — subsequent resolutions / rejections don't change the aggregate's state. Per syntax.md §13 "rejects on first rejection". |
| [promise_array.il](crates/ilang-cli/tests/programs/04_modules/promise_array.il) | `Promise<i64[]>` / `Promise<string[]>` — await yields the array and downstream code reads `.length` / `[i]`. |
| [promise_basic.il](crates/ilang-cli/tests/programs/04_modules/promise_basic.il) | Promise.resolve, .then chaining with value transformation, .then's running in registration order off the work-stealing pool. Single chain so the fixture's expected output is deter… |
| [promise_catch_recovery.il](crates/ilang-cli/tests/programs/04_modules/promise_catch_recovery.il) | `.catch(handler)` recovers from a rejection — the handler's return value lands in the downstream Promise as Resolved. A `.then` after the recovery sees that value. Per syntax.md §… |
| [promise_chain_intermediate_release.il](crates/ilang-cli/tests/programs/04_modules/promise_chain_intermediate_release.il) | Chained `p.then(f).catch(g)`: the intermediate promise is a FRESH method receiver and must be released after the dispatch — the upstream's waiter holds 1 on the DOWNSTREAM, so the… |
| [promise_chain_propagates_rejection.il](crates/ilang-cli/tests/programs/04_modules/promise_chain_propagates_rejection.il) | A rejection at link 1 of a `.then` chain propagates past every subsequent `.then` until it reaches a `.catch`. Each intermediate `.then` is supposed to skip its handler and pass t… |
| [promise_deferred_resolvers.il](crates/ilang-cli/tests/programs/04_modules/promise_deferred_resolvers.il) | The deferred pattern (JS `Promise.withResolvers`): the executor stores its resolve / reject callbacks into outer containers and returns without settling; timers settle the promise… |
| [promise_executor.il](crates/ilang-cli/tests/programs/04_modules/promise_executor.il) | `new Promise<T>(executor)` — executor body runs on the pool, uses the resolve/reject callbacks to settle. .catch then recovers a typed value. |
| [promise_executor_cell_arc.il](crates/ilang-cli/tests/programs/04_modules/promise_executor_cell_arc.il) | ARC of the three cells `new Promise<T>(executor)` mints: the executor closure arrives with a transferred 1 and the runtime allocates resolve_cb / reject_cb with rc=1 — all three m… |
| [promise_executor_first_settle_wins.il](crates/ilang-cli/tests/programs/04_modules/promise_executor_first_settle_wins.il) | `new Promise(fn(resolve, reject) { ... })` — the executor settles the promise the first time it calls `resolve` or `reject`; subsequent calls are silently ignored. Per syntax.md §… |
| [promise_executor_resolve_then_reject.il](crates/ilang-cli/tests/programs/04_modules/promise_executor_resolve_then_reject.il) | Inside an executor body, calling `resolve(a)` then `reject(b)` in sequence leaves the Promise Resolved with `a` — the second call is a no-op per "first settle wins". Pairs with th… |
| [promise_field_attach_then.il](crates/ilang-cli/tests/programs/04_modules/promise_field_attach_then.il) | Attach a `.then` to a class field of type `Promise<T>` through the instance. The field carries the upstream alive across the access; the `.then` fires once the executor settles on… |
| [promise_float_callback.il](crates/ilang-cli/tests/programs/04_modules/promise_float_callback.il) | A `Promise<f64>` (or any float-valued promise) hands its value to a `.then` callback through a float-register ABI, and a callback that returns a float propagates it correctly to t… |
| [promise_float_executor.il](crates/ilang-cli/tests/programs/04_modules/promise_float_executor.il) | Float-typed promises through the executor path. The executor's `resolve` parameter is `fn(f64)` / `fn(f32)`, so the call site passes the value in a FLOAT register — the runtime mu… |
| [promise_fresh_arg_release.il](crates/ilang-cli/tests/programs/04_modules/promise_fresh_arg_release.il) | A FRESH promise passed as an argument follows the same fresh-transfer rule as every other heap type: the callee's store (field assignment / container push) takes its own retain, s… |
| [promise_in_class_field.il](crates/ilang-cli/tests/programs/04_modules/promise_in_class_field.il) | A class field of type `Promise<T>` participates in ARC like any other heap field — assigning and reading through `this`, and dropping the instance cascade-releases the promise. Co… |
| [promise_optional.il](crates/ilang-cli/tests/programs/04_modules/promise_optional.il) | `Promise<T?>` — await yields an Optional, then match on some/none. Exercises pattern-binding type resolution for `Optional<T>` payload (`some(v)` binding is `T`). |
| [promise_print_state.il](crates/ilang-cli/tests/programs/04_modules/promise_print_state.il) | `console.log(p)` / `${p}` on a Promise print its settled state. Both dispatches used to fall through to the raw-int arm and render the pointer value.  Pending-state prints are det… |
| [promise_race.il](crates/ilang-cli/tests/programs/04_modules/promise_race.il) | Promise.race([p1, p2]) — settles with whichever input promise settles first. With both inputs already-resolved (eager settle), the runtime fires both .then callbacks on the pool; … |
| [promise_race_array_order_tiebreak.il](crates/ilang-cli/tests/programs/04_modules/promise_race_array_order_tiebreak.il) | `Promise.race` with all-synchronously-settled inputs picks the first one in array order (deterministic tie-break). Per the pre-scan added in 84307028. The pool ordering of the eve… |
| [promise_reject.il](crates/ilang-cli/tests/programs/04_modules/promise_reject.il) | Promise rejection paths: - executor reject → .catch recovery (Promise<string>) - Promise.reject(msg) static → .catch (Promise<()>)  Each test runs as its own chain so the chain-in… |
| [promise_resolve_via_executor.il](crates/ilang-cli/tests/programs/04_modules/promise_resolve_via_executor.il) | Mirror of `mk_reject_via_executor`: an executor body that resolves the Promise asynchronously through the pool. Lock in the resolve-side of the double-release fix (e1de6e02) — the… |
| [promise_result.il](crates/ilang-cli/tests/programs/04_modules/promise_result.il) | `Promise<Result<T, E>>` — await yields the Result, then match on ok/err. With `Result` now monomorphized per call site (the previous built-in registration is gone), the `err(e)` b… |
| [promise_stub_merged_registration.il](crates/ilang-cli/tests/programs/04_modules/promise_stub_merged_registration.il) | Release builds merge identical Rust functions (ICF): `promise_resolve_stub` / `promise_race_resolve_stub` (and the reject pair) have identical bodies and collapse to one address. … |
| [promise_then_chain_three_deep.il](crates/ilang-cli/tests/programs/04_modules/promise_then_chain_three_deep.il) | `.then(cb1).then(cb2).then(cb3)` — each link receives the previous link's return value, and the chain settles in order. The intermediate Promises are released as their handlers fi… |
| [promise_then_returns_nested_promise.il](crates/ilang-cli/tests/programs/04_modules/promise_then_returns_nested_promise.il) | `.then(cb)` types out as `Promise<U>` where `U = cb`'s return type — there's no JS-style auto-unwrap of a returned promise. Returning `Promise<i64>` from a `.then` therefore lands… |
| [promise_user_class.il](crates/ilang-cli/tests/programs/04_modules/promise_user_class.il) | `Promise<UserClass>` — await yields the class instance, field access works through it. Exercises the retain/release plumbing in the v2 state machine for heap-typed await bindings. |
| [promise_user_enum.il](crates/ilang-cli/tests/programs/04_modules/promise_user_enum.il) | `Promise<UserEnum>` (non-generic) — variant data flows back through await and match on the resolved value. |
| [promise_user_generic_enum.il](crates/ilang-cli/tests/programs/04_modules/promise_user_generic_enum.il) | `Promise<Box<T>>` — a user-defined generic enum inside Promise. Previously failed with "unsupported in M1: user-defined generic types" because monomorphize_enums skipped FnExpr cl… |
| [pub_use_namespaced.il](crates/ilang-cli/tests/programs/04_modules/pub_use_namespaced.il) | `pub use M` (without `{ * }`) keeps M's items namespaced under the umbrella — callers reach them as `<umbrella>.<M>.<item>`. Use the `as _ { * }` form when you want the flatten be… |
| [pub_use_three_level.il](crates/ilang-cli/tests/programs/04_modules/pub_use_three_level.il) | 3-step `pub use` chain — the entry reaches a fn declared 3 hops deep through the canonical names of each umbrella. The parser's `renormalize_merged` registers every dotted prefix … |
| [pub_use_with_alias_error.il](crates/ilang-cli/tests/programs/04_modules/pub_use_with_alias_error.il) | `pub use module as <alias>` is rejected — the umbrella contract is to re-publish the inner module under its canonical name; an alias on the umbrella side would muddle which name d… |
| [regex_basic.il](crates/ilang-cli/tests/programs/04_modules/regex_basic.il) | Built-in `regex` module — wraps Rust's `regex` crate. Smoke-tests the public API: test / find / findAll / replace / split, with the `i` flag for case-insensitive matching. |
| [regex_capture_flags_arc.il](crates/ilang-cli/tests/programs/04_modules/regex_capture_flags_arc.il) | `std.regex` beyond the smoke test (`regex_basic.il` covers test / find / findAll / replace / split with the `i` flag). This pins the broad surface that was unpinned: capture-group… |
| [repr_c_array_field.il](crates/ilang-cli/tests/programs/04_modules/repr_c_array_field.il) | Fixed-length numeric array as a `@extern(C) struct` field. Bytes are embedded inline (no heap header) so the layout matches the C `T arr[N];` declaration. Index access on the fiel… |
| [repr_c_basic.il](crates/ilang-cli/tests/programs/04_modules/repr_c_basic.il) | `@extern(C) { struct ... }` basics: declare, allocate, read/write fields. Doesn't actually call any native fn — just exercises the layout / zero-init semantics so failures localis… |
| [repr_c_bitfield.il](crates/ilang-cli/tests/programs/04_modules/repr_c_bitfield.il) | `@bits(N)` bitfields on `@extern(C) struct` fields. Consecutive bitfields of the same underlying integer width pack into a single shared storage unit (GCC-style packing); the JIT … |
| [repr_c_bitfield_u64.il](crates/ilang-cli/tests/programs/04_modules/repr_c_bitfield_u64.il) | `@bits(N)` on a `u64` field. Consecutive u64 bitfields pack into a single 8-byte storage unit, and the JIT must read/write that unit at its FULL 64-bit width. The codegen used to … |
| [repr_c_by_value.il](crates/ilang-cli/tests/programs/04_modules/repr_c_by_value.il) | Pass `@extern(C) struct`s **by value** to a host C-ABI fn. The JIT expands each struct param into 1–2 i64 chunks per the AArch64 / SysV "integer-only ≤ 16 B composite" rule. The R… |
| [repr_c_by_value_hfa.il](crates/ilang-cli/tests/programs/04_modules/repr_c_by_value_hfa.il) | HFA (Homogeneous Floating-point Aggregate): a struct of 1..=4 fields all of the same float type passes via FP registers (V0..V3 on AArch64 AAPCS64; XMM0..XMM3 on x86_64 SysV) inst… |
| [repr_c_by_value_indirect.il](crates/ilang-cli/tests/programs/04_modules/repr_c_by_value_indirect.il) | By-value for **structs > 16 B**: the JIT marks the param with Cranelift's `StructArgument(size)` purpose, which delegates to the per-target C ABI (AArch64 AAPCS64 hands the callee… |
| [repr_c_by_value_return.il](crates/ilang-cli/tests/programs/04_modules/repr_c_by_value_return.il) | By-value extended to **struct returns** (≤ 16 B integer-only). The C side returns the struct in 1–2 GPRs (X0 / X0:X1 on AArch64; RAX / RAX:RDX on x86_64 SysV); the JIT receives th… |
| [repr_c_by_value_sret.il](crates/ilang-cli/tests/programs/04_modules/repr_c_by_value_sret.il) | By-value return for **structs > 16 B**: caller allocates the destination buffer and passes a hidden pointer to the C fn via the indirect-result register (X8 on AArch64, RDI on x86… |
| [repr_c_clock_gettime.il](crates/ilang-cli/tests/programs/04_modules/repr_c_clock_gettime.il) | `@extern(C) struct` declares a struct whose memory layout matches a C struct, so it can be passed to native extern fns by pointer. The C side reads/writes fields directly at the u… |
| [repr_c_decl_order.il](crates/ilang-cli/tests/programs/04_modules/repr_c_decl_order.il) | `@extern(C) struct` layout no longer depends on declaration order. Here `rect` is declared **before** the `point` it embeds; the JIT topologically sorts struct layouts so this sti… |
| [repr_c_enum_field.il](crates/ilang-cli/tests/programs/04_modules/repr_c_enum_field.il) | `@extern(C) struct` with a unit-only `enum: uN` field — the field has to lay out at the enum's underlying-int width (`u16` → 2 bytes, `u8` → 1, `u32` → 4) so the struct's offsets … |
| [repr_c_enum_field_unknown_aborts.il](crates/ilang-cli/tests/programs/04_modules/repr_c_enum_field_unknown_aborts.il) | Companion to `repr_c_enum_field.il`: when the C side has written a value into an enum field that doesn't correspond to any declared variant, ilang's CRepr-struct field load must a… |
| [repr_c_flags_enum_field.il](crates/ilang-cli/tests/programs/04_modules/repr_c_flags_enum_field.il) | `@flags` enums hold OR-combined bit values of the underlying integer type, so any bit pattern the C side writes into a `@flags`-typed CRepr struct field is a valid value. Reading … |
| [repr_c_flex_array.il](crates/ilang-cli/tests/programs/04_modules/repr_c_flex_array.il) | C99 flexible array member: `T[]` as the last field of a `@extern(C) struct`. `new StructName(n)` widens the allocation by `n * sizeof(T)` bytes for the trailing variable area; the… |
| [repr_c_nested.il](crates/ilang-cli/tests/programs/04_modules/repr_c_nested.il) | Nested `@extern(C) struct`: an outer struct can embed another `@extern(C) struct` inline (no pointer, no separate allocation). Field offsets follow C struct layout, so the bytes a… |
| [repr_c_packed.il](crates/ilang-cli/tests/programs/04_modules/repr_c_packed.il) | `@packed` drops natural alignment so every field sits at offset = sum of prior sizes (no padding) and the struct's overall alignment is 1. Mirrors C's `__attribute__((packed))`.  … |
| [repr_c_str_field.il](crates/ilang-cli/tests/programs/04_modules/repr_c_str_field.il) | `string` field on a `@extern(C) struct`. Storage is a heap pointer to ilang's `StringRc` (8 bytes), pre-initialised to an interned empty string at construction so reads on a fresh… |
| [repr_c_struct_fixed_array_local.il](crates/ilang-cli/tests/programs/04_modules/repr_c_struct_fixed_array_local.il) | Fixed-length local array whose element type is a top-level @extern(C) struct. Elements are populated with struct literals inside a bare array literal and read back via indexed fie… |
| [repr_c_struct_fixed_array_memcpy.il](crates/ilang-cli/tests/programs/04_modules/repr_c_struct_fixed_array_memcpy.il) | Pass a fixed-length local array of an @extern(C) struct as the source pointer to libc memcpy. The decay must yield a base pointer over the inline element data (no ARC header offse… |
| [repr_c_union.il](crates/ilang-cli/tests/programs/04_modules/repr_c_union.il) | `union` inside `@extern(C)` lays every field at offset 0 — writing one overwrites the others. Useful for C union interop (`siginfo_t`, `union sigval`, etc.) and for type punning b… |
| [simd_int_lane_float_literal_error.il](crates/ilang-cli/tests/programs/04_modules/simd_int_lane_float_literal_error.il) | A SIMD lane element must fit its lane scalar type — the same rule as scalar assignment, where `let x: i32 = 1.0` is rejected (float -> int needs an explicit `as`). The SIMD constr… |
| [simd_vector_types.il](crates/ilang-cli/tests/programs/04_modules/simd_vector_types.il) | SIMD vector types — array-literal coercion against an annotated type slot. Per syntax.md §13b. First-class operations / lane access are not exposed yet; this fixture only exercise… |
| [std_string_from_bytes.il](crates/ilang-cli/tests/programs/04_modules/std_string_from_bytes.il) | `std.ffi.stringFromBytes(p, n)` copies `n` bytes from a raw pointer into a fresh owned ilang `string`. Length-counted companion to `stringFromCstr` — used by binding shims that su… |
| [struct_array_release.il](crates/ilang-cli/tests/programs/04_modules/struct_array_release.il) | Dynamic `Quad[]` built via `push` then dropped at function exit — exercises the release path that previously crashed with a misaligned-pointer dereference (`__release_array` treat… |
| [struct_array_to_c_ptr.il](crates/ilang-cli/tests/programs/04_modules/struct_array_to_c_ptr.il) | Passing an ilang `T[]` (where `T` is a CRepr struct) into a C function that takes `*T` must hand the C side the element data pointer, not the ilang array header. Mirrors the wgpu … |
| [struct_by_value_class_init.il](crates/ilang-cli/tests/programs/04_modules/struct_by_value_class_init.il) | Regression: passing a multi-field struct by value as a positional init argument used to crash the Cranelift verifier — the call site pushed a single pointer while `clif_signature_… |
| [struct_lit_in_method_via_dep.il](crates/ilang-cli/tests/programs/04_modules/struct_lit_in_method_via_dep.il) | `lib_struct_lit_in_method.Builder.make()` builds a `Pair` via struct literal. Without the loader fix that qualifies `StructLit.class`, this fails to load with   "undefined class \… |
| [struct_literal.il](crates/ilang-cli/tests/programs/04_modules/struct_literal.il) | Struct literal syntax: `T { f1: v1, f2: v2 }`. For top-level `struct` / `union` (CRepr) the literal must initialize every declared field — partial / missing fields are a type erro… |
| [struct_top_level.il](crates/ilang-cli/tests/programs/04_modules/struct_top_level.il) | Top-level `struct` / `union` (declared outside `@extern(C) { ... }`). Same value-type / C-layout semantics as `@extern(C) struct`, but the field types are restricted: no `char`, `… |
| [target_filter.il](crates/ilang-cli/tests/programs/04_modules/target_filter.il) | `@target("os")` / `@target(not "os")` — host-OS filter applied by the loader before merge. Same-name fns differentiated by OS leave exactly one survivor per host; same goes for cl… |
| [time_basic.il](crates/ilang-cli/tests/programs/04_modules/time_basic.il) | Exercises the `time` module — wall / monotonic clocks, sleep, DateTime breakdown / composition / ISO round-trip / strftime formatting. Uses a fixed epoch (2026-05-21T15:30:45.123Z… |
| [timer_capture_in_callback.il](crates/ilang-cli/tests/programs/04_modules/timer_capture_in_callback.il) | A `setTimeout` callback captures a heap value (string) from the outer scope. The capture's lifetime must outlive the timer entry — the runtime retains the closure cell on schedule… |
| [timer_clear_timeout.il](crates/ilang-cli/tests/programs/04_modules/timer_clear_timeout.il) | `clearTimeout(id)` cancels a pending timer before it fires — the callback never runs and its captured closure is released when the timer entry is dropped. A short non-cancelled ti… |
| [timer_microtask_order.il](crates/ilang-cli/tests/programs/04_modules/timer_microtask_order.il) | Event-loop ordering guarantees: - same-due timers fire in registration order (the heap's seq   tie-break) - queued continuations run before due timers at a drain point   (JS micro… |
| [timer_set_interval_clear.il](crates/ilang-cli/tests/programs/04_modules/timer_set_interval_clear.il) | `setInterval(cb, ms)` keeps firing until `clearInterval(id)`. Run-to-completion: nothing fires while main's own code runs — the end-of-program drain services the timer heap, re-ar… |
| [timer_set_timeout_fires.il](crates/ilang-cli/tests/programs/04_modules/timer_set_timeout_fires.il) | `setTimeout(cb, ms)` schedules `cb` after `ms` milliseconds. The runtime drains pending timers at program end (mirrors Node's "wait for the event loop"), so a 0ms timer reliably f… |
| [try_operator.il](crates/ilang-cli/tests/programs/04_modules/try_operator.il) | Postfix `?` — Result short-circuit operator. `expr?` unwraps `Result.ok(v)` to `v`, and on `Result.err(e)` early-returns `Result.err(e)` from the enclosing fn (whose own return ty… |
| [use_alias_with_selective.il](crates/ilang-cli/tests/programs/04_modules/use_alias_with_selective.il) | (ヘッダ無し) |
| [use_as_underscore.il](crates/ilang-cli/tests/programs/04_modules/use_as_underscore.il) | `use module as _ { Item }` — namespace-suppressing selective import. Only the listed bare names become visible; no `module.X` / `alias.X` path is registered. Per syntax.md §13b. |
| [use_circular_entry_pub_let_unsupported.il](crates/ilang-cli/tests/programs/04_modules/use_circular_entry_pub_let_unsupported.il) | Documents the one known shape that the circular-`use` support can't currently handle: an entry that itself declares `pub let X = ...` and lives in a `use` cycle where the partner … |
| [use_circular_log.il](crates/ilang-cli/tests/programs/04_modules/use_circular_log.il) | Regression fixture for the entry-stmt duplication bug fixed in 207c4b12. Before the fix, a side-effect statement (`console.log`, `test.expect`, …) in the entry would run *twice* w… |
| [use_circular_pair.il](crates/ilang-cli/tests/programs/04_modules/use_circular_pair.il) | Mutual `use` between two sibling modules. `use_circular_pair` imports `lib_circular_other`, which `use`s `use_circular_pair` back. The loader's discover  parse split absorbs the c… |
| [use_class_from_module.il](crates/ilang-cli/tests/programs/04_modules/use_class_from_module.il) | Instantiating  invoking methods on a class declared in another module. The qualified `new lib_counter.Counter(...)` form is what the loader rewrites class references to. |
| [use_const.il](crates/ilang-cli/tests/programs/04_modules/use_const.il) | Module-qualified const reference: `lib_const.PI` is resolved at compile time and substituted with the literal value, so no runtime lookup happens. |
| [use_enum_from_module.il](crates/ilang-cli/tests/programs/04_modules/use_enum_from_module.il) | Construct  match an enum declared in another module. Both the enum value and the helper fn are accessed via the module name. |
| [use_math.il](crates/ilang-cli/tests/programs/04_modules/use_math.il) | Built-in `math` module — shipped inside the compiler, no on-disk `math.il` needed.  IMPORTANT: every `math.X` intrinsic the runtime exports needs at least one call here so the AOT… |
| [use_pub_let_selective.il](crates/ilang-cli/tests/programs/04_modules/use_pub_let_selective.il) | Selective import of `pub let` (not fn / class / enum) — the bare name binds to the loader-renamed slot and reads land on the same storage the declaring module exports. Per syntax.… |
| [use_selective.il](crates/ilang-cli/tests/programs/04_modules/use_selective.il) | Selective import: `use module { name1, name2 }` brings only the listed names into the local scope, accessed without the module prefix. |
| [use_two_aliases_same_module.il](crates/ilang-cli/tests/programs/04_modules/use_two_aliases_same_module.il) | (ヘッダ無し) |
| [use_whole_module.il](crates/ilang-cli/tests/programs/04_modules/use_whole_module.il) | `use lib_helpers` brings the whole module in under its name; access each member via `lib_helpers.member`. |

## 05_edge_cases (387)

| fixture | pin している内容 |
|---|---|
| [arc_breadth_entries_set_weak_closures.il](crates/ilang-cli/tests/programs/05_edge_cases/arc_breadth_entries_set_weak_closures.il) | Round-23 breadth sweep — NO new bugs (third clean round). Pins the shapes probed: entries() tuple iteration with an early return, Set churn with @derive(Eq, Hash) keys, weak-upgra… |
| [arc_cycle_via_weak.il](crates/ilang-cli/tests/programs/05_edge_cases/arc_cycle_via_weak.il) | Parent ↔ Child where Child holds a `.weak` back-ref to Parent. This is the canonical use of weak refs: prevents a cycle that would otherwise leak (parent held by child held by par… |
| [array_alias_via_let.il](crates/ilang-cli/tests/programs/05_edge_cases/array_alias_via_let.il) | `let alias = arr` rebinds without copying — both names refer to the same heap allocation. Mutations through one are visible through the other. (The class-aliasing case is in 02_cl… |
| [array_index_panic.il](crates/ilang-cli/tests/programs/05_edge_cases/array_index_panic.il) | Reading past the end of an array aborts at runtime. (Both interp and JIT report this; the harness only sees a non-zero exit  stderr.) |
| [array_indexof_includes.il](crates/ilang-cli/tests/programs/05_edge_cases/array_indexof_includes.il) | `.indexOf` returns the first occurrence (or -1), `.includes` returns bool. Edge cases: empty array, missing element, duplicate elements (must hit the FIRST one), heap-typed elemen… |
| [array_literal_common_ancestor.il](crates/ilang-cli/tests/programs/05_edge_cases/array_literal_common_ancestor.il) | Array literal with mixed-but-related class elements unifies to the common ancestor, matching the if/else / match branch unification path.  Pre-fix the type checker fixed the eleme… |
| [array_literal_nested_lengths.il](crates/ilang-cli/tests/programs/05_edge_cases/array_literal_nested_lengths.il) | `[[a, b], [c]]` infers an outer `i64[][]` even though the inner element types differ in static length (`i64[2]` vs `i64[1]`). Pre-fix the type checker pinned the outer element typ… |
| [array_map_filter_chain.il](crates/ilang-cli/tests/programs/05_edge_cases/array_map_filter_chain.il) | `.map` and `.filter` returning fresh arrays, including the empty cases (empty input → empty output, predicate-rejects-all, predicate-accepts-all) and chaining. `closures.il` exerc… |
| [array_of_optionals.il](crates/ilang-cli/tests/programs/05_edge_cases/array_of_optionals.il) | `i64?[]` — array of Optional elements, mixed some/none. Verifies that the optional-discriminant survives storage and indexed retrieval. `optional_*.il` covers single-binding optio… |
| [array_of_tuples.il](crates/ilang-cli/tests/programs/05_edge_cases/array_of_tuples.il) | Arrays whose elements are tuples — each element carries heterogeneous fields accessed via `t[N]` constant index. Tuple- element arrays are useful as records, and the iteration sha… |
| [array_param_aliasing.il](crates/ilang-cli/tests/programs/05_edge_cases/array_param_aliasing.il) | An array passed to a function with a typed `xs: T[]` parameter must alias the caller's binding — mutations through the parameter (index assign, push, pop) have to be visible to th… |
| [array_pop_until_drained.il](crates/ilang-cli/tests/programs/05_edge_cases/array_pop_until_drained.il) | Repeated `pop()` past the end keeps returning none — never panics, never wraps the length below zero. `empty_container_ops.il` shows pop-once on empty; this fixture nails down the… |
| [array_reverse_via_loop.il](crates/ilang-cli/tests/programs/05_edge_cases/array_reverse_via_loop.il) | Manual reverse via a `while` loop building a fresh array. There's no built-in `reverse` method, so this is the canonical pattern; we exercise it across primitive and heap-typed el… |
| [array_slice_independent_buffer.il](crates/ilang-cli/tests/programs/05_edge_cases/array_slice_independent_buffer.il) | `arr.slice(lo, hi)` returns a fresh array — mutations to the slice must NOT leak into the source. The opposite must also hold: the source can mutate without disturbing previously-… |
| [async_fn_returns_result.il](crates/ilang-cli/tests/programs/05_edge_cases/async_fn_returns_result.il) | An `async fn` whose declared return type is a generic enum — e.g. `async fn getR(): Result<Box, string>` — desugars to a poll fn that settles the result promise via `Promise.$prom… |
| [async_zero_await_return.il](crates/ilang-cli/tests/programs/05_edge_cases/async_zero_await_return.il) | A zero-await `async fn` whose body exits via `return` (not a tail expression). The desugar wraps the body's value in `Promise.resolve`, but it used to wrap ONLY the tail — leaving… |
| [bare_field_assign_composite_element_wrap.il](crates/ilang-cli/tests/programs/05_edge_cases/bare_field_assign_composite_element_wrap.il) | Regression guard: a bare-name field write `f = <composite literal>` must construct the literal with the FIELD's declared element types, not the inferred ones. The bare path reuses… |
| [bare_field_assign_composite_widen.il](crates/ilang-cli/tests/programs/05_edge_cases/bare_field_assign_composite_widen.il) | Confirmation-round pin (第34弾): bare-name field writes `f = expr` that need the field's declared type beyond the plain `T -> T?` wrap fixed in 第33弾. After unifying the explicit and… |
| [bare_field_assign_optional_wrap.il](crates/ilang-cli/tests/programs/05_edge_cases/bare_field_assign_optional_wrap.il) | Regression guard: the implicit bare-name field write `f = v` inside a method routed through a degenerate store that ignored the field's declared type — it never applied the `T → T… |
| [break_outside_loop_error.il](crates/ilang-cli/tests/programs/05_edge_cases/break_outside_loop_error.il) | `break` is only meaningful inside a `while` / `loop` / `for` body. Top-level (or a fn body that isn't inside a loop) must reject it so users don't get a silent fall-through. |
| [break_releases_loop_body_locals.il](crates/ilang-cli/tests/programs/05_edge_cases/break_releases_loop_body_locals.il) | `break v` from a loop body skips `lower_block_value`'s normal release pass — anything declared inside the loop body has to be released explicitly before the jump, or its `deinit` … |
| [break_value_in_while_error.il](crates/ilang-cli/tests/programs/05_edge_cases/break_value_in_while_error.il) | `break v` is only allowed in `loop`, not `while` / `for`. |
| [break_value_type_mismatch_error.il](crates/ilang-cli/tests/programs/05_edge_cases/break_value_type_mismatch_error.il) | All `break v` in the same `loop` must have compatible types. |
| [by_value_float_field_error.il](crates/ilang-cli/tests/programs/05_edge_cases/by_value_float_field_error.il) | By-value struct passing on `@extern(C)` packs the struct into 1–2 i64 GPR chunks. A float field would actually flow through FP registers per AArch64 AAPCS64 HFA / x86_64 SysV SSE … |
| [c_only_type_outside_block_error.il](crates/ilang-cli/tests/programs/05_edge_cases/c_only_type_outside_block_error.il) | Calling an `@lib` extern declaration from outside an `@extern(C)` block is rejected — the symbol only resolves through the extern-C codegen path, and ordinary code that called it … |
| [cast_float_to_int_saturating.il](crates/ilang-cli/tests/programs/05_edge_cases/cast_float_to_int_saturating.il) | Float → integer cast must saturate per Rust's `as` semantics (since Rust 1.45 / `cranelift::ins::fcvt_to_*_sat`):   NaN          → 0   Inf         → target type's MAX   -Inf      … |
| [chained_method_receiver_release.il](crates/ilang-cli/tests/programs/05_edge_cases/chained_method_receiver_release.il) | A chained method call `a.m1().m2()` leaked the intermediate `a.m1()` whenever m1 returned an Object. The fresh-receiver release in object method lowering only fired when the metho… |
| [chained_receiver_iface_fnfield.il](crates/ilang-cli/tests/programs/05_edge_cases/chained_receiver_iface_fnfield.il) | Same fresh-intermediate-receiver leak the direct-method path had (round 29), now closed on the two remaining dispatch paths:   - interface VirtCall:  `mkHolder().grab().n`   - clo… |
| [class_const_runtime_reassign_error.il](crates/ilang-cli/tests/programs/05_edge_cases/class_const_runtime_reassign_error.il) | `const` static fields are still one-time-assigned even when the initializer is a runtime expression — explicit reassignment from user code is rejected. |
| [class_ref_equality.il](crates/ilang-cli/tests/programs/05_edge_cases/class_ref_equality.il) | `==` on class instances is reference identity, NOT structural. `class_eq_ref.il` is the dedicated fixture but doesn't combine with array membership / map membership / Optional unw… |
| [closure_array_dispatch.il](crates/ilang-cli/tests/programs/05_edge_cases/closure_array_dispatch.il) | An array of closures — stored, indexed, and iterated. Elements may capture outer state; dispatching by index or via forEach invokes the right one. Exercises closures as first-clas… |
| [closure_as_user_fn_arg.il](crates/ilang-cli/tests/programs/05_edge_cases/closure_as_user_fn_arg.il) | A capturing closure passed to a user-defined higher-order function. The closure runs inside the callee, and because it mutates a shared outer-scope binding the effect is visible a… |
| [closure_bare_field_assign.il](crates/ilang-cli/tests/programs/05_edge_cases/closure_bare_field_assign.il) | Regression guard: a method-internal closure that ASSIGNS to a field by bare name (`slot = nb`, not `this.slot = nb`) must resolve `this` through its captures. Bare field *reads* a… |
| [closure_bare_field_reassign_churn.il](crates/ilang-cli/tests/programs/05_edge_cases/closure_bare_field_reassign_churn.il) | Confirmation-round pin (第35弾): the bare-name field *write* fixed in 第32弾 (capture-based `this` resolution) and routed through `store_value_to_field` in 第33弾 must keep ARC balanced… |
| [closure_bare_method_call_error.il](crates/ilang-cli/tests/programs/05_edge_cases/closure_bare_method_call_error.il) | A bare method call inside a method-internal closure (`compute()` instead of `this.compute()`) is deliberately not supported: only fields and properties are routed through capture … |
| [closure_calls_captured_fn.il](crates/ilang-cli/tests/programs/05_edge_cases/closure_calls_captured_fn.il) | A closure body can call a captured fn-typed local by bare name. Pre-fix both the interp's free-variable scan and the JIT's Call arm only ever looked at top-level fns / `let` bindi… |
| [closure_capture_runs_deinit.il](crates/ilang-cli/tests/programs/05_edge_cases/closure_capture_runs_deinit.il) | A closure that captures a class instance must call the instance's `deinit` when the closure itself is dropped.  Pre-fix the interpreter's `release` recursed through Object / Array… |
| [closure_captures_heap_array.il](crates/ilang-cli/tests/programs/05_edge_cases/closure_captures_heap_array.il) | Closures capture by value at creation time — but for heap-typed captures the value IS the handle, so a mutation through the outer binding is visible inside the closure (and vice-v… |
| [closure_field_no_spurious_this.il](crates/ilang-cli/tests/programs/05_edge_cases/closure_field_no_spurious_this.il) | A closure stored in a class field that captures only a plain local / param used to spuriously capture `this` too: the free-var collector added `this` as a candidate for EVERY bare… |
| [closure_iife.il](crates/ilang-cli/tests/programs/05_edge_cases/closure_iife.il) | Immediately-invoked function expressions: a parenthesised closure called on the spot, `(fn() { ... })()`. Covers a bare IIFE, one that captures an outer binding, and one that retu… |
| [closure_in_field_invoked.il](crates/ilang-cli/tests/programs/05_edge_cases/closure_in_field_invoked.il) | A closure stored in a class field, then invoked indirectly by re-binding to a local. Direct call syntax `(this.f)(x)` isn't supported by the parser yet, so the via-local pattern i… |
| [closure_multi_var_capture.il](crates/ilang-cli/tests/programs/05_edge_cases/closure_multi_var_capture.il) | Closure with multiple mutable captures of different shapes — a primitive counter and a heap-typed string accumulator. Both must share the lifetime of the closure value: their cell… |
| [closure_mutable_capture.il](crates/ilang-cli/tests/programs/05_edge_cases/closure_mutable_capture.il) | A captured-and-mutated variable lives in a shared heap cell: mutations inside the closure body persist across repeated invocations of the same closure. Different `let` instances —… |
| [closure_mutates_captured_class.il](crates/ilang-cli/tests/programs/05_edge_cases/closure_mutates_captured_class.il) | Closure capturing a class instance (heap, by-value capture of the reference) and calling its mutating method. The instance reference is captured inline (immutable binding — the bo… |
| [closure_nested_shared_cell.il](crates/ilang-cli/tests/programs/05_edge_cases/closure_nested_shared_cell.il) | Nested closures: when an outer wrapper has a mutable capture and the body builds an inner closure that captures the SAME name, the inner shares the outer's cell — writes through t… |
| [closure_of_closure_per_call_snapshot.il](crates/ilang-cli/tests/programs/05_edge_cases/closure_of_closure_per_call_snapshot.il) | An outer closure factory that mutates its own captured `n` on each call AND, before returning, snapshots the current `n` into a fresh local. The inner closure captures THAT local … |
| [closure_optional_mutable_capture.il](crates/ilang-cli/tests/programs/05_edge_cases/closure_optional_mutable_capture.il) | Mutable closure capture whose static type is `T?`. Each call reads the current Optional via `if let some(...)`, derives the next value, and reassigns the captured slot to a fresh … |
| [closure_pair_shared_cell.il](crates/ilang-cli/tests/programs/05_edge_cases/closure_pair_shared_cell.il) | Two closures built in the SAME scope, each capturing the same mutable outer-scope name, SHARE one cell (JS / Swift-default semantics): a write through one is visible to the other … |
| [closure_self_recursion.il](crates/ilang-cli/tests/programs/05_edge_cases/closure_self_recursion.il) | `let f: fn(...) = fn(...) { ... f(...) ... }` — a closure whose body calls itself by name. The capture mechanism can't snapshot `f` at construction time (the closure value isn't b… |
| [closure_shared_mutable_capture.il](crates/ilang-cli/tests/programs/05_edge_cases/closure_shared_mutable_capture.il) | Shared mutable capture (JS / Swift-default): a closure that mutates a captured variable writes through to the SAME storage the outer scope (and sibling closures) see. Works for bo… |
| [closure_swap_heap_capture.il](crates/ilang-cli/tests/programs/05_edge_cases/closure_swap_heap_capture.il) | Mutable closure capture of a heap-typed value (a class instance), where the body REASSIGNS the captured slot. The cell-write path has to release the old value's reference and reta… |
| [closure_this_capture_in_method.il](crates/ilang-cli/tests/programs/05_edge_cases/closure_this_capture_in_method.il) | A closure built inside a method body captures `this` implicitly. The closure's body can call methods on `this` — including mutating ones — and the writes hit the original receiver… |
| [closure_transitive_shared_capture.il](crates/ilang-cli/tests/programs/05_edge_cases/closure_transitive_shared_capture.il) | Three levels of nested closures all sharing ONE mutable cell: the innermost writes the outermost factory's captured `n`, and the write is visible up the whole chain and persists a… |
| [compound_assign_index_and_field.il](crates/ilang-cli/tests/programs/05_edge_cases/compound_assign_index_and_field.il) | Compound assignments (`+= -= *= /= %= &= \|= ^= <<= >>=`) on non-trivial l-values: array index, map index, class field. The existing fixtures only exercise compound assignments on… |
| [console_log_dynamic_class.il](crates/ilang-cli/tests/programs/05_edge_cases/console_log_dynamic_class.il) | `console.log(<obj>)` prints the dynamic class name  dynamic field set, matching the interpreter's behaviour for upcasts.  Pre-fix the JIT printed the static class — `let a: A = ne… |
| [console_log_fn.il](crates/ilang-cli/tests/programs/05_edge_cases/console_log_fn.il) | `console.log(<fn>)` parity between interp and JIT.  Pre-fix the JIT printed the raw closure pointer (a meaningless integer) for first-class fn values, while the interpreter prints… |
| [console_log_generic_strip_type_args.il](crates/ilang-cli/tests/programs/05_edge_cases/console_log_generic_strip_type_args.il) | `console.log(<generic instance>)` strips the `<...>` monomorphisation suffix to match the interpreter's `Display`.  Pre-fix the JIT carried monomorphised names like `Box<A>` and `… |
| [console_log_inside_fn.il](crates/ilang-cli/tests/programs/05_edge_cases/console_log_inside_fn.il) | `console.log` inside a fn body / method body — visible everywhere, not just at top-level. Pre-fix the interpreter installed `console` into `self.vars`, but a fn call does `mem::ta… |
| [console_log_map.il](crates/ilang-cli/tests/programs/05_edge_cases/console_log_map.il) | `console.log(map)` parity between interp and JIT.  Pre-fix the JIT printed the raw header pointer; now it walks the map's entries and prints `{k: v, ...}` with recursive value for… |
| [console_log_object_field_order.il](crates/ilang-cli/tests/programs/05_edge_cases/console_log_object_field_order.il) | `console.log(obj)` field order matches between interp and JIT. Pre-fix the JIT sorted alphabetically (`emit_print_object` used `as_str().cmp(...)`) while the interpreter sorted by… |
| [console_log_optional.il](crates/ilang-cli/tests/programs/05_edge_cases/console_log_optional.il) | `console.log(some(<value>))` prints the inner value, not the box pointer. Pre-fix the JIT side passed the raw box pointer to `emit_print_value`, so primitive Optional values print… |
| [console_log_weak.il](crates/ilang-cli/tests/programs/05_edge_cases/console_log_weak.il) | `console.log(weak)` parity between interp and JIT.  Pre-fix the JIT printed `<weak C alive>` instead of the interpreter's `weak(<alive>)`, AND the JIT read the wrong offset for th… |
| [const_div_zero_error.il](crates/ilang-cli/tests/programs/05_edge_cases/const_div_zero_error.il) | const division by zero is rejected at compile time. |
| [const_lit_fits.il](crates/ilang-cli/tests/programs/05_edge_cases/const_lit_fits.il) | Const declarations with explicit numeric annotations have their folded literal value range-checked at load time. Pre-fix `const X: i8 = 200` quietly substituted to `200 as i8` and… |
| [const_local_in_fn.il](crates/ilang-cli/tests/programs/05_edge_cases/const_local_in_fn.il) | Local `const x = expr` inside a fn body — same shape as `let`, but reassignment is rejected. |
| [const_local_reassign_error.il](crates/ilang-cli/tests/programs/05_edge_cases/const_local_reassign_error.il) | `const` is a one-time-assign binding — any later assignment is a type error. |
| [const_modulo_zero_error.il](crates/ilang-cli/tests/programs/05_edge_cases/const_modulo_zero_error.il) | A statically ill-formed const — division / modulo by zero, an out-of-range shift — is a COMPILE error, not a value to demote to the runtime init path (which would panic at module … |
| [const_no_init_error.il](crates/ilang-cli/tests/programs/05_edge_cases/const_no_init_error.il) | `const` requires an initializer expression — the parser rejects bare `const NAME: T` (no `= …`) at decl time. |
| [const_runtime_expression.il](crates/ilang-cli/tests/programs/05_edge_cases/const_runtime_expression.il) | `const` accepts any expression for its initializer (not just compile-time literals). When the RHS isn't constant-foldable the loader demotes it to a runtime one-shot init that run… |
| [continue_outside_loop_error.il](crates/ilang-cli/tests/programs/05_edge_cases/continue_outside_loop_error.il) | `continue` is only meaningful inside a `while` / `loop` / `for` body. Using it outside any loop must be a compile-time error. |
| [control_transfer_not_a_value.il](crates/ilang-cli/tests/programs/05_edge_cases/control_transfer_not_a_value.il) | `return` / `break` / `continue` transfer control and yield no value, so using one as a sub-expression in a value-combining position (here a function argument) is rejected with a c… |
| [crepr_struct_assign_index_field.il](crates/ilang-cli/tests/programs/05_edge_cases/crepr_struct_assign_index_field.il) | `arr[i].kind = Mode.active` — AssignIndex の対象が CRepr struct で、 その内側 inline enum field を更新する path。 lower 経路は `let tmp = arr[i]; tmp.kind = ...` に等価か、 構文として直接の 連鎖を許すかは試して確認する。 |
| [crepr_struct_enum_field_as_by_value_param.il](crates/ilang-cli/tests/programs/05_edge_cases/crepr_struct_enum_field_as_by_value_param.il) | CRepr struct を fn の by-value 引数として受け、 fn 内部で inline enum field を read する。 caller 側が新規に作って渡す (fresh) のと、 既存 Local を渡す (borrowed) の両方を試す。  extern C ABI で CRepr struct を by-value 渡しす… |
| [crepr_struct_enum_field_assign.il](crates/ilang-cli/tests/programs/05_edge_cases/crepr_struct_enum_field_assign.il) | CRepr struct (`@extern(C)` block 内) の unit-only enum field を `field = Enum.variant` で書き込み、 同じ field を読み戻して値が 一致することを確認。  pre-fix (df51064a): `ExprKind::AssignField` の `is_heap` 判定… |
| [crepr_struct_enum_field_assign_in_closure.il](crates/ilang-cli/tests/programs/05_edge_cases/crepr_struct_enum_field_assign_in_closure.il) | CRepr struct を closure で capture して、 closure body 内で inline enum field を書き換える path。 captures_in_scope 経路の AssignField 判定 (838d2dc4 の lower_match_optional / lower_if_let 修正を経由した) が… |
| [crepr_struct_enum_field_flags.il](crates/ilang-cli/tests/programs/05_edge_cases/crepr_struct_enum_field_flags.il) | `@flags` 属性付き enum を CRepr struct の field として持つ。 `@flags` enum は OR-combined bit pattern も valid 値として扱う (codegen が `__enum_unit_get` を呼ぶ、 checked 版ではなく)。 LoadField codegen で `layo… |
| [crepr_struct_enum_field_in_array_elem.il](crates/ilang-cli/tests/programs/05_edge_cases/crepr_struct_enum_field_in_array_elem.il) | CRepr struct (inline enum field を持つ) を ilang の Array に入れて AssignIndex  LoadField の組み合わせを確認。 `arr[i].mode = ...` のような書き方が文法的に許される場合、 AssignField path に内側 で落ちて inline enum slot として扱… |
| [crepr_struct_enum_field_load_to_local.il](crates/ilang-cli/tests/programs/05_edge_cases/crepr_struct_enum_field_load_to_local.il) | CRepr struct (`@extern(C)` block 内) の unit-only enum field を load して local 変数に bind し、 match で分解できることを確認。  `decl/extern_c.rs` は inline 用に `MirTy::CReprEnum(eid)` を field metadata … |
| [crepr_struct_enum_field_match_direct.il](crates/ilang-cli/tests/programs/05_edge_cases/crepr_struct_enum_field_match_direct.il) | `match crepr_struct.enum_field { ... }` — local に bind せず直接 match の scrutinee として渡す path。 `lower_match` の入口で `is_fresh_object_expr(scrutinee)` 判定が走り、 838d2dc4 の PatternBinding `ne… |
| [crepr_struct_enum_field_nested.il](crates/ilang-cli/tests/programs/05_edge_cases/crepr_struct_enum_field_nested.il) | ネストされた CRepr struct: 外側 struct の field に別 CRepr struct を持ち、 内側 struct の enum field を read / write する。 `decl/extern_c.rs` の layout pass は内側 struct の field type (含む `CReprEnum`) を見て… |
| [crepr_struct_enum_field_pass_to_fn.il](crates/ilang-cli/tests/programs/05_edge_cases/crepr_struct_enum_field_pass_to_fn.il) | CRepr struct の inline enum field を fn 引数として渡し、 caller-releases-fresh path が `__release_enum` を inline integer に対して呼ばないことを pin する。  `pkt.kind` は `LoadField` で `MirTy::CReprEnum` から… |
| [crepr_struct_enum_field_via_pointer_arg.il](crates/ilang-cli/tests/programs/05_edge_cases/crepr_struct_enum_field_via_pointer_arg.il) | fn の戻り値で CRepr struct を渡し、 caller 側で inline enum field を read する path。 SDL2 で AudioSpec を作って caller に返す パターン (sdl_audio.il の `init` 内 `new AudioSpec()` の延長) を 最小化したもの。  caller-rel… |
| [crepr_struct_field_discard.il](crates/ilang-cli/tests/programs/05_edge_cases/crepr_struct_field_discard.il) | fn の戻り値で CRepr struct を返し、 caller 側で `let _` で捨てる。 caller-releases-fresh が `__release_object` を呼ばずに済むことを pin (CRepr Object は is_arc_slot=false のため heap release 対象外)。  50 round で `… |
| [crepr_struct_field_in_arc_class.il](crates/ilang-cli/tests/programs/05_edge_cases/crepr_struct_field_in_arc_class.il) | ARC 親 class の field に CRepr struct を持たせる: 親 class は 通常の heap object として retain/release されるが、 内側の CRepr struct field は inline 配置。 内側 struct の `Mode` (CReprEnum) field を read / writ… |
| [crepr_struct_large_byval_param.il](crates/ilang-cli/tests/programs/05_edge_cases/crepr_struct_large_byval_param.il) | 80 byte の CRepr struct を by-value param で渡す。 chunks 上限 超えなので caller 側 `calls.rs::struct_byval_size_with_max` の memcpy scratch 経路に乗る (Cranelift の `StructArgument` purpose が AArch64… |
| [crepr_struct_owned_break_in_while.il](crates/ilang-cli/tests/programs/05_edge_cases/crepr_struct_owned_break_in_while.il) | while ループ内で CRepr struct を `new` し、 一定回数で `break` する。 scope-exit に到達せず break で抜ける path で、 owned CRepr local が `release_top_scope_objects` 経由で正しく free されるか を pin。 break で抜ける round … |
| [crepr_struct_owned_early_return.il](crates/ilang-cli/tests/programs/05_edge_cases/crepr_struct_owned_early_return.il) | fn 内で CRepr struct を `new` し、 条件付きで `return` する。 scope-exit に到達せず early return する path で、 owned CRepr local が `release_top_scope_objects` 経由で正しく free される か、 tail return value の CR… |
| [crepr_struct_return_chained_discard.il](crates/ilang-cli/tests/programs/05_edge_cases/crepr_struct_return_chained_discard.il) | 同じ block 内で複数の CRepr struct discard が連続。 各 discard で sret pre-alloc → call → __mir_free のペアが順序を保って揃う かを pin。 ペアがズレると 50 round で `liveAllocCount` が累積で 大きくなる。  mir-jit only — liveAl… |
| [crepr_struct_return_closure_body_tail.il](crates/ilang-cli/tests/programs/05_edge_cases/crepr_struct_return_closure_body_tail.il) | 匿名関数 (closure) の body の tail に CRepr struct の `new` を 置き、 closure 経由で discard を回す。 closure body の Return が sret 経路に乗り、 release_value=true で callee buffer が回収され るかを pin。 `Inst::Cal… |
| [crepr_struct_return_generic_id.il](crates/ilang-cli/tests/programs/05_edge_cases/crepr_struct_return_generic_id.il) | generic identity fn を CRepr struct で monomorphize する。 `fn id<T>(x: T): T { x }` が `T = Box` で展開されたとき、 引数 (by-value CRepr param) を tail で素通しする path が caller の sret pre-alloc と整合するか… |
| [crepr_struct_return_hfa_virt_call.il](crates/ilang-cli/tests/programs/05_edge_cases/crepr_struct_return_hfa_virt_call.il) | 全 float の CRepr struct を class method 戻り値にして virt_call で 呼ぶ。 internal-fn 規約では HFA 経路を skip して sret 強制 (本 修正の趣旨) なので、 caller (VirtCall) と callee の signature が HFA-eligible なサイズでも合う… |
| [crepr_struct_return_if_arms.il](crates/ilang-cli/tests/programs/05_edge_cases/crepr_struct_return_if_arms.il) | `if` の各 arm から CRepr struct を返す fn を呼んで、 結果を 共通の binding に流す。 各 arm の sret 経路が phi node 越しに 整合し、 binding の Local として scope-exit で 1 回だけ free されるかを pin (二重 free / leak の検出)。  mir-j… |
| [crepr_struct_return_large_sret.il](crates/ilang-cli/tests/programs/05_edge_cases/crepr_struct_return_large_sret.il) | 80 byte の CRepr struct を internal fn で return。 chunks 上限 (IL_BYVAL_CHUNK_MAX = 64) を超えるので、 sret 強制以前から sret 経路に乗っている path。 caller pre-alloc → callee memcpy → free が 50 round 回しても … |
| [crepr_struct_return_via_override_vtable.il](crates/ilang-cli/tests/programs/05_edge_cases/crepr_struct_return_via_override_vtable.il) | 親クラスの CRepr struct return メソッドを子クラスが @override し、 親型変数経由で呼ぶ (= virt_call で dispatch される) ことを pin。 子の実装が呼ばれる  sret 経路が vtable slot にまたがって一貫する  liveAllocCount がフラットなことを 50 round で確認… |
| [crepr_struct_return_via_trampoline.il](crates/ilang-cli/tests/programs/05_edge_cases/crepr_struct_return_via_trampoline.il) | CRepr struct return を trampoline closure 経由で呼んだとき、 caller の sret pre-alloc → trampoline body → 本物の fn の direct call → trampoline body の sret memcpy という二段経路が ABI 整合を 保つことを pin。 tra… |
| [cross_class_method_call.il](crates/ilang-cli/tests/programs/05_edge_cases/cross_class_method_call.il) | Two classes calling each other's methods via stored references. `A.bump` delegates to `B.dec`, which recursively decrements until it reaches 1. Exercises method-dispatch through a… |
| [deep_elif_chain.il](crates/ilang-cli/tests/programs/05_edge_cases/deep_elif_chain.il) | `if/elif/.../else` chain with many arms. The parser folds these into a right-leaning `If { else_branch: If { else_branch: ... } }` tree; lowering must traverse it without explodin… |
| [deep_inheritance_super_chain.il](crates/ilang-cli/tests/programs/05_edge_cases/deep_inheritance_super_chain.il) | Four-level inheritance with super.f() at every override. `inheritance_basic.il` covers two levels (Animal → Dog → Puppy) but doesn't pin down whether `super` resolves to the *imme… |
| [deep_release_binary_tree.il](crates/ilang-cli/tests/programs/05_edge_cases/deep_release_binary_tree.il) | Companion to `deep_release_iterative.il`, which only tears down LINEAR graphs (a `next`-linked list and a cons-list). Here every node owns TWO heap children (`left` and `right`), … |
| [deep_release_iterative.il](crates/ilang-cli/tests/programs/05_edge_cases/deep_release_iterative.il) | Releasing a deep object graph must not recurse per link: the per-kind cascade used to call itself through every object → optional → object hop and blew the native stack at ~100k l… |
| [default_arg_after_required_error.il](crates/ilang-cli/tests/programs/05_edge_cases/default_arg_after_required_error.il) | Once a parameter has a default, every later parameter must too. |
| [deinit_chain_through_pair.il](crates/ilang-cli/tests/programs/05_edge_cases/deinit_chain_through_pair.il) | `Pair` owns two `Tracker`s as fields. When `Pair` drops at scope exit, its own (empty) deinit fires, then the two field references are released — each releasing reference brings t… |
| [deinit_fires_on_top_level_let.il](crates/ilang-cli/tests/programs/05_edge_cases/deinit_fires_on_top_level_let.il) | `deinit` runs for top-level `let` bindings at program exit, matching the JIT's `__main` exit-drop sweep.  Pre-fix the interpreter dropped `Rc`s when the Interpreter struct went ou… |
| [destructure_arc.il](crates/ilang-cli/tests/programs/05_edge_cases/destructure_arc.il) | ARC for destructuring binds. Each heap binding takes its own retained share (the source keeps its slot shares for its own release cascade), and a fresh source's transient 1 drops … |
| [division_by_zero_float.il](crates/ilang-cli/tests/programs/05_edge_cases/division_by_zero_float.il) | Float division by zero does NOT panic — it follows IEEE 754: finite / 0.0 → inf / -inf, 0.0 / 0.0 → NaN. |
| [division_by_zero_int.il](crates/ilang-cli/tests/programs/05_edge_cases/division_by_zero_int.il) | Integer division (and modulo) by zero is a runtime panic. |
| [downcast_arc.il](crates/ilang-cli/tests/programs/05_edge_cases/downcast_arc.il) | ARC balance for `is` / `as?`. Three pinned bugs: DowncastOrNone boxed into a bare 8-byte [value] cell with no rc / kind tag — nothing ever released it (8 bytes leaked per hit) and… |
| [early_exit_payload_escape.il](crates/ilang-cli/tests/programs/05_edge_cases/early_exit_payload_escape.il) | Escape hatches across the early-exit sweeps — the value LEAVES through the exit the sweep is cleaning up after: - `return b` where `b` is a fresh scrutinee's payload: the sweep   … |
| [early_exit_sweep_continue_scrutinee.il](crates/ilang-cli/tests/programs/05_edge_cases/early_exit_sweep_continue_scrutinee.il) | Early-exit ARC sweeps, round 2 (the `return` sweep landed in early_return_scope_release.il): - `continue` jumps past the loop body's scope-exit pass and   released NOTHING before … |
| [early_exit_weak_property_iface.il](crates/ilang-cli/tests/programs/05_edge_cases/early_exit_weak_property_iface.il) | Round-17 surface pins — shapes the early-exit sweeps and ARC machinery must keep exact, none of which had a dedicated fixture: - a weak field's registration inside a fn left via e… |
| [early_return_scope_release.il](crates/ilang-cli/tests/programs/05_edge_cases/early_return_scope_release.il) | An early `return` jumps past `lower_block`'s scope-exit release pass, so `lower_return` sweeps every live heap binding above the param scope (mirroring `lower_break`'s sweep for l… |
| [empty_array_field_assign.il](crates/ilang-cli/tests/programs/05_edge_cases/empty_array_field_assign.il) | `this.field = []` where field is `T[]` works under both interpreter and JIT. Pre-fix the JIT errored with "JIT array literal must have at least one element" because AssignField fe… |
| [empty_array_in_typed_position.il](crates/ilang-cli/tests/programs/05_edge_cases/empty_array_in_typed_position.il) | Empty array literal `[]` flows into any typed slot whose annotation supplies the element type — fn args, tuple elements, `some(...)` inner. Pre-fix only the `let x: T[] = []` path… |
| [empty_array_no_annotation_error.il](crates/ilang-cli/tests/programs/05_edge_cases/empty_array_no_annotation_error.il) | `let x = []` without a type annotation can't pick an element type — the checker requires either `let x: T[] = []` or a non-empty literal. Per syntax.md §3 "Variables". |
| [empty_container_ops.il](crates/ilang-cli/tests/programs/05_edge_cases/empty_container_ops.il) | Operations on empty containers — should be defined behavior, NOT panics, for the "absent" cases. |
| [enum_ctor_type_arg_refine_arg_some_tuple.il](crates/ilang-cli/tests/programs/05_edge_cases/enum_ctor_type_arg_refine_arg_some_tuple.il) | Follow-up to enum_ctor_type_arg_refine_in_stores.il (第48弾): the same enum-ctor type-arg refinement must reach the remaining positions where a constructor like `Result.err("e")` (E… |
| [enum_ctor_type_arg_refine_in_stores.il](crates/ilang-cli/tests/programs/05_edge_cases/enum_ctor_type_arg_refine_in_stores.il) | A generic enum constructor whose type params aren't fully determined by its args (e.g. `Result.err("e")` fixes E but not T, `Maybe.nope` fixes nothing) must take the unfilled para… |
| [enum_ctor_type_arg_refine_payload_and_index.il](crates/ilang-cli/tests/programs/05_edge_cases/enum_ctor_type_arg_refine_payload_and_index.il) | A generic enum constructor whose type params aren't fully resolved by its args (e.g. `Result.err("e")` fixes E but not T) must take the unfilled params from the declared type of t… |
| [enum_recursive_payload.il](crates/ilang-cli/tests/programs/05_edge_cases/enum_recursive_payload.il) | A self-referential enum payload — `node` carries `Tree` recursively, so the payload is a heap-allocated boxed value. Match destructuring must keep the bindings alive across the ar… |
| [enum_repr_implicit.il](crates/ilang-cli/tests/programs/05_edge_cases/enum_repr_implicit.il) | `pub enum E: T { ... }` values flow into a `T`-typed slot without an explicit `as T` cast. Covers assignment positions (function args / struct-field init) and `==` / `!=` comparis… |
| [extern_c_const_null.il](crates/ilang-cli/tests/programs/05_edge_cases/extern_c_const_null.il) | `pub const NAME: T = expr` is allowed inside `@extern(C) { ... }` so binding modules can ship typed C-pointer / size_t constants (canonical example: `NULL`). The annotation and th… |
| [f32_from_f64_all_positions.il](crates/ilang-cli/tests/programs/05_edge_cases/f32_from_f64_all_positions.il) | Clean-round pin for f64 -> f32 narrowing (rounds 134/135): a bare float literal (typed f64) demotes to f32 in EVERY construction / storage position. Rounds 134/135 fixed the two t… |
| [field_heap_reassign_releases_old.il](crates/ilang-cli/tests/programs/05_edge_cases/field_heap_reassign_releases_old.il) | Heap-typed field reassignment must release the previous occupant before storing the new value, otherwise the old value (and any resources it owns) leaks until program exit. Covers… |
| [fixed_array_equality_error.il](crates/ilang-cli/tests/programs/05_edge_cases/fixed_array_equality_error.il) | Structural `==` (round 127) covers DYNAMIC arrays (`T[]`); fixed-length arrays (`T[n]`) have a different storage profile and are deliberately left out, so `==` on them stays a cle… |
| [fixed_array_heap_elem_cells.il](crates/ilang-cli/tests/programs/05_edge_cases/fixed_array_heap_elem_cells.il) | Fixed-length heap-element arrays in EVERY container cell — tuple slots, dynamic-array elements (literal / push / index write), Map values (literal / set), closure captures, Promis… |
| [fixed_array_heap_elem_generic_arg.il](crates/ilang-cli/tests/programs/05_edge_cases/fixed_array_heap_elem_generic_arg.il) | A generic type parameter MAY resolve to a fixed-length heap-element array. Cells that take it by value copy on store (Optional via some()/coercion, enum payloads) — so the Optiona… |
| [fixed_array_heap_elem_return_reassign_fill.il](crates/ilang-cli/tests/programs/05_edge_cases/fixed_array_heap_elem_return_reassign_fill.il) | The last three fixed-length heap-element array restrictions, lifted once the values became ordinary rc'd arrays: - RETURNING one — read semantics, like let/params: returning a   l… |
| [fixed_array_heap_elem_store.il](crates/ilang-cli/tests/programs/05_edge_cases/fixed_array_heap_elem_store.il) | Fixed-length arrays (`T[N]`) with HEAP elements — full ARC support. Ownership model: - a FRESH literal's binding owns the header-less buffer plus one   share per element; scope ex… |
| [fixed_array_optional_tuple_wrap.il](crates/ilang-cli/tests/programs/05_edge_cases/fixed_array_optional_tuple_wrap.il) | Confirmation pin (第45弾): the composite-element-wrap fixes (第36 / 第41) generalize to fixed-length arrays `T[N]`, which use the rc'd representation from 第19弾. All sound:    * Box?[2… |
| [fixed_array_top_level_alias.il](crates/ilang-cli/tests/programs/05_edge_cases/fixed_array_top_level_alias.il) | Top-level `let` bindings live in `__main`, whose exit epilogue has its OWN release loop (decl/bodies.rs) separate from the in-fn scope sweep. A fixed-length-array alias bound at t… |
| [fixed_length_array_for_in.il](crates/ilang-cli/tests/programs/05_edge_cases/fixed_length_array_for_in.il) | Fixed-length array `T[N]` participates in `for-in` and the higher- order array methods (`.map`) the same way as a dynamic `T[]`. `array_basics.il` exercises the literal+index form… |
| [flags_enum_bitwise.il](crates/ilang-cli/tests/programs/05_edge_cases/flags_enum_bitwise.il) | `@flags` enum: `\|`, `&`, `^` on enum values lower through the `__enum_box` host helper — extract each operand's tag, combine with the integer op, box the i64 result back into the… |
| [flags_enum_has_and_not.il](crates/ilang-cli/tests/programs/05_edge_cases/flags_enum_has_and_not.il) | `@flags` enum `has` and `~` were checker-accepted but mis-lowered:   * `f.has(other)` — the synthetic bool method `(f & other) == other`     had no MIR lowering, so any `.has(..)`… |
| [float_special_arith.il](crates/ilang-cli/tests/programs/05_edge_cases/float_special_arith.il) | Float special-value arithmetic. IEEE 754 says inf-inf, inf*0, inf+(-inf) all produce NaN; inf+inf stays inf; finite/inf is 0. Negative zero compares equal to positive zero. |
| [float_to_string.il](crates/ilang-cli/tests/programs/05_edge_cases/float_to_string.il) | `.toString()` on f32 / f64 — pre-fix the MIR lowerer routed the call to a `float_to_string` builtin that wasn't declared in codegen, so any `(1.0).toString()` aborted with "unregi… |
| [fn_default_args_multiple.il](crates/ilang-cli/tests/programs/05_edge_cases/fn_default_args_multiple.il) | Functions with multiple default-valued parameters. `default_args.il` in 01_basics covers a single default param; this fixture pins down behavior with three defaults — partial-supp… |
| [fn_ref_as_arg_and_local.il](crates/ilang-cli/tests/programs/05_edge_cases/fn_ref_as_arg_and_local.il) | Bare references to top-level fns reify as `fn(...): T` values: they can be passed as arguments, bound to locals, and reassigned. Pre-fix the JIT generated a per-name trampoline la… |
| [fn_return_lit_fits.il](crates/ilang-cli/tests/programs/05_edge_cases/fn_return_lit_fits.il) | Same fits-in-target rule that just landed for `let` annotations applied to fn return tails: `fn f(): i8 { 200 }` is rejected (and was previously silently accepted, returning -56 f… |
| [forin_early_return_arc.il](crates/ilang-cli/tests/programs/05_edge_cases/forin_early_return_arc.il) | ARC at a `return` out of a for-in body, both directions:  - the FRESH iterable (`for e in m.entries()`) is released on the   return path — its Release lives in the loop's exit blo… |
| [forin_live_mutation.il](crates/ilang-cli/tests/programs/05_edge_cases/forin_live_mutation.il) | `for x in arr` reads the length every lap (live semantics, like JS `for..of`): a pop inside the body ends the loop early, and a push visits the appended elements. The old lowering… |
| [fresh_receiver_inline_member_read.il](crates/ilang-cli/tests/programs/05_edge_cases/fresh_receiver_inline_member_read.il) | Reading a member straight off a fresh-receiver index — `makeMap()["k"].n` / `makeArr()[1].n` — lowers to `retain v; release receiver; load_field v; release v`. arc_peephole used t… |
| [generic_class_inheritance_error.il](crates/ilang-cli/tests/programs/05_edge_cases/generic_class_inheritance_error.il) | "Inheritance on generic classes isn't supported" (syntax.md §7 L990). A child class can't name an instantiated generic parent like `class Sub: Box<i64> { ... }` — the parser rejec… |
| [generic_class_method_builds_enum.il](crates/ilang-cli/tests/programs/05_edge_cases/generic_class_method_builds_enum.il) | A generic class method that CONSTRUCTS a generic enum used to fail at MIR lower with "unknown enum Maybe" / "unknown enum Result". Class monomorphization (`class.rs::subst_expr`) … |
| [generic_fn_arg_infer_prefers_concrete.il](crates/ilang-cli/tests/programs/05_edge_cases/generic_fn_arg_infer_prefers_concrete.il) | Inferring a generic fn's type params from its args recorded the FIRST binding per param (`or_insert_with`). An argument like `Result.err("e")` pins `E` but leaves `T = Any`; if it… |
| [generic_fn_call_solved_from_context.il](crates/ilang-cli/tests/programs/05_edge_cases/generic_fn_call_solved_from_context.il) | Calling a generic fn whose only `T`-bearing argument can't pin `T` — `f(Result.err("e"))` against `fn f<T>(r: Result<T, string>)` — left the call instantiated as `<Any>`: `Result.… |
| [generic_fn_nested_composition.il](crates/ilang-cli/tests/programs/05_edge_cases/generic_fn_nested_composition.il) | Composing user generic free functions: one generic call's result is the argument to another, and both type params are solved purely by inference (no annotations). Existing generic… |
| [generic_fn_return_type_inference.il](crates/ilang-cli/tests/programs/05_edge_cases/generic_fn_return_type_inference.il) | A generic fn whose type parameter appears ONLY in the return type (`fn makeArr<T>(): T[]`, `fn wrapErr<T>(): Result<T, string>`) can't pin that param from its arguments — inferenc… |
| [generic_fn_returns_result.il](crates/ilang-cli/tests/programs/05_edge_cases/generic_fn_returns_result.il) | Built-in `Result<T, E>` is monomorphized per call site (it has no source enum decl). `monomorphize_fns`, when specializing a generic fn body, re-mangles each generic-enum `EnumCto… |
| [generic_in_tuple_field.il](crates/ilang-cli/tests/programs/05_edge_cases/generic_in_tuple_field.il) | Regression guard: a CONCRETE generic instantiation nested inside a tuple type must be mangled to its monomorphized `Object` name.  第39弾 added a `Type::Tuple` arm to `subst_type` /… |
| [generic_method_chain.il](crates/ilang-cli/tests/programs/05_edge_cases/generic_method_chain.il) | A chained call through a generic method that returns a generic instantiation — `b.remap(f).remap(g).remap(h)` — needs FIXED-POINT monomorphization. Each link's result class (`Box<… |
| [generic_method_on_generic_class.il](crates/ilang-cli/tests/programs/05_edge_cases/generic_method_on_generic_class.il) | A generic method (with its own `<U>` type parameter) on a GENERIC class failed at MIR lowering ("no method apply<string> on class"): class monomorphization renames `Box<T>` to the… |
| [generic_method_returns_generic.il](crates/ilang-cli/tests/programs/05_edge_cases/generic_method_returns_generic.il) | A generic method on a generic class that RETURNS a generic instantiation of that class (`remap<U>: Box<U>` building `new Box<U>`) failed at MIR lowering ("unknown type: Box<U>"). … |
| [generic_method_this_call.il](crates/ilang-cli/tests/programs/05_edge_cases/generic_method_this_call.il) | A generic method on a generic class that calls ANOTHER generic method through `this` (`twice<U> { this.apply(f) }`) failed at MIR lowering ("no method apply<string> on class"). Th… |
| [generic_stack_with_optional_t.il](crates/ilang-cli/tests/programs/05_edge_cases/generic_stack_with_optional_t.il) | Generic class whose methods both consume and return `T?`. The monomorphized `Stack<i64>` and `Stack<string>` each get distinct concrete code, and the `T?` return type has to round… |
| [generic_tuple_type_param.il](crates/ilang-cli/tests/programs/05_edge_cases/generic_tuple_type_param.il) | Regression guard: monomorphization must substitute a generic type parameter nested inside a TUPLE type. `subst_type` recursed into Array / Optional / Weak / Generic / Fn but had n… |
| [if_diverging_branch_value.il](crates/ilang-cli/tests/programs/05_edge_cases/if_diverging_branch_value.il) | An `if` used in value position where ONE branch diverges (`return` / `break` / `todo()` / a nested all-paths-diverging if) must take the join type from the live branch alone, and … |
| [if_else_as_expression.il](crates/ilang-cli/tests/programs/05_edge_cases/if_else_as_expression.il) | `if`/`else` is an expression — it produces a value, the two arm types must unify, and it can nest arbitrarily deep. The basics fixtures only use `if` for control flow; this one pi… |
| [index_store_tuple_element_wrap.il](crates/ilang-cli/tests/programs/05_edge_cases/index_store_tuple_element_wrap.il) | Regression guard: an index-assignment `coll[i] = <composite literal>` must build the literal with the container's declared element/value type, so a tuple whose element needs wrapp… |
| [inheritance_no_override_keyword_error.il](crates/ilang-cli/tests/programs/05_edge_cases/inheritance_no_override_keyword_error.il) | Hiding a parent's method without `override` is an error. |
| [inheritance_orphan_override_error.il](crates/ilang-cli/tests/programs/05_edge_cases/inheritance_orphan_override_error.il) | `override` without a matching parent method is an error. |
| [inheritance_override_sig_mismatch_error.il](crates/ilang-cli/tests/programs/05_edge_cases/inheritance_override_sig_mismatch_error.il) | Override must have the same signature as the parent. |
| [init_calls_overridable_method.il](crates/ilang-cli/tests/programs/05_edge_cases/init_calls_overridable_method.il) | `init` may call methods on `this` that subclasses override. Even though the call appears in the BASE init body, virtual dispatch must resolve to the subclass override (the receive… |
| [int_literal_fits_annotation.il](crates/ilang-cli/tests/programs/05_edge_cases/int_literal_fits_annotation.il) | Integer literals that fit the target's width are accepted; ones that don't need to be rejected at type-check time. Pre-fix the general `assignable` int-narrowing rule fired before… |
| [int_min_max_wrap.il](crates/ilang-cli/tests/programs/05_edge_cases/int_min_max_wrap.il) | Integer boundaries: i64 MIN/MAX literals parse and round-trip. Two's-complement wrap on `MAX  1`. Cast-then-wrap at narrower widths — including the i8 and u8 boundaries that aren'… |
| [int_signed_narrow_overflow.il](crates/ilang-cli/tests/programs/05_edge_cases/int_signed_narrow_overflow.il) | Pins the documented integer-overflow model (docs/syntax.md "Integer overflow"). Arithmetic never panics and wraps two's-complement, but the WIDTH it wraps at depends on the operat… |
| [int_to_string_signed.il](crates/ilang-cli/tests/programs/05_edge_cases/int_to_string_signed.il) | `.toString()` on a signed integer must format the value with its sign, matching Rust's `i*::to_string`. Pre-fix the lowerer widened i8 / i16 / i32 args to i64 via uextend (zero-fi… |
| [interface_raw_ptr_sig_error.il](crates/ilang-cli/tests/programs/05_edge_cases/interface_raw_ptr_sig_error.il) | Interface methods declared outside an `@extern(C) {}` block follow the same "no raw C pointers" rule as fn / class signatures. A top-level `pub interface` referencing `*const u8` … |
| [interface_raw_ptr_sig_extern_c_ok.il](crates/ilang-cli/tests/programs/05_edge_cases/interface_raw_ptr_sig_extern_c_ok.il) | Same `*const u8` parameter that fails at top level is legal when the interface lives inside an `@extern(C) {}` block — the surrounding scope opts into the C type vocabulary. This … |
| [interface_void_ret_outside_extern_c_error.il](crates/ilang-cli/tests/programs/05_edge_cases/interface_void_ret_outside_extern_c_error.il) | C-only return types (`void`, `size_t`, `char` proper) are also blocked in top-level interface signatures. |
| [is_as_sibling_classes.il](crates/ilang-cli/tests/programs/05_edge_cases/is_as_sibling_classes.il) | `is` walks the parent chain — a `D extends B` instance is an `A` AND a `B` AND a `D`, but NOT a sibling `C`. `as?` returns `some(...)` when the dynamic class is a subtype, `none` … |
| [iter_mutation_snapshot.il](crates/ilang-cli/tests/programs/05_edge_cases/iter_mutation_snapshot.il) | Callback-driven iteration must snapshot: the callback may mutate the container mid-walk. Array forEach / map / filter / find / findIndex / every / some read (len, data) once and t… |
| [large_array.il](crates/ilang-cli/tests/programs/05_edge_cases/large_array.il) | Build a 100-element array via push, then sum it. |
| [leak_array_map_heap_result.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_array_map_heap_result.il) | `xs.map(fn(x) { ... })` whose closure returns a heap value (string here) — the result array's drop must cascade-release every produced string so the registry doesn't grow per iter… |
| [leak_array_method_chain.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_array_method_chain.il) | `xs.map(...).filter(...)` produces TWO intermediate fresh arrays per iteration — the map result and the filter result. Both must drop at scope exit. A regression that retains the … |
| [leak_array_push_grow_loop.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_array_push_grow_loop.il) | `host_array_push`'s realloc path doubles the data buffer when the capacity is exceeded. Pre-fix the OLD buffer was leaked on every grow (~log2(N) leaks per N pushes), so a long `x… |
| [leak_assign_index_set_promise_weak.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_assign_index_set_promise_weak.il) | `arr[i] = new_value` for a heap-typed Array element has to release the slot's prior occupant and (for borrowed rhs) retain the new one — pre-fix the `elem_is_heap` match in `ExprK… |
| [leak_builder_method_chain.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_builder_method_chain.il) | Builder pattern: `b.add(1).add(2).add(3)` — each `add` returns `this`, so every intermediate is just a 1 retain on the SAME instance. The trailing `let _ = b` then releases the ch… |
| [leak_call_fresh_heap_args.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_call_fresh_heap_args.il) | Free-fn calls and method calls with fresh heap args follow the same caller-releases-fresh convention as `lower_new`. Without the symmetric post-call release in `lower_call` / `low… |
| [leak_class_fn_field.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_class_fn_field.il) | `new C(fresh_fn)` storing the closure into a `fn(...)` field — `lower_new`'s post-call release only covered `MirTy::Object` fresh args, leaving fresh `Fn` (anonymous closure liter… |
| [leak_class_no_fields_loop.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_class_no_fields_loop.il) | A class with no fields — just methods. The `new` allocation is a 16-byte header-only object. Currently the class-cell free is guarded (see `leak_object_alloc_loop.il`), so the byt… |
| [leak_class_set_field.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_class_set_field.il) | A class field of type `Set<T>` must drop both the set and every element it holds when the owner cell releases. Pre-fix the AOT-side `field_kind_tag` mapped Set → KIND_NONE, so the… |
| [leak_closure_body_cell_str_concat.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_closure_body_cell_str_concat.il) | Reassigning a captured `string` cell from INSIDE the closure body, then returning the cell via a `Var` tail. Two paths have to agree:    - `ExprKind::Assign` → `captures_in_scope`… |
| [leak_closure_captures_closure.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_closure_captures_closure.il) | Two closures built per iteration where the second captures the first by name. The capture is a closure pointer, so the inner closure has a heap-typed (Closure-kind) capture slot t… |
| [leak_closure_cell_loop.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_closure_cell_loop.il) | Per-iteration closure construction allocates a fresh `[fn_addr \| rc \| captures...]` cell. The cell must be freed when its rc reaches 0 — without it, every `let f = fn(): T { ...… |
| [leak_closure_cell_reassign.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_closure_cell_reassign.il) | Reassigning a let-bound variable from outer scope after a closure has captured it pinning the cell. The capture forces the binding into `Binding::Cell` (the cell shared between th… |
| [leak_closure_field_replace_loop.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_closure_field_replace_loop.il) | `obj.f = new_closure` replaces a closure-typed field. The old closure value held by the field has to be released BEFORE the new one is stored, otherwise the field-write path leaks… |
| [leak_closure_heap_capture.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_closure_heap_capture.il) | A closure capturing a heap-typed value (Array here) must retain the captured array on construction and release it on closure drop. Combined with the closure-cell free (`leak_closu… |
| [leak_compound_assign_index_loop.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_compound_assign_index_loop.il) | `xs[i] = v` / `xs[i] *= v` etc. — compound assignment on an array index l-value. The lowering reads the current value, runs the binary op, then stores back. For primitive elements… |
| [leak_early_return_heap_array.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_early_return_heap_array.il) | A function with an early `return` for the empty case AND a fall-through tail expression — both paths produce a heap-typed array. The caller's binding holds the returned array; on … |
| [leak_empty_range_for_in.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_empty_range_for_in.il) | `for x in 5..5 {}` and `for x in 7..=6 {}` are zero-iteration loops — the body never runs. The for-in lowering still has to set up the iter state (start/end checked once) but must… |
| [leak_enum_heap_payload.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_enum_heap_payload.il) | `EnumName.payloadVariant(heapVal)` cell drop must cascade- release any heap-typed payload (Array / Optional / Object / Str / Tuple / Map / Closure). EnumPayload extraction retains… |
| [leak_enum_match_arm_loop.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_enum_match_arm_loop.il) | Per-iteration enum value with a payload, then `match` extracts the inner. The match arm's binding must release the cell back down the cascade chain (a heap payload is retained on … |
| [leak_enum_multi_payload_kinds.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_enum_multi_payload_kinds.il) | An enum with a mix of unit  (i64, i64) tuple-payload  (f64, f64) tuple-payload variants. Per loop iter, one of the three is constructed and immediately consumed by a match. Cell d… |
| [leak_enum_payload_aliased_set.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_enum_payload_aliased_set.il) | `enum E { v: (Set<T>) }` ctor with a non-fresh Set argument must retain it so the enum cell owns its own 1 — otherwise the cell's release-cascade decrements an rc the caller never… |
| [leak_enum_payload_loop.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_enum_payload_loop.il) | `EnumName.payloadVariant(args)` allocates a `[tag \| payload...]` cell of size `(1  n_payload) * 8`. The cell is registered in `ENUM_REGISTRY` (rc  total_bytes) so `host_release_e… |
| [leak_enum_unit_loop.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_enum_unit_loop.il) | Repeated `EnumName.unitVariant` in a hot loop must not allocate a fresh 8-byte cell per construction. The unit-variant cache (3c73a64) interns one cell per (global_enum_id, discri… |
| [leak_escape_closure_with_heap.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_escape_closure_with_heap.il) | A function constructs a heap-typed local AND returns a closure that captures it. The closure carries a 1 on the array; when the closure value drops, the captured array's last refe… |
| [leak_field_reassign_heap.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_field_reassign_heap.il) | `o.f = new_value` where `f` is heap-typed: the field-store path has to release the field's previous value BEFORE the new one lands. Pre-fix a missing release in the field-write lo… |
| [leak_fn_returning_fresh_array.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_fn_returning_fresh_array.il) | A function constructs a fresh `i64[]` and returns it. The caller binds the return to `arr`, reads `arr.length`, and `arr` falls out of scope on the next loop iteration. The return… |
| [leak_for_in_fresh_receiver.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_for_in_fresh_receiver.il) | `for x in make_arr()` — the fresh array returned by `make_arr` has no surviving binding after the loop, so its data buffer  48-byte header must be freed at the loop's exit sweep (… |
| [leak_for_in_string_array.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_for_in_string_array.il) | `for s in fresh_string_array() { ... }` — the iter is a fresh `string[]` whose elements are rc-tracked StringRc entries. The for-in path has to release the iter array (cell  buffe… |
| [leak_fresh_enum_arg.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_fresh_enum_arg.il) | Caller-releases-fresh extended to `MirTy::Enum` args. Pre-fix the `needs_post_release` set in `call_fn.rs` / `calls/object.rs` / `calls/mod.rs` omitted Enum (matching the AssignFi… |
| [leak_fresh_receiver_members.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_fresh_receiver_members.il) | Fresh receivers of builtin members must drop their transient 1 after the dispatch. Three sibling paths used to leak the whole receiver per call:   - method calls on fresh string /… |
| [leak_fresh_set_arg.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_fresh_set_arg.il) | Caller-releases-fresh extended to `MirTy::Set`. Pre-fix the `needs_post_release` set in `call_fn.rs` / `calls/object.rs` / `calls/mod.rs` omitted Set (matching the AssignField ret… |
| [leak_fresh_weak_arg.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_fresh_weak_arg.il) | Caller-releases-fresh extended to `MirTy::Weak`. Weak refs alias the strong object's pointer, but `__retain_weak` / `__release_weak` bump a side-table count that `__release_object… |
| [leak_generic_class_heap_t.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_generic_class_heap_t.il) | Generic class instantiated with a heap-typed T (`Box<i64[]>`). The monomorphized class's field has KIND_ARRAY, so the object drop path has to cascade-release the inner array. A re… |
| [leak_hof_closures.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_hof_closures.il) | Run every higher-order method that takes a closure (array.map / .filter / .forEach / .find / .findIndex / .every / .some / .sort and map.forEach) many times in a loop, then assert… |
| [leak_if_let_str_tail.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_if_let_str_tail.il) | `if let some(s) = <fresh>` の some 分岐で binding を tail として 返す Str バリエーション。 838d2dc4 で `lower_if_let` も PatternBinding 経路に揃ったので、 string の rc が cascade と balance するかを `liveStringCount… |
| [leak_literal_aliased_heap_kinds.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_literal_aliased_heap_kinds.il) | Array / Tuple / Optional literal element retain must cover every kind whose runtime cascade releases the slot. Pre-fix the five `is_heap` lists in `lower/literals.rs` (two array l… |
| [leak_loop_break_heap.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_loop_break_heap.il) | `loop { break heap_value }` — the heap value carried out by `break v` is the loop expression's result. The result binding holds a 1 from the break path; on scope exit the value dr… |
| [leak_map_bool_to_array.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_map_bool_to_array.il) | `Map<bool, T[]>` — a 2-key map where the keys are booleans and the values are heap-typed arrays. The map cell  value cascade must reclaim both arrays on map drop. `leak_map_int_to… |
| [leak_map_delete_loop.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_map_delete_loop.il) | `Map.delete(k)` must release the removed value so `deinit` fires at the deletion site, not at program exit. Pre-fix `host_map_delete` discarded the removed value without `release_… |
| [leak_map_int_to_array.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_map_int_to_array.il) | `Map<i64, T[]>` with array values — index-write replacement must release the previous array. Per-iteration insertions of fresh arrays into a fresh Map must drop both the map cell … |
| [leak_map_keys_values_iter_loop.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_map_keys_values_iter_loop.il) | `m.keys()` and `m.values()` snapshot the map into a fresh `K[]` / `V[]`. Each call allocates a new array; iterating it with `for-in` consumes and drops it. A regression that retai… |
| [leak_map_overwrite_loop.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_map_overwrite_loop.il) | `Map<K, ClassT>["k"] = newValue` must release the displaced previous value so `deinit` fires immediately, not at program exit. Pre-fix `host_map_set` did `m.inner.insert(mk, value… |
| [leak_map_string_keys.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_map_string_keys.il) | `Map<string, V>` must own and release its string keys symmetrically with its values. Pre-fix, host_map_set parked the caller's key pointer in `str_key_origs` without a retain and … |
| [leak_match_as_call_arg.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_match_as_call_arg.il) | `match` の結果をそのまま関数引数として渡すパターン。 caller-releases-fresh と PatternBinding tail-Var Retain の 連携を確認: arm の some(v) { v } で v は borrowed (PatternBinding で needs_retain_on_tail = true → R… |
| [leak_match_enum_fresh_scrut.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_match_enum_fresh_scrut.il) | fresh な `Enum` を `match` で受けて payload binding を arm tail で返すパターン。 47d4f979 の既存 fixture は scrutinee が Local (= 借用) のケースしか pin していない。 ここでは scrutinee が fresh (関数戻り値) で、 838d2dc4 の `l… |
| [leak_match_enum_struct_payload_tail.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_match_enum_struct_payload_tail.il) | `enum E { v: { b: Box } }` の struct-shaped payload binding を arm tail で返すパターン (47d4f979 fixture の borrowed scrut 版に対して、 ここでは fresh scrut)。 838d2dc4 の `lower_match_enum` の struct b… |
| [leak_match_field_assign_rhs.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_match_field_assign_rhs.il) | `obj.field = match opt { some(v) { v } none { default } }` の 形。 RHS の match 結果が field-assign の retain 経路と PatternBinding の tail Retain の両者を通る。 二重 retain に ならないことを確認。  mir-jit only… |
| [leak_match_nested.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_match_nested.il) | 入れ子 `match`: outer の some arm が inner の `match` を scrutinee として持つ。 outer の some binding `v` (Box?) を inner の match に渡し、 inner の some binding `b` (Box) を tail で返す。 PatternBinding の… |
| [leak_match_optional_array_tail.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_match_optional_array_tail.il) | fresh `Optional<T[]>` を `match` で受けて `some(xs) { xs }` を arm tail とするパターン。 838d2dc4 の PatternBinding 経路で inner array の rc が cascade と balance することを `liveAllocCount` で確認 (Box が arr… |
| [leak_match_optional_str_tail.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_match_optional_str_tail.il) | fresh `Optional<string>` を `match` で受けて `some(s) { s }` を arm tail とするパターン。 838d2dc4 で `Binding::PatternBinding` の `needs_retain_on_tail = scrut_is_fresh` が有効になり、 `lower_block_hin… |
| [leak_match_payload_binding.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_match_payload_binding.il) | `match` arms that pattern-bind a heap-typed enum payload must not leak the inner cell. Pre-fix, `EnumPayload` codegen auto-retained the extracted value — an orphan 1 with no match… |
| [leak_method_returns_param.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_method_returns_param.il) | Method / fn that returns its non-this heap param as the tail. Pre-fix this pattern leaked one cell per call — two related causes: (1) lower_block_hinted's tail-alias retain fired … |
| [leak_nested_array_literal.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_nested_array_literal.il) | `i64[][]` literal in a loop — the outer array's drop has to cascade-release each inner array (which itself frees its data buffer). The kind tag for the outer array must be KIND_AR… |
| [leak_nested_heap_cascade.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_nested_heap_cascade.il) | `i64[][]` (array of arrays) — the outer array's drop cascades release on every inner array, so nothing leaks.  `host_release_array` reads the kind_tag header word and dispatches t… |
| [leak_nested_iflet_loop.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_nested_iflet_loop.il) | Two layers of `if let some(...)` — the inner if-let extracts from another Optional value derived inside the outer's body. Both Optional shells must drop on their respective scope … |
| [leak_new_fresh_heap_args.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_new_fresh_heap_args.il) | `new C(fresh_heap_arg)` for every heap arg type whose init's field assignment retains. `lower_new`'s post-call release set must match the field-assign retain set; this fixture pin… |
| [leak_object_alloc_loop.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_object_alloc_loop.il) | Per-iteration object construction allocates a `[class_id \| rc \| fields...]` cell. Currently `release_object` cascades field release but does NOT call `host_mir_free` on the cell… |
| [leak_object_map.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_object_map.il) | `Map<MyClass, V>` keys must retain on `set` and release on `delete` / `clear` / map-drop, exactly like values. Mirror `leak_object_set.il` on the map side: the key is the tracked … |
| [leak_object_set.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_object_set.il) | `Set<MyClass>` must retain on `add` and release on `delete` / `clear` / set-drop. Mirror `leak_map_delete_loop.il` but with a class-typed element: insert  delete in a tight loop s… |
| [leak_optional_cell_loop.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_optional_cell_loop.il) | `some(value)` allocates a 24-byte Optional cell. The cell must be freed when its rc reaches 0 — without it, every per-frame `let opt = some(x)` leaks 24 bytes.  Pre-fix `host_rele… |
| [leak_optional_iflet_loop.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_optional_iflet_loop.il) | `if let some(x) = opt { ... }` extracts the inner value from the Optional cell. The cell's 1 was the let-binding's, and when the binding falls out of scope at loop-end the cell dr… |
| [leak_optional_of_array.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_optional_of_array.il) | `T[]?` — an Optional whose inner is itself heap-typed. The Optional cell carries 1 on the array; if the cell is `none`, no array allocated; if `some(arr)`, both must drop on scope… |
| [leak_optional_weak_field_reassign.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_optional_weak_field_reassign.il) | `obj.weak_field = some(other)` — an Optional<Weak<T>> field. The weak rc on the target is bumped on `some(...)` and released when the Optional shell drops at the next iteration's … |
| [leak_promise_local_drop.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_promise_local_drop.il) | `Promise<T>` locals were never released at scope exit because `release_top_scope_objects`'s `needs_release` list omitted `MirTy::Promise`. Adding it alone breaks the async-fn wrap… |
| [leak_property_getter_heap.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_property_getter_heap.il) | A computed property whose getter returns a heap-typed value (here: an array reference into a field). Per call, the getter produces a 1 on the array; the caller's `.length` consume… |
| [leak_result_match_loop.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_result_match_loop.il) | `Result<T, E>` is the canonical built-in payload-bearing enum — both `ok(v)` and `err(e)` allocate a 2-slot enum cell that has to drop after the consumer reads its inner. The stri… |
| [leak_self_recursive_closure_call.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_self_recursive_closure_call.il) | A self-recursive closure (`let fib = fn() { ... fib(...) ... }`) is called repeatedly. Each call's stack frame allocates closure arg slots and intermediate i64 values, but the clo… |
| [leak_set_string_elems.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_set_string_elems.il) | `Set<string>` locals must be released at scope exit. Pre-fix the scope-exit release pass (`release_top_scope_objects`) omitted `MirTy::Set`, so a set bound with `let s = new Set<.… |
| [leak_static_field_counter.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_static_field_counter.il) | Each `new Counter()` increments a static counter from `init`, each scope-exit drop calls `deinit` which increments another. At end-of-loop both counters MUST equal the iteration c… |
| [leak_string_concat_inplace_reassign.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_string_concat_inplace_reassign.il) | Per-iteration `s = s  n.toString()` — the in-place reassign concat.  `s = s  <expr>` on a string Local routes through the `StrConcatInplace` runtime helper, which grows `s`'s own … |
| [leak_string_concat_loop.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_string_concat_loop.il) | Per-iteration `"prefix"  n.toString()` — the intermediate `toString()` result and the concat output must both be released by the end of each iteration so a long loop doesn't grow … |
| [leak_string_method_results.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_string_method_results.il) | String methods that return fresh strings — `toUpper`, `toLower`, `trim`, `replace`, `slice`, `charAt` — must register with the rc-tracked string registry and release at scope exit… |
| [leak_string_split_result.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_string_split_result.il) | `s.split(",")` returns `string[]` whose elements are fresh `leak_cstring` entries. The result array's drop must cascade-release every entry so the registry doesn't grow per iterat… |
| [leak_struct_lit_heap_fields.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_struct_lit_heap_fields.il) | `class C { f: T = ... }` struct literal (`C { f: borrowed_x }`) has to retain `borrowed_x` so the field's 1 doesn't share an rc share with the source binding. Pre-fix the StructLi… |
| [leak_super_method_call_loop.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_super_method_call_loop.il) | `super.inc()` inside an override resolves at compile time to the parent class's specific function and is dispatched directly (no vtable walk). The call passes `this` through uncha… |
| [leak_template_literal_loop.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_template_literal_loop.il) | Template literals fold parts through `fmt_value`  `str_concat`, both of which mint fresh registry strings. The lowerer used to keep every intermediate (and the result was classifi… |
| [leak_tuple_cell_loop.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_tuple_cell_loop.il) | Per-iteration tuple construction allocates a `[rc \| packed \| elements...]` cell. The cell must be freed when its rc reaches 0 — without it, every `let t = (x, y)` in a loop leak… |
| [leak_tuple_destructure_loop.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_tuple_destructure_loop.il) | `let (a, b) = pair` destructure pattern: the tuple cell is allocated by the literal (`(i, i*2)`), bound to `pair`, then destructured. After the loop body, both `pair` and the (a, … |
| [leak_tuple_field_reassign.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_tuple_field_reassign.il) | `obj.tup = (...)` where the field's type is a tuple — the field- store path has to release the previous tuple cell before storing the new one. Each iteration replaces an existing … |
| [leak_tuple_heap_elements.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_tuple_heap_elements.il) | Tuple cascade dispatches per-element kind, not just Object. `(Array<i64>, Optional<i64>)` etc. must reclaim each element's heap allocation when the tuple drops.  Pre-fix `host_rel… |
| [leak_var_reassign_fn.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_var_reassign_fn.il) | `let f: fn(): T = ...; f = ...` for closure-typed locals. `MirTy::Fn` is on `is_heap` because Fn values are box-allocated closures (env capture cell  function ptr). Pre-fix the As… |
| [leak_var_reassign_heap_kinds.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_var_reassign_heap_kinds.il) | Variable reassignment (`x = new_value`) must release whatever the slot held before the new value lands. Pre-fix the `is_heap` match in `ExprKind::Assign` only listed `MirTy::Objec… |
| [leak_var_reassign_promise.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_var_reassign_promise.il) | `let p: Promise<...> = ...; p = ...` was leaking the prior promise on every reassignment. Pre-fix the `is_heap` match in `ExprKind::Assign` only listed `MirTy::Object`, so a Promi… |
| [leak_var_reassign_promise_await.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_var_reassign_promise_await.il) | Reassigning a `Promise<T>` slot inside an `async fn` where the value comes through `await`. The async lowering wraps the body in a pending-promise wrapper, so the rc accounting ar… |
| [leak_var_reassign_weak.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_var_reassign_weak.il) | `let w: T.weak = ...; w = ...` was leaking a weak-table entry per reassignment. Pre-fix the `is_heap` match in `ExprKind::Assign` only listed `MirTy::Object`, so the prior weak sl… |
| [leak_weak_get_loop.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_weak_get_loop.il) | `weak.get()` returns `T?` — when the target is alive it bumps the strong rc and produces `some(t)`. The caller's `if let some(s) = ...` extracts `s`, runs the body, then drops `s`… |
| [leak_weak_local_drop.il](crates/ilang-cli/tests/programs/05_edge_cases/leak_weak_local_drop.il) | `ClassName.weak` references used to permanently tombstone the underlying object box: `jit_setup`/`aot` skipped registering the class size table for any weak-referenced class, so `… |
| [let_underscore_immediate_drop.il](crates/ilang-cli/tests/programs/05_edge_cases/let_underscore_immediate_drop.il) | `let _ = expr` is a discard binding: the value drops AS SOON AS the let-stmt finishes, not at scope exit. Pre-fix the underscore name was treated like an ordinary binding, so `let… |
| [linked_list_via_optional_field.il](crates/ilang-cli/tests/programs/05_edge_cases/linked_list_via_optional_field.il) | `next: Self?` field forms a linked list. Walking it via `if let some(n) = node.next` recurses cleanly; reassigning `next` to `none` detaches the rest of the chain (the original ta… |
| [live_alloc_probe_nonblocking.il](crates/ilang-cli/tests/programs/05_edge_cases/live_alloc_probe_nonblocking.il) | `test.liveAllocBytes()` pumps the event loop (queued continuations  already-due timers) and returns — it must NOT sleep until a pending timer's due time. With an armed interval a … |
| [loop_and_iflet_shadowing.il](crates/ilang-cli/tests/programs/05_edge_cases/loop_and_iflet_shadowing.il) | A for-loop variable, an inner block's `let`, and `if let some(v)` each introduce a fresh binding scoped to the loop / block / arm that shadows but never overwrites a same-named ou… |
| [loop_break_diverging_value_codegen.il](crates/ilang-cli/tests/programs/05_edge_cases/loop_break_diverging_value_codegen.il) | A `break <diverging-expr>` whose VALUE transfers control — `break (return v)`, or `break (if ... else ...)` where every path returns — must not pin the loop's exit-block param to … |
| [loop_break_todo_diverges.il](crates/ilang-cli/tests/programs/05_edge_cases/loop_break_todo_diverges.il) | A `break <diverging-expr>` (e.g. `break todo()`) must NOT pin the `loop`'s break-value type. The diverging break never actually breaks — control aborts while evaluating `todo()` —… |
| [loop_break_value_ownership.il](crates/ilang-cli/tests/programs/05_edge_cases/loop_break_value_ownership.il) | `loop { break v }` evaluates to v, and `lower_break` guarantees the exit-block value owns a 1 (borrowed break values get a retain, fresh ones keep their own). The loop expression … |
| [loop_no_break_diverges.il](crates/ilang-cli/tests/programs/05_edge_cases/loop_no_break_diverges.il) | A `loop` with NO `break` never falls through to a value — it exits only via `return` (or runs forever). It must type as the fn's return type (divergent), not `()`. Without this, `… |
| [map_index_missing_key_panics.il](crates/ilang-cli/tests/programs/05_edge_cases/map_index_missing_key_panics.il) | Reading a missing key with the index form `m[k]` is a runtime panic — the unchecked read, documented as "missing key panics at runtime", with `m.get(k)` as the safe Optional-retur… |
| [map_keys_values_iteration.il](crates/ilang-cli/tests/programs/05_edge_cases/map_keys_values_iteration.il) | `m.keys()` / `m.values()` produce arrays usable in `for-in`. Each returned array must be sized to `m.size()` and contain (in some order) the live keys / values. The order is unspe… |
| [map_literal_common_ancestor.il](crates/ilang-cli/tests/programs/05_edge_cases/map_literal_common_ancestor.il) | Map literal with mixed-but-related class values unifies to the common ancestor, mirroring the array-literal / branch unification path.  Pre-fix `{"a": new Circle(), "b": new Squar… |
| [map_of_arrays.il](crates/ilang-cli/tests/programs/05_edge_cases/map_of_arrays.il) | `Map<K, T[]>` — values are heap arrays. Index-read returns the shared array reference (mutations through it visible through the map); index-write replaces the slot's array, leavin… |
| [map_set_overwrite.il](crates/ilang-cli/tests/programs/05_edge_cases/map_set_overwrite.il) | Setting the same key repeatedly must overwrite, never grow. `map_basics.il` checks delete  has  get/set on distinct keys but doesn't pin down the "set is upsert, not insert" seman… |
| [map_with_class_values.il](crates/ilang-cli/tests/programs/05_edge_cases/map_with_class_values.il) | `Map<K, ClassT>` — values are heap pointers. Index-write replaces the previous instance (which must be released so its deinit fires only when no other binding holds it); reads ret… |
| [match_arm_optional_none_literal_widening.il](crates/ilang-cli/tests/programs/05_edge_cases/match_arm_optional_none_literal_widening.il) | 023a0ccb で `is_fresh_object_expr` の Match / IfLet 判定に `Str literal` だけ「fresh-equivalent」 として許容したが、 同じ 性質を持つ literal は他にもあるはず:    - `none` (`MirTy::Optional<Unit>`、 NewOptional ins… |
| [match_arm_returns_other_binding_var.il](crates/ilang-cli/tests/programs/05_edge_cases/match_arm_returns_other_binding_var.il) | `arm_returns_own_binding` (body_cx.rs) は arm body tail の `Var(n)` が **arm pattern の binding 名** と一致するときだけ true を返す。 別 binding (例: enclosing scope の Local) と同名でも別 scope な 場合の挙動を pi… |
| [match_arm_yields_enum_refine.il](crates/ilang-cli/tests/programs/05_edge_cases/match_arm_yields_enum_refine.il) | A `match` whose arms YIELD enum ctors, bound to an unannotated `let`, left each arm ctor's type args `Any`. `let res = match r { ok(v) { Result.ok(v) } err(e) { Result.err(e) } }`… |
| [match_enum_payload_binding_str_str_repr.il](crates/ilang-cli/tests/programs/05_edge_cases/match_enum_payload_binding_str_str_repr.il) | `: string` repr の enum を CRepr 親じゃない (= 通常 ARC heap) として持つ class field を match で分解する。 string repr enum は CReprEnum 化対象外 (`is_inline_repr_enum` で int_repr = false) なので、 通常の Enum he… |
| [match_exhaustive.il](crates/ilang-cli/tests/programs/05_edge_cases/match_exhaustive.il) | Match must cover every case. Wildcard `_` catches the rest; explicit per-variant arms work too. |
| [match_int_bool_diverging_arm.il](crates/ilang-cli/tests/programs/05_edge_cases/match_int_bool_diverging_arm.il) | An integer or bool `match` arm that diverges (`return` / `break` / `todo()` / an all-paths-diverging if) must be skipped from the match's value join — like the enum and string mat… |
| [match_int_range_needs_wildcard.il](crates/ilang-cli/tests/programs/05_edge_cases/match_int_range_needs_wildcard.il) | Exhaustiveness on an integer scrutinee is value-range-UNAWARE: even when open-ended range arms (`..0`, `1..`) plus the literal `0` between them mathematically cover every i64, the… |
| [match_on_int_and_string_literal.il](crates/ilang-cli/tests/programs/05_edge_cases/match_on_int_and_string_literal.il) | Match arms on int and string literals — the discriminator is a value, not an enum tag. Wildcard `_` is mandatory since the value space is open. The basics fixtures only exercise e… |
| [match_optional_int_arm_fresh_widening.il](crates/ilang-cli/tests/programs/05_edge_cases/match_optional_int_arm_fresh_widening.il) | 023a0ccb の Match fresh 判定は arm body が `Str` literal の場合 にだけ「fresh-equivalent」として許容する。 int / bool literal も rc=-1 相当 (= release-safe) なので、 同じ扱いで良いはず。 現状の判定では int literal の arm が混じる… |
| [match_payload_shadows_outer.il](crates/ilang-cli/tests/programs/05_edge_cases/match_payload_shadows_outer.il) | A pattern binding inside a match arm introduces a fresh local scoped to that arm. It must shadow any outer variable of the same name without overwriting it. |
| [method_chain_returns_self.il](crates/ilang-cli/tests/programs/05_edge_cases/method_chain_returns_self.il) | Builder-pattern chain: each method returns `this` for fluent calls, and the side effect on `this.n` must accumulate exactly once per hop. A regression that re-evaluates the receiv… |
| [method_field_shadows_top_level_let.il](crates/ilang-cli/tests/programs/05_edge_cases/method_field_shadows_top_level_let.il) | A top-level `let` whose name collides with a class field used to hijack the field reference inside that class's methods: the bare `Var(name)` resolver in `lower_var_expr` consulte… |
| [method_tail_bare_top_level_fn.il](crates/ilang-cli/tests/programs/05_edge_cases/method_tail_bare_top_level_fn.il) | Class method whose tail is the bare name of a **top-level fn** (not a field). After the body_cx.rs fix, `tail_is_borrow` matches `ExprKind::Var(name)` when `this_class.is_some()` … |
| [method_tail_bare_var_if_arm.il](crates/ilang-cli/tests/programs/05_edge_cases/method_tail_bare_var_if_arm.il) | Method body whose tail is an `if/else` expression where each arm is a bare-var implicit field reference. Before the in_fn_body_top gating, `lower_block_hinted` applied the borrow-… |
| [method_tail_bare_var_match_arm.il](crates/ilang-cli/tests/programs/05_edge_cases/method_tail_bare_var_match_arm.il) | Method body whose tail is a `match` whose arms are bare-var implicit field references. Same shape as the if-arm regression (05_edge_cases/method_tail_bare_var_if_arm.il), but rout… |
| [method_tail_match_enum_payload.il](crates/ilang-cli/tests/programs/05_edge_cases/method_tail_match_enum_payload.il) | `match` arm whose tail is the pattern-bound payload variable of a tuple-payload enum variant. The payload binding looks like a bare `Var(b)` to lower; it must NOT be misidentified… |
| [mixed_freshness_join.il](crates/ilang-cli/tests/programs/05_edge_cases/mixed_freshness_join.il) | Mixed-freshness joins — an `if` / `match` / `if let` whose branches disagree on ownership (one arm builds a fresh value, the other hands back a param / binding). The join now NORM… |
| [modulo_by_zero.il](crates/ilang-cli/tests/programs/05_edge_cases/modulo_by_zero.il) | Integer modulo-by-zero shares the same panic path as division-by-zero (the `division_by_zero_int.il` comment hints at this — but no test actually exercised the `%` operator, so a … |
| [mono_recursion_limit_error.il](crates/ilang-cli/tests/programs/05_edge_cases/mono_recursion_limit_error.il) | A generic class whose method names a STRICTLY DEEPER instantiation of itself (`Wrap<T>.doubled(): Wrap<Wrap<T>>`) can never finish monomorphizing — each level is a distinct concre… |
| [multi_level_field_mutation.il](crates/ilang-cli/tests/programs/05_edge_cases/multi_level_field_mutation.il) | `o.inner.v = 99` writes to the `v` field of the object reachable through `o.inner` — the existing Inner instance. Replacing `o.inner` with a fresh Inner detaches the original (whi… |
| [nan_ordering.il](crates/ilang-cli/tests/programs/05_edge_cases/nan_ordering.il) | NaN ordering: every comparison involving NaN — including the reflexive `<=` / `>=` — must return false. `division_by_zero_float.il` covers `==`/`!=` reflexivity; this one covers t… |
| [negative_division.il](crates/ilang-cli/tests/programs/05_edge_cases/negative_division.il) | Integer division truncates toward zero (Rust / C semantics); modulo follows the same rule, so `(a/b)*b  a%b == a` always holds. |
| [nested_closure_field_capture.il](crates/ilang-cli/tests/programs/05_edge_cases/nested_closure_field_capture.il) | Regression guard for the on-demand `this` capture (b12fe580): a NESTED closure that reads a field by bare name must still capture `this`. The first cut of the fix only resolved `t… |
| [nested_enum_ctor_refine.il](crates/ilang-cli/tests/programs/05_edge_cases/nested_enum_ctor_refine.il) | `refine_enum_ctor_args` filled an enum ctor's OWN type args from the target but never recursed into the ctor's PAYLOAD arguments. So a nested ctor like `Result.ok(Maybe.nope)` aga… |
| [nested_generic.il](crates/ilang-cli/tests/programs/05_edge_cases/nested_generic.il) | Deeply nested generic class instantiation. Each (Box, args) pair monomorphizes to its own concrete class; nesting works without depth limits. |
| [nested_loop_break_continue.il](crates/ilang-cli/tests/programs/05_edge_cases/nested_loop_break_continue.il) | In nested for-loops, `break` and `continue` must affect the innermost loop only. A regression that hoists either to the outer loop is easy to write and silent — the program just r… |
| [nested_optional.il](crates/ilang-cli/tests/programs/05_edge_cases/nested_optional.il) | Optional<Optional<T>>. The interpreter handles this via Box<Option<...>>; the JIT's nullable-pointer Optional layout can't distinguish Some(none) from none for the same inner poin… |
| [numeric_cast_chain.il](crates/ilang-cli/tests/programs/05_edge_cases/numeric_cast_chain.il) | Multi-step `as` casts. `numeric_conversions.il` covers single-hop casts with in-range values; this fixture stresses chains where each hop changes width and/or sign and may truncat… |
| [numeric_optional_wrap_coerce.il](crates/ilang-cli/tests/programs/05_edge_cases/numeric_optional_wrap_coerce.il) | Boxing a numeric value into a DIFFERENTLY-typed numeric Optional must convert it to the inner type first. `8.5` (f64) into an `f32?` slot used to store the raw f64 — the f64/f32 w… |
| [objc_fn_outside_extern_block_error.il](crates/ilang-cli/tests/programs/05_edge_cases/objc_fn_outside_extern_block_error.il) | `@objc("selector:") fn ...` declarations live inside an `@extern(ObjC) { ... }` block and desugar to a typed `objc_msgSend` alias plus a wrapper. The wrapper takes `*objc_class` /… |
| [object_eq_across_subclass.il](crates/ilang-cli/tests/programs/05_edge_cases/object_eq_across_subclass.il) | `obj_a == obj_b` should work when one operand is a subclass of the other — both runtimes compare by reference identity, the upcast doesn't change the underlying pointer.  Pre-fix … |
| [optional_weak_from_strong.il](crates/ilang-cli/tests/programs/05_edge_cases/optional_weak_from_strong.il) | A strong `Object` flowing into a `Node.weak?` slot is the bare strong→weak auto-downgrade composed with the `T → T?` wrap. An explicit `some(strongRef)` worked (it coerces the inn… |
| [optional_wrap_set_retain.il](crates/ilang-cli/tests/programs/05_edge_cases/optional_wrap_set_retain.il) | Wrapping a `Set` into an `Optional` must retain the set, like every other heap inner. `retain_if_heap` (used by `coerce`'s `T → T?` wrap arm, closure captures, and the some-field … |
| [override_covariant_return.il](crates/ilang-cli/tests/programs/05_edge_cases/override_covariant_return.il) | Method override may narrow the return type to a subclass — standard OO covariant-return semantics. Pre-fix the type checker rejected `override clone(): A` (A extends S) of `clone(… |
| [primitive_unknown_method_error.il](crates/ilang-cli/tests/programs/05_edge_cases/primitive_unknown_method_error.il) | Numeric / bool primitives have a tiny built-in method set (`toString`, plus `isFinite` / `isNaN` on floats). Typos like `isNan` (JS convention is `isNaN` — both Ns capitalised) us… |
| [property_cross_getter.il](crates/ilang-cli/tests/programs/05_edge_cases/property_cross_getter.il) | One getter calling another (`isFreezing` → `description`). The getter dispatch path has to support re-entry on the same instance without confusing `this` or recursing into the set… |
| [pub_deinit_error.il](crates/ilang-cli/tests/programs/05_edge_cases/pub_deinit_error.il) | `deinit` is the destructor — invoked by the ARC runtime when the rc reaches zero, never by user code. Marking it `pub` implies external callability that doesn't exist; the type ch… |
| [question_op_optional.il](crates/ilang-cli/tests/programs/05_edge_cases/question_op_optional.il) | `?` on an Optional — `none` propagates (early `return none` from the enclosing fn, which must be a sync fn returning T?), `some` unwraps. The parser's type-blind `?` desugar is re… |
| [range_backwards_and_singleton.il](crates/ilang-cli/tests/programs/05_edge_cases/range_backwards_and_singleton.il) | Range edge cases that `for_in_range.il` doesn't pin down: a "backwards" range (start > end) is empty, NOT iterating from start downward; an inclusive single-element range hits exa… |
| [range_non_int_error.il](crates/ilang-cli/tests/programs/05_edge_cases/range_non_int_error.il) | 範囲の端は整数型のみ。 |
| [range_outside_for_error.il](crates/ilang-cli/tests/programs/05_edge_cases/range_outside_for_error.il) | 範囲式は for-in のイテレータ位置でしか使えない。 |
| [raw_ptr_outside_block_error.il](crates/ilang-cli/tests/programs/05_edge_cases/raw_ptr_outside_block_error.il) | Raw C pointer types (`*T` / `*const T`) are nameable only inside an `@extern(C) {}` block — using one in user code outside the block must surface as a type error so raw pointers s… |
| [recursion_deep.il](crates/ilang-cli/tests/programs/05_edge_cases/recursion_deep.il) | Recursive function — classic fib. Verifies the call stack handles depth ≥ 20 in both interp and JIT. |
| [recursive_method_optional_tree.il](crates/ilang-cli/tests/programs/05_edge_cases/recursive_method_optional_tree.il) | Self-recursive method on a class whose fields hold `Self?`. Walks the tree via `if let some(...)`. Combines: optional unwrap on instance fields, recursive method dispatch (the sam… |
| [release_map_and_tuple_runs_deinit.il](crates/ilang-cli/tests/programs/05_edge_cases/release_map_and_tuple_runs_deinit.il) | When a Map / Tuple holding class instances is released, the interpreter must walk its values and call `deinit` on each — matching the JIT's per-Map / per-Tuple drop wrapper.  Pre-… |
| [repr_enum_int_literal_compare.il](crates/ilang-cli/tests/programs/05_edge_cases/repr_enum_int_literal_compare.il) | A `pub enum E: T` value compares against a `T`-typed VARIABLE (`msg == WindowMessage.destroy`, `msg: u32`) via the enum-repr promotion. But the same comparison against an int LITE… |
| [return_from_loop_body.il](crates/ilang-cli/tests/programs/05_edge_cases/return_from_loop_body.il) | `return` inside a `for-in` / `while` body must unwind the loop AND the enclosing function. A regression that only `break`s the loop would leave the function falling through to the… |
| [set_arc_placements.il](crates/ilang-cli/tests/programs/05_edge_cases/set_arc_placements.il) | Confirms `Set<T>` ARC is uniform across the placements that the wrap-coerce fix (第77弾, optional_wrap_set_retain.il) did NOT touch — variable / Optional reassignment and an early `… |
| [static_array_field_error.il](crates/ilang-cli/tests/programs/05_edge_cases/static_array_field_error.il) | A `static` class field's `LoadStatic` / `StoreStatic` codegen only handles single-word slot values (numeric / bool / string pointer). The checker used to also accept a dynamic arr… |
| [static_collides_with_method_error.il](crates/ilang-cli/tests/programs/05_edge_cases/static_collides_with_method_error.il) | A static method can't share a name with an instance method. |
| [static_const_assign_error.il](crates/ilang-cli/tests/programs/05_edge_cases/static_const_assign_error.il) | Class-level `const name: T = …` declares an immutable static slot. Reassigning it through `ClassName.name = …` must be a compile-time error — `static name: T = …` (without `const`… |
| [static_field_runtime_init.il](crates/ilang-cli/tests/programs/05_edge_cases/static_field_runtime_init.il) | Static field initializers accept any expression — when the RHS isn't compile-time constant the loader demotes it to a runtime init that runs once at program startup. The slot itse… |
| [static_field_shared_with_subclass.il](crates/ilang-cli/tests/programs/05_edge_cases/static_field_shared_with_subclass.il) | `static_fields.il` covers reading/writing a static field on the declaring class. This fixture pins down the next layer: a subclass inherits the SAME backing storage — bumps from a… |
| [static_field_unsupported_type_error.il](crates/ilang-cli/tests/programs/05_edge_cases/static_field_unsupported_type_error.il) | Static field types are limited to numeric primitives, bool, string, or dynamic arrays of numeric primitives. Object types still need a slot-init phase the JIT doesn't ship yet. |
| [static_no_this_error.il](crates/ilang-cli/tests/programs/05_edge_cases/static_no_this_error.il) | `this` is invalid in a static method body. |
| [stmt_starting_with_bracket.il](crates/ilang-cli/tests/programs/05_edge_cases/stmt_starting_with_bracket.il) | A statement that starts with an array literal `[...]` is its own statement, not a subscript glued onto the previous expression. The postfix index only binds when `[` is on the sam… |
| [strategy_pattern_via_fn_field.il](crates/ilang-cli/tests/programs/05_edge_cases/strategy_pattern_via_fn_field.il) | Strategy-pattern shape: a class stores a `fn(...)` field and dispatches through it. The field can be replaced at runtime to swap behaviour. Verifies that the closure value survive… |
| [string_arc_chains_template_selfconcat.il](crates/ilang-cli/tests/programs/05_edge_cases/string_arc_chains_template_selfconcat.il) | Confirmation pin (第44弾): string-ARC shapes around the in-place concat fix (第43弾). All sound — no string-registry growth across a long churn:    * method chain on a fresh string  (… |
| [string_buffer_alignment.il](crates/ilang-cli/tests/programs/05_edge_cases/string_buffer_alignment.il) | Strings live as `[ i64 length \| UTF-8 bytes \| \0 ]` on the heap; the user-visible pointer points at the first byte. `host_str_*` helpers read the length prefix as `*((p - 8) as … |
| [string_char_iteration.il](crates/ilang-cli/tests/programs/05_edge_cases/string_char_iteration.il) | Iterate a string character-by-character via `charAt(i)` for `i` in `0..length`. `length` is in code points (not bytes), so multibyte strings round-trip cleanly. `string_edge.il` c… |
| [string_edge.il](crates/ilang-cli/tests/programs/05_edge_cases/string_edge.il) | Edge cases for the string built-ins: empty input, multibyte characters, out-of-range indices. |
| [string_ordering_error.il](crates/ilang-cli/tests/programs/05_edge_cases/string_ordering_error.il) | Strings support `+` (concatenation) and `==` / `!=` (structural equality) but NOT ordering — `<` / `<=` / `>` / `>=` between two strings is a type error, not a silent byte/length … |
| [string_replace_overlapping.il](crates/ilang-cli/tests/programs/05_edge_cases/string_replace_overlapping.il) | `replace` patterns where the replacement text contains characters from the search needle, where the needle appears repeatedly, and where the replacement length differs from the ne… |
| [string_repr_enum.il](crates/ilang-cli/tests/programs/05_edge_cases/string_repr_enum.il) | `: string`-repr enums — each unit variant carries an explicit string discriminant (`= "literal"`). The value is reachable via `enumValue as string`; `match` still dispatches on th… |
| [string_repr_enum_flags_error.il](crates/ilang-cli/tests/programs/05_edge_cases/string_repr_enum_flags_error.il) | `@flags` is incompatible with `: string`-repr enums — bitwise ops are int-only, and ORing two strings has no meaning. The MIR lowerer rejects the combination. |
| [string_repr_enum_int_disc_error.il](crates/ilang-cli/tests/programs/05_edge_cases/string_repr_enum_int_disc_error.il) | On a `: string`-repr enum every variant must declare its discriminant as a string literal. Mixing in an integer discriminant is a lower-time error. |
| [string_split_and_escape_edges.il](crates/ilang-cli/tests/programs/05_edge_cases/string_split_and_escape_edges.il) | String escape characters round-trip through `.length` (which counts code points), and split() handles consecutive separators  trailing separator the JS way (every position contrib… |
| [string_unicode_escapes_and_combining.il](crates/ilang-cli/tests/programs/05_edge_cases/string_unicode_escapes_and_combining.il) | `\u{...}` code-point escapes, combining characters, and the code-point (not byte) basis of the string methods that take or return an index. `string_edge.il` covers `.length` / `ch… |
| [string_with_nul_byte.il](crates/ilang-cli/tests/programs/05_edge_cases/string_with_nul_byte.il) | Strings carry a leading `i64 length` header so embedded NUL bytes don't truncate `length`, comparisons, or concatenation. The trailing `\0` is for cstr-compat at the FFI boundary … |
| [struct_by_value_calls.il](crates/ilang-cli/tests/programs/05_edge_cases/struct_by_value_calls.il) | True by-value passing for CRepr struct / union across ilang → ilang calls. Previously these were pointer-passed, which both gave aliasing semantics that conflicted with the docume… |
| [struct_outside_extern_block_error.il](crates/ilang-cli/tests/programs/05_edge_cases/struct_outside_extern_block_error.il) | Top-level `struct` (declared outside `@extern(C) { ... }`) cannot mention C-only types: `char`, `void`, `size_t`, `ssize_t`, or raw pointers. The check is transitive — referencing… |
| [struct_stack_promote.il](crates/ilang-cli/tests/programs/05_edge_cases/struct_stack_promote.il) | CRepr struct stack-promotion: a `struct` literal whose pointer never leaves the function frame is allocated as a Cranelift StackSlot instead of going through `__mir_alloc`. Escape… |
| [structural_eq_nested_and_refs.il](crates/ilang-cli/tests/programs/05_edge_cases/structural_eq_nested_and_refs.il) | Clean-round pin for structural `==` (round 127): deep mutual recursion across tuple / optional / array kinds, `==` in condition positions, and reference-kind slots (Map) inside a … |
| [structural_eq_tuple_array_optional.il](crates/ilang-cli/tests/programs/05_edge_cases/structural_eq_tuple_array_optional.il) | `==` / `!=` on heap value containers — tuples, dynamic arrays, and optionals — compare STRUCTURALLY (element-wise), the same value equality string and enum `==` already have. Befo… |
| [super_no_parent_error.il](crates/ilang-cli/tests/programs/05_edge_cases/super_no_parent_error.il) | `super` only makes sense inside a class that `extends` another. Calling `super.method()` from a method on a class with no parent must be a compile-time error so users can't preten… |
| [super_outside_method_error.il](crates/ilang-cli/tests/programs/05_edge_cases/super_outside_method_error.il) | `super` is bound to the surrounding class context. A top-level function (or any fn outside a class method body) has no class to lift `super` against — the compiler must reject it … |
| [timer_interval_multifire_arc.il](crates/ilang-cli/tests/programs/05_edge_cases/timer_interval_multifire_arc.il) | `setInterval` beyond the smoke tests: an interval RE-ARMS and fires repeatedly until `clearInterval`, a `clearTimeout` before the due time cancels the callback entirely, double-cl… |
| [todo_reached_panics.il](crates/ilang-cli/tests/programs/05_edge_cases/todo_reached_panics.il) | Reaching `todo()` at runtime aborts with a "not yet implemented" message and a non-zero exit, exactly like Rust's `todo!()`. |
| [todo_unreached_compiles.il](crates/ilang-cli/tests/programs/05_edge_cases/todo_unreached_compiles.il) | `todo()` is a diverging built-in (Rust's `todo!()`): it type-checks as any expected type and panics if reached. A match arm or fn body left as `todo()` must compile cleanly, and c… |
| [top_level_let_used_in_fn_deinit_once.il](crates/ilang-cli/tests/programs/05_edge_cases/top_level_let_used_in_fn_deinit_once.il) | A top-level `let` whose value is referenced from a named fn body (so the loader promotes it to a host slot) must still run its `deinit` exactly once at program exit. Every read in… |
| [transient_class_value_runs_deinit.il](crates/ilang-cli/tests/programs/05_edge_cases/transient_class_value_runs_deinit.il) | A class instance that's produced as a temporary and never bound — `make().field`, `f(make())`, `make()` as an expr-stmt — should fire `deinit` as soon as it leaves the surrounding… |
| [transient_for_in_and_map_method.il](crates/ilang-cli/tests/programs/05_edge_cases/transient_for_in_and_map_method.il) | Two more transient-temporary release sites the interpreter previously missed:    1. `for t in make_arr() { ... }` — the iterator is a fresh      array; once the loop exits, no bin… |
| [transient_map_index.il](crates/ilang-cli/tests/programs/05_edge_cases/transient_map_index.il) | `make_map()[k]` extracts one entry's value; the unselected entries had no other owner. Pre-fix the interp dropped the map via Rust's `Rc` and never released the leftover values' c… |
| [transient_method_and_index.il](crates/ilang-cli/tests/programs/05_edge_cases/transient_method_and_index.il) | Two more transient-temporary release sites that the interpreter previously missed:    1. `calc().add(2, 3)` — fresh receiver under a method call.      The receiver leaves no survi… |
| [transient_tuple_index.il](crates/ilang-cli/tests/programs/05_edge_cases/transient_tuple_index.il) | `make_tup()[0]` extracts element 0 and the rest of the tuple is unreferenced. Pre-fix the interp dropped the tuple via Rust's `Rc` and never released the unselected element's clas… |
| [trojan_source_rejected.il](crates/ilang-cli/tests/programs/05_edge_cases/trojan_source_rejected.il) | Trojan-source defence: the RLO (U+202E) bidi override would silently flip the visual order of the assignment, making the reader see one expression while the lexer reads another. T… |
| [try_nested_in_call_arg_refine.il](crates/ilang-cli/tests/programs/05_edge_cases/try_nested_in_call_arg_refine.il) | The `?` operator desugars to a block whose err arm does `return Result.err(e)` — that `Result.err` fixes E (from `e`) but leaves T as `Any`, to be refined from the enclosing fn's … |
| [try_op_heap_result_arc.il](crates/ilang-cli/tests/programs/05_edge_cases/try_op_heap_result_arc.il) | `?` on a heap-payload `Result` must keep ARC exact on BOTH paths: the ok path carries the `Box` out, and the err path early-returns `Result.err(e)` — its synthesised return jumps … |
| [tuple_element_wrap_all_stores.il](crates/ilang-cli/tests/programs/05_edge_cases/tuple_element_wrap_all_stores.il) | Confirmation pin (第42弾): a tuple whose element needs a `T -> T?` wrap (`(Box?, Box)`) is built correctly at every remaining store site, completing the composite-element-wrap sweep… |
| [tuple_index_non_literal_error.il](crates/ilang-cli/tests/programs/05_edge_cases/tuple_index_non_literal_error.il) | Tuple element access (`t[N]`) requires a compile-time non-negative integer literal — a runtime variable index would defeat the per-slot type tracking the checker relies on. Per sy… |
| [tuple_index_oob_error.il](crates/ilang-cli/tests/programs/05_edge_cases/tuple_index_oob_error.il) | Tuple indexing must stay within the declared arity. The check happens at type-check time because the index is a const literal, so the failure is reported before any execution. |
| [tuple_let_destructure.il](crates/ilang-cli/tests/programs/05_edge_cases/tuple_let_destructure.il) | `let (a, b) = pair` destructure pattern. The tuple is heterogeneous and may include heap elements; each binding must end up at the declared type. `tuple_basics.il` exercises tuple… |
| [u8_to_enum_cast.il](crates/ilang-cli/tests/programs/05_edge_cases/u8_to_enum_cast.il) | `u8 as Enum` round-trips its bit pattern unchanged. Pre-fix the JIT used `sextend` so `255u8 as Code as u32` produced 4294967295 (the i32 bit pattern of -1) instead of 255. (codeg… |
| [union_outside_extern_block_error.il](crates/ilang-cli/tests/programs/05_edge_cases/union_outside_extern_block_error.il) | Top-level `union` (declared outside `@extern(C) { ... }`) inherits the same field-type restriction as a top-level `struct`: no `char`, `void`, `size_t`, `ssize_t`, or raw pointers… |
| [unknown_attribute_error.il](crates/ilang-cli/tests/programs/05_edge_cases/unknown_attribute_error.il) | The attribute set is closed: an unknown attribute name is a parse error listing the known set. Before this check a typo'd attribute (`@derieve`, `@bogus`) parsed successfully and … |
| [unresolved_type_arg_diagnostic.il](crates/ilang-cli/tests/programs/05_edge_cases/unresolved_type_arg_diagnostic.il) | A generic enum ctor / generic fn call whose type parameter can't be pinned from its args AND has no expected type from context leaves the param `Any`. That used to abort lowering … |
| [unwrap_none_panics.il](crates/ilang-cli/tests/programs/05_edge_cases/unwrap_none_panics.il) | `none.unwrap()` is an explicit "I checked this" assertion. If the caller is wrong, the runtime panics rather than silently using garbage. |
| [void_ptr_implicit_to_typed.il](crates/ilang-cli/tests/programs/05_edge_cases/void_ptr_implicit_to_typed.il) | `*void` / `*const void` is the C "untyped pointer" and flows into any `*T` slot under the usual const-narrowing rule, without an explicit `as *T` cast. This is what `NULL` (typed … |
| [weak_backref_cascade_release_order.il](crates/ilang-cli/tests/programs/05_edge_cases/weak_backref_cascade_release_order.il) | Parent → strong children array, child → `Parent.weak` back-ref. When the parent's release cascade frees a child, the child's field release weak-releases the parent — whose strong … |
| [weak_bind_keeps_zombie_alive.il](crates/ilang-cli/tests/programs/05_edge_cases/weak_bind_keeps_zombie_alive.il) | `let w: T.weak = strongRef` must retain a weak share so the box survives as a zombie (strong rc 0, memory valid) for as long as the weak lives — that is what lets `WeakUpgrade` pe… |
| [weak_doubly_linked_list.il](crates/ilang-cli/tests/programs/05_edge_cases/weak_doubly_linked_list.il) | A doubly-linked list: `next` is a strong forward chain, `prev` a weak back-chain. Existing weak fixtures cover parent ↔ child (tree-shaped) structures; this pins a LINEAR list wal… |
| [weak_fresh_into_container_and_arg.il](crates/ilang-cli/tests/programs/05_edge_cases/weak_fresh_into_container_and_arg.il) | A fresh strong downgraded into a weak CONSUMER slot — a field, an array element (push or literal), a tuple element, or a `T.weak` function parameter — must take the weak share bef… |
| [weak_in_structural_equality.il](crates/ilang-cli/tests/programs/05_edge_cases/weak_in_structural_equality.il) | Clean-round pin: a `weak` reference participates in structural equality (rounds 127/129) by REFERENCE — two weaks to the same target are equal, to different targets are not. This … |
| [weak_join_and_fresh_into_weak.il](crates/ilang-cli/tests/programs/05_edge_cases/weak_join_and_fresh_into_weak.il) | `let w: T.weak = <expr>` where <expr> is a fresh-classified source — an if/else or match join, or a bare `new T()`. The join retains its strong branch value and a `new` owns its a… |
| [weak_parent_back_reference.il](crates/ilang-cli/tests/programs/05_edge_cases/weak_parent_back_reference.il) | Parent-child structure where the child holds a `Parent.weak` to avoid a strong cycle. Walking from a child through `weak.get()` must yield the live parent (Some); after the parent… |
| [weak_reassign_keeps_zombie_alive.il](crates/ilang-cli/tests/programs/05_edge_cases/weak_reassign_keeps_zombie_alive.il) | Sibling of weak_bind_keeps_zombie_alive.il for the REASSIGNMENT path (`w = strongRef`, not `let w = strongRef`). The assign lowering shared the let-binding's "coerced => fresh" sh… |
| [weak_return_strong_as_weak.il](crates/ilang-cli/tests/programs/05_edge_cases/weak_return_strong_as_weak.il) | `return strongExpr` from a `: T.weak` function downgrades the strong to a weak. The return path drops the source's strong 1, so it must take a weak share first (order matters — a … |
| [weak_temp_receiver_release.il](crates/ilang-cli/tests/programs/05_edge_cases/weak_temp_receiver_release.il) | A fresh weak returned from a call and consumed by `.get()` (`mkWeak().get()`) must release its weak share, the same way fresh optional / array / string / promise receivers are rel… |
| [weak_upgrade_outlives_last_strong.il](crates/ilang-cli/tests/programs/05_edge_cases/weak_upgrade_outlives_last_strong.il) | `weak.get()` upgrades to a strong binding that must keep the target alive for the binding's whole lifetime — even if every OTHER strong reference drops while the binding is held. … |
| [while_continue_skip.il](crates/ilang-cli/tests/programs/05_edge_cases/while_continue_skip.il) | `continue` inside a `while` loop jumps to the condition check, skipping the rest of the body. The basics fixtures only show continue inside `for-in`; the while-specific path (wher… |
| [wrap_cells_and_hinted_map.il](crates/ilang-cli/tests/programs/05_edge_cases/wrap_cells_and_hinted_map.il) | Round-22 finds, all pre-existing: 1. `h.opt = new Box(1)` (field typed Box?) — AssignField's own    wrap coerce never released the fresh source (the sixth wrap    position, after … |
| [wrap_coerce_container_stores.il](crates/ilang-cli/tests/programs/05_edge_cases/wrap_coerce_container_stores.il) | Implicit `T → T?` / `T → T.weak` wrap coerce at CONTAINER STORE positions that the 第20-22 wrap-coerce rounds missed. Those rounds fixed the wrap at `let` / arg / return / reassign… |
| [wrap_coerce_fresh_release.il](crates/ilang-cli/tests/programs/05_edge_cases/wrap_coerce_fresh_release.il) | The `T → T?` / `T → T.weak` auto-wrap coerce mints the wrapper's own share of the inner. When the SOURCE was itself owned (a fresh value's transfer 1, or a block tail the alias/bo… |

## 06_enums (6)

| fixture | pin している内容 |
|---|---|
| [enum_match_invalid_value_panics.il](crates/ilang-cli/tests/programs/06_enums/enum_match_invalid_value_panics.il) | A `pub enum E: T` can hold a value cast in from an out-of-range int (`n as E`). A `match` that covers every declared variant is exhaustive to the checker, so it has no wildcard — … |
| [enum_structural_equality.il](crates/ilang-cli/tests/programs/06_enums/enum_structural_equality.il) | Enum `==` / `!=` compare STRUCTURALLY: equal iff the variant tags match AND every payload compares equal. Previously only the discriminant tag was compared, so `circle(5) == circl… |
| [flags_enum_combined_identity.il](crates/ilang-cli/tests/programs/06_enums/flags_enum_combined_identity.il) | A `@flags` combined value (`read \| write`, `~read`, a masked `f & X`, an `int as EnumName` cast) is produced by `$enum.box`. Pre-fix that boxer allocated a FRESH rc-less cell eve… |
| [match_all_arms_return_tail.il](crates/ilang-cli/tests/programs/06_enums/match_all_arms_return_tail.il) | A `match` whose every arm diverges with `return`, sitting as the function tail, must type-check — exactly like `if { return } else { return }` does. The arms contribute no value t… |
| [repr_enum_comparison.il](crates/ilang-cli/tests/programs/06_enums/repr_enum_comparison.il) | Ordering (`<` `<=` `>` `>=`) between two values of the SAME repr enum must compare the discriminant tags, NOT the boxed pointers. The checker permits the comparison (a repr enum's… |
| [repr_enum_patterns.il](crates/ilang-cli/tests/programs/06_enums/repr_enum_patterns.il) | Confirms the working repr-enum (`enum E: T`) idioms that lacked a fixture, probed in the bit-ops rounds (第68/69弾 fixed the broken neighbours: `~`, flags `has`, and `enum == int-li… |

## 06_overloading (19)

| fixture | pin している内容 |
|---|---|
| [ambiguous_error.il](crates/ilang-cli/tests/programs/06_overloading/ambiguous_error.il) | Two overloads tying for the best score → typechecker rejects the call as ambiguous. (Cross-type widening cost is symmetric here.) |
| [best_match.il](crates/ilang-cli/tests/programs/06_overloading/best_match.il) | When multiple overloads are viable via implicit conversions, the scoring rule picks the one with the closest match. Exact type wins over implicit-widening, which wins over int→flo… |
| [by_arg_type.il](crates/ilang-cli/tests/programs/06_overloading/by_arg_type.il) | Function overloading by parameter type. Same name, different implementations, dispatched at the call site by exact match. |
| [by_arity.il](crates/ilang-cli/tests/programs/06_overloading/by_arity.il) | Overloading by parameter count. |
| [callback_via_test_extern.il](crates/ilang-cli/tests/programs/06_overloading/callback_via_test_extern.il) | Pass a top-level ilang fn as a C callback. `test.applyI32Cb` is a host extern declared as   extern "C" fn(cb: extern "C" fn(i64, i64) -> i32, a: i64, b: i64) -> i32 on the Rust si… |
| [default_overload.il](crates/ilang-cli/tests/programs/06_overloading/default_overload.il) | Overloads can coexist with default arguments. The exact-arity match always wins over a default-filled one — `f(x)` picks the one-param overload, not the two-param one with a defau… |
| [firstclass_overloaded_error.il](crates/ilang-cli/tests/programs/06_overloading/firstclass_overloaded_error.il) | Referring to an overloaded name as a first-class value (`let f = name`) is rejected — overload resolution needs argument types to disambiguate. Per syntax.md §6 "First-class refer… |
| [float_widening_score.il](crates/ilang-cli/tests/programs/06_overloading/float_widening_score.il) | `f32 ↔ f64` widening costs 1 in overload scoring — the same as integer widening within a sign. A call with an `f32` argument against `(f32, _)` vs `(f64, _)` overloads should pick… |
| [fn_typed_arg_overload.il](crates/ilang-cli/tests/programs/06_overloading/fn_typed_arg_overload.il) | Overloads keyed on fn-typed parameters with distinct signatures. The call site routes by the closure's static fn type — passing `fn(i64): i64` lands on the i64-shaped overload, `f… |
| [generic_conflict_error.il](crates/ilang-cli/tests/programs/06_overloading/generic_conflict_error.il) | Generic  non-generic same name is rejected — keeps overload resolution simple (generic resolution is its own special path). |
| [literal_narrowing_score.il](crates/ilang-cli/tests/programs/06_overloading/literal_narrowing_score.il) | Scoring rule cost=2 for literal-narrowing — a numeric literal that fits a narrower parameter type costs more than an exact match on the same call. Per syntax.md §6 "Picking rule". |
| [multi_param_widening.il](crates/ilang-cli/tests/programs/06_overloading/multi_param_widening.il) | Multi-parameter overload resolution — the picker sums each parameter's cost. `best_match.il` covers the single-parameter case; this one pins down the asymmetric cost combinations … |
| [no_match_error.il](crates/ilang-cli/tests/programs/06_overloading/no_match_error.il) | No viable overload — typechecker rejects. |
| [optional_arg_overload.il](crates/ilang-cli/tests/programs/06_overloading/optional_arg_overload.il) | `fn f(x: T)` vs `fn f(x: T?)` — overload resolution picks the exact non-optional one for a bare `T` value, the Optional one for `none` / `some(x)` / a `T?` slot. Per syntax.md §6 … |
| [optional_wrap_score.il](crates/ilang-cli/tests/programs/06_overloading/optional_wrap_score.il) | `T → T?` auto-wrap costs 3 in overload scoring. An exact-`T` candidate (cost 0) wins decisively over a `T?` candidate; passing the optional explicitly picks the `T?` overload. Per… |
| [overload_selection_heap_arc.il](crates/ilang-cli/tests/programs/06_overloading/overload_selection_heap_arc.il) | Overload resolution must hand the chosen overload its heap argument (and heap return value) with correct ARC — the existing overloading fixtures only check that the right overload… |
| [return_type_only_dup_error.il](crates/ilang-cli/tests/programs/06_overloading/return_type_only_dup_error.il) | Two overloads with the same parameter types but different return types are rejected — overload resolution looks at the call-site argument types only, so a return-type-only duplica… |
| [static_method_by_arg_type.il](crates/ilang-cli/tests/programs/06_overloading/static_method_by_arg_type.il) | Static method overloading: same name, different signatures, dispatched at the call site like instance / top-level overloads. |
| [unit_return_overload.il](crates/ilang-cli/tests/programs/06_overloading/unit_return_overload.il) | Overload on parameter types where every variant returns `()` (no return-type annotation). The dispatcher still walks the arg signatures, the matching body executes for its side ef… |

## 07_method_overloading (12)

| fixture | pin している内容 |
|---|---|
| [ambiguous_error.il](crates/ilang-cli/tests/programs/07_method_overloading/ambiguous_error.il) | Two method overloads tying for the best score → ambiguous. |
| [best_match.il](crates/ilang-cli/tests/programs/07_method_overloading/best_match.il) | Best-match scoring picks the exact overload over the widening one. |
| [by_arg_type.il](crates/ilang-cli/tests/programs/07_method_overloading/by_arg_type.il) | Method overloading by parameter type, dispatched at the call site. |
| [by_arity.il](crates/ilang-cli/tests/programs/07_method_overloading/by_arity.il) | Method overloading by arity (different number of params). |
| [defaults_with_overload.il](crates/ilang-cli/tests/programs/07_method_overloading/defaults_with_overload.il) | Default arguments coexist with method overloading — a candidate whose arity matches the call exactly wins over a default-filled candidate (the latter takes a 1000 score penalty pe… |
| [deinit_overload_error.il](crates/ilang-cli/tests/programs/07_method_overloading/deinit_overload_error.il) | deinit cannot be overloaded — there is exactly one deinit per class. |
| [generic_class_conflict_error.il](crates/ilang-cli/tests/programs/07_method_overloading/generic_class_conflict_error.il) | Methods on a generic class cannot be overloaded (keeps mono+overload resolution paths separate). |
| [init_method_overload_heap_arc.il](crates/ilang-cli/tests/programs/07_method_overloading/init_method_overload_heap_arc.il) | `init` and method overload resolution must pass the chosen overload its heap arguments with correct ARC. The existing method-overloading fixtures check which overload is picked, n… |
| [init_overload.il](crates/ilang-cli/tests/programs/07_method_overloading/init_overload.il) | init() can be overloaded; `new C(...)` picks the best match. |
| [parent_method_overload_error.il](crates/ilang-cli/tests/programs/07_method_overloading/parent_method_overload_error.il) | A child class can overload methods the *parent doesn't* declare (covered by `subclass_overload.il`), but it can't introduce a new overload sharing a name with a parent method — `o… |
| [static_overload.il](crates/ilang-cli/tests/programs/07_method_overloading/static_overload.il) | `static` methods can be overloaded — same arity/type rules as instance methods, mangled to `name__<param_types>` after type checking. Per syntax.md §7 `static` methods. |
| [subclass_overload.il](crates/ilang-cli/tests/programs/07_method_overloading/subclass_overload.il) | Method overloading is now allowed on subclasses too — earlier it was root-class only ("overloading is not supported in inheritance hierarchies"). The check is: overloads are fine … |

## 08_properties (13)

| fixture | pin している内容 |
|---|---|
| [getter_only.il](crates/ilang-cli/tests/programs/08_properties/getter_only.il) | Read-only computed property (getter, no setter). |
| [getter_setter_basic.il](crates/ilang-cli/tests/programs/08_properties/getter_setter_basic.il) | Computed property: read goes through getter, write through setter. |
| [getter_tail_bare_var_heap.il](crates/ilang-cli/tests/programs/08_properties/getter_tail_bare_var_heap.il) | Property getter body returning the bare field name (`inner`) of a heap-typed field. Same desugar shape as the `Box<T>.get(): T { x }` regression that ~16% race-aborted under paral… |
| [getter_used_in_method.il](crates/ilang-cli/tests/programs/08_properties/getter_used_in_method.il) | A method can read the class's own `get` property through `this` — the property dispatch resolves inside the class body just like a field read. |
| [property_accessor_reads_global.il](crates/ilang-cli/tests/programs/08_properties/property_accessor_reads_global.il) | A property getter / setter body may reference a top-level `let` (a global) the same way a regular method does. The slot-promotion pass (`build_slot_table` -> `collect_fn_free_var_… |
| [property_field_collision_error.il](crates/ilang-cli/tests/programs/08_properties/property_field_collision_error.il) | A property and a field can't share a name. |
| [property_heap_value_arc.il](crates/ilang-cli/tests/programs/08_properties/property_heap_value_arc.il) | ARC balance for heap-valued properties. Property reads are OWNED: the getter's tail retains like any method tail (computed getters whose tail is fresh own their 1 anyway), and the… |
| [property_method_collision_error.il](crates/ilang-cli/tests/programs/08_properties/property_method_collision_error.il) | A `get`/`set` property can't share a name with a method. Field collision is in `property_field_collision_error.il`; this fixture pins the method case from syntax.md §7 "Property n… |
| [readonly_assign_error.il](crates/ilang-cli/tests/programs/08_properties/readonly_assign_error.il) | Assigning to a getter-only property errors at typecheck. |
| [setter_only.il](crates/ilang-cli/tests/programs/08_properties/setter_only.il) | Write-only property (setter, no getter). Reading errors at typecheck. |
| [setter_param_type_mismatch_error.il](crates/ilang-cli/tests/programs/08_properties/setter_param_type_mismatch_error.il) | Getter return type and setter param type must match the property type. |
| [setter_used_in_method.il](crates/ilang-cli/tests/programs/08_properties/setter_used_in_method.il) | A method can write through the class's own `set` property — `this.name = v` inside the body routes to the setter, not to a raw field store. |
| [static_getter_setter.il](crates/ilang-cli/tests/programs/08_properties/static_getter_setter.il) | Class-level computed properties: `static get` / `static set`. Read via `ClassName.name`, write via `ClassName.name = v`. Per syntax.md §7. |

## 09_subtyping (53)

| fixture | pin している内容 |
|---|---|
| [array_literal_subclass.il](crates/ilang-cli/tests/programs/09_subtyping/array_literal_subclass.il) | Array literal with subclass elements flowing into Parent[] (cd61814  32111f1: thread subclass through composite literals). |
| [array_push_subclass.il](crates/ilang-cli/tests/programs/09_subtyping/array_push_subclass.il) | `xs.push(new Child())` where `xs: Parent[]` (cd61814). |
| [as_upcast.il](crates/ilang-cli/tests/programs/09_subtyping/as_upcast.il) | Explicit `as` upcast — `b as Parent` where `B extends Parent` (475cd79). |
| [branch_join_common_interface.il](crates/ilang-cli/tests/programs/09_subtyping/branch_join_common_interface.il) | `if` / `match` / array-literal joins of two classes that share no parent class but implement a common INTERFACE now join to that interface, mirroring how subclasses of a common pa… |
| [branch_join_common_interface_ambiguous_error.il](crates/ilang-cli/tests/programs/09_subtyping/branch_join_common_interface_ambiguous_error.il) | When two branch classes share MORE THAN ONE common interface, the join is ambiguous — `common_object_join` can't pick one without an expected type at the join point, so it falls t… |
| [covariant_join_interface_result.il](crates/ilang-cli/tests/programs/09_subtyping/covariant_join_interface_result.il) | Clean-round pin for the join covariance (第79-81弾): the earlier fixtures cover classes / arrays / maps / tuples, but not two cases that also rely on it —    * an `if`/`match` over … |
| [covariant_return.il](crates/ilang-cli/tests/programs/09_subtyping/covariant_return.il) | Override with a covariant (subclass) return type (1c75465). |
| [deinit_chain_to_parent.il](crates/ilang-cli/tests/programs/09_subtyping/deinit_chain_to_parent.il) | Swift-style deinit chaining: when an instance dies, the most derived class's deinit runs first, then each ancestor's deinit in turn — automatically, without the user calling super… |
| [field_init_subclass.il](crates/ilang-cli/tests/programs/09_subtyping/field_init_subclass.il) | Class fields typed at parent receiving subclass instances and subclass-typed composites at init time (cd61814). |
| [fn_arg_subclass.il](crates/ilang-cli/tests/programs/09_subtyping/fn_arg_subclass.il) | Subclass values flowing into fn args of parent / parent-composite types (cd61814). |
| [fn_return_subclass.il](crates/ilang-cli/tests/programs/09_subtyping/fn_return_subclass.il) | Subclass values returned from a fn that declares the parent type. The body's tail is checked through `value_assignable` so composite literals (Array, Tuple, Optional) flow through… |
| [generic_class_implements_interface.il](crates/ilang-cli/tests/programs/09_subtyping/generic_class_implements_interface.il) | A generic class that declares an interface (`class Box<T>: Sized`) should have each instantiation (`Box<i64>`) satisfy that interface — the relation lives on the base class, indep… |
| [generic_enum_covariant_join.il](crates/ilang-cli/tests/programs/09_subtyping/generic_enum_covariant_join.il) | Generic-enum literal covariance extends through `if` / `match` joins. A single covariant arm already worked — `fn f(): Box<Animal> { Box.hold(new Dog()) }` — but two arms each bui… |
| [generic_enum_covariant_nested.il](crates/ilang-cli/tests/programs/09_subtyping/generic_enum_covariant_nested.il) | Generic-enum ctor literal covariance composes through the `some(..)` / tuple / array descent, and applies at the fn-arg and fn-return positions, not just a direct `let`. The covar… |
| [generic_enum_literal_covariant.il](crates/ilang-cli/tests/programs/09_subtyping/generic_enum_literal_covariant.il) | A generic enum ctor LITERAL is covariant in its type args, the same way an array / map literal is: `Result.ok(new Dog())` (`Result<Dog, _>`) flows into a `Result<Animal, string>` … |
| [generic_enum_literal_covariant_alias_error.il](crates/ilang-cli/tests/programs/09_subtyping/generic_enum_literal_covariant_alias_error.il) | Generic-enum covariance is LITERAL-only. An aliased `Result<Dog, string>` variable does NOT assign to `Result<Animal, string>` — only a ctor literal (`Result.ok(new Dog())`) covar… |
| [generic_fn_local_let.il](crates/ilang-cli/tests/programs/09_subtyping/generic_fn_local_let.il) | Body-local `let y: T[]` referencing the generic fn's own type param (fba3d1b — `current_type_params` fallback in validate_type and rewrite of let annotations). |
| [generic_instance_branch_join.il](crates/ilang-cli/tests/programs/09_subtyping/generic_instance_branch_join.il) | `if` / `match` branches whose arms are generic-class instantiations join through the arms' BASE class, so they take the common interface just like non-generic conformers do. Previ… |
| [generic_instance_interface_covariance.il](crates/ilang-cli/tests/programs/09_subtyping/generic_instance_interface_covariance.il) | A generic-class instance flows into its declared interface in every covariance position, not just a plain parameter / local / array (which `generic_class_implements_interface.il` … |
| [generic_instance_is_as_discrimination.il](crates/ilang-cli/tests/programs/09_subtyping/generic_instance_is_as_discrimination.il) | Clean-round pin (follows round 124's is/as? fix): `is` / `as?` on a GENERIC class instance recognises the interface it implements and discriminates by type argument. Each `Box<T>`… |
| [if_else_common_ancestor.il](crates/ilang-cli/tests/programs/09_subtyping/if_else_common_ancestor.il) | if/else arms returning sibling subclasses unify at their nearest common ancestor (a5e9e2c, 92a2185). |
| [iflet_common_ancestor.il](crates/ilang-cli/tests/programs/09_subtyping/iflet_common_ancestor.il) | if-let branches with subclass results unify at common ancestor (92a2185 — was missing entirely from the if-let join path). |
| [interface_array_and_downcast.il](crates/ilang-cli/tests/programs/09_subtyping/interface_array_and_downcast.il) | Interface-typed array and downcast (`as?`) from an interface slot to a concrete class. `interface_basic.il` covers passing an implementing class through an `Iface`-typed parameter… |
| [interface_chain_fresh_receiver_arc.il](crates/ilang-cli/tests/programs/09_subtyping/interface_chain_fresh_receiver_arc.il) | Clean-round pin for the interface-dispatch ARC family (rounds 119/120): a FRESH interface-typed receiver whose method returns a heap value, in a chain (`getFactory().make().n`). T… |
| [interface_covariance_and_downcast.il](crates/ilang-cli/tests/programs/09_subtyping/interface_covariance_and_downcast.il) | Confirmation pin (第47弾): an interface implementor flows into every covariant store position, and `as?` downcasts back to the concrete class — the subclass machinery (wrap, covaria… |
| [interface_dispatch_fresh_arg_release.il](crates/ilang-cli/tests/programs/09_subtyping/interface_dispatch_fresh_arg_release.il) | Passing a FRESH heap value as an argument to a virtually-dispatched call must release the caller's transient 1 after the call, exactly like the direct (concrete-receiver) method p… |
| [interface_inheritance.il](crates/ilang-cli/tests/programs/09_subtyping/interface_inheritance.il) | Plain (non-@com) interface inheritance: `interface B: A` means a B is also an A. A class implementing B must satisfy A's methods too, a B-typed receiver can call A's inherited met… |
| [interface_inheritance_missing_method_error.il](crates/ilang-cli/tests/programs/09_subtyping/interface_inheritance_missing_method_error.il) | A class implementing `B` (where `interface B: A`) must also implement A's inherited methods. `Bad` provides `bar` (B's own) but not `foo` (inherited from A), so conformance fails … |
| [interface_is_as_additional_and_inherited.il](crates/ilang-cli/tests/programs/09_subtyping/interface_is_as_additional_and_inherited.il) | `is` / `as?` must recognise EVERY interface a class conforms to, not just the first declared base.  Before: the runtime type test (`emit_is_subclass`) computed its accept set by w… |
| [interface_method_base_typed_receiver.il](crates/ilang-cli/tests/programs/09_subtyping/interface_method_base_typed_receiver.il) | Calling an interface method through a PARENT-CLASS-typed receiver that holds a subclass instance must dispatch correctly (it used to segfault).  Before: a class that implements an… |
| [interface_subclass_dispatch_combos.il](crates/ilang-cli/tests/programs/09_subtyping/interface_subclass_dispatch_combos.il) | Clean-round pin for the interface vtable-slot family (rounds 119/120): a subclass of an interface-implementing class dispatches correctly across combinations the round-120 fixture… |
| [let_upcast.il](crates/ilang-cli/tests/programs/09_subtyping/let_upcast.il) | `let x: Parent = new Child()` — implicit upcast on the let annotation. Was rejected by the TC's let arm before fb16dfc. |
| [map_literal_covariant_value.il](crates/ilang-cli/tests/programs/09_subtyping/map_literal_covariant_value.il) | A map literal flows into a `Map<K, Parent>` / `Map<K, Parent?>` slot the same way an array literal flows into `Parent[]`. The checker used to infer the value type from the entries… |
| [map_literal_covariant_value_alias_error.il](crates/ilang-cli/tests/programs/09_subtyping/map_literal_covariant_value_alias_error.il) | Map value covariance is LITERAL-ONLY. A fresh `{...}` literal can land in a `Map<K, Parent>` slot (see map_literal_covariant_value.il), but an aliased `Map<K, Child>` variable mus… |
| [map_value_subclass.il](crates/ilang-cli/tests/programs/09_subtyping/map_value_subclass.il) | Map<K, Parent> accepts any subclass value — the value slot keeps its runtime class, so reading back through the parent face still virtual-dispatches to the right override. |
| [match_common_ancestor.il](crates/ilang-cli/tests/programs/09_subtyping/match_common_ancestor.il) | match arms returning multiple sibling subclasses join at the nearest common ancestor (92a2185). |
| [method_tail_bare_var_parent_field.il](crates/ilang-cli/tests/programs/09_subtyping/method_tail_bare_var_parent_field.il) | Subclass method body whose tail is the bare name of a heap field **declared on the parent**. The bare-var → implicit `this.field` path runs through the same lower as `Box<T>.get()… |
| [multiple_interface_impl.il](crates/ilang-cli/tests/programs/09_subtyping/multiple_interface_impl.il) | A class can declare a parent plus multiple interfaces via the same `:` base list — parent first, interfaces follow after commas. Each interface slot accepts the concrete value and… |
| [nested_container_covariance.il](crates/ilang-cli/tests/programs/09_subtyping/nested_container_covariance.il) | Nested / composite covariance for fresh container literals, and the `T[] -> T[]?` (and friends) Optional auto-wrap. Round 26 widened the `T -> T?` wrap to accept a subclass source… |
| [optional_parent_with_none.il](crates/ilang-cli/tests/programs/09_subtyping/optional_parent_with_none.il) | `Optional<Parent>` with both `some(Child)` and `none` flowing through the same slot. `optional_subclass.il` exercises the some-only direction; this fixture covers `none`, function… |
| [optional_subclass.il](crates/ilang-cli/tests/programs/09_subtyping/optional_subclass.il) | `Parent? ← some(new Child())` and the `some(complex_literal)` recursion case (29363ed). |
| [overload_subclass_distance.il](crates/ilang-cli/tests/programs/09_subtyping/overload_subclass_distance.il) | Overload resolution prefers the closer parent when an arg could go to multiple ancestors. Pre-fix `score_arg` didn't know about class subtype at all, so a `C: B: A` arg flowing in… |
| [override_return_mismatch_error.il](crates/ilang-cli/tests/programs/09_subtyping/override_return_mismatch_error.il) | `override` must reproduce the parent's signature exactly — a return-type drift is rejected at type check, alongside the parameter-type drift case in `override_signature_mismatch_e… |
| [override_signature_mismatch_error.il](crates/ilang-cli/tests/programs/09_subtyping/override_signature_mismatch_error.il) | `override` must reproduce the parent's signature exactly — a parameter-type drift is rejected at type check. Per syntax.md §7 "The override's signature must match the parent's exa… |
| [property_interface_subclass_dispatch.il](crates/ilang-cli/tests/programs/09_subtyping/property_interface_subclass_dispatch.il) | Clean-round pin: a class with virtual PROPERTIES that also implements an interface, plus a subclass overriding a getter, all reached through a parent-class-typed receiver. Propert… |
| [property_partial_override.il](crates/ilang-cli/tests/programs/09_subtyping/property_partial_override.il) | A subclass that overrides only ONE accessor of an inherited property keeps the parent's accessor for the other direction. The checker built each class's property accessor set from… |
| [property_virtual_dispatch.il](crates/ilang-cli/tests/programs/09_subtyping/property_virtual_dispatch.il) | Property getters / setters dispatch VIRTUALLY, like regular methods: an overridden accessor reached through a base-typed reference runs the override, not the base version. Previou… |
| [set_element_subclass.il](crates/ilang-cli/tests/programs/09_subtyping/set_element_subclass.il) | Set<Parent> accepts any subclass element provided the parent satisfies the value-equality protocol (`@derive(Eq, Hash)` here). The runtime class is preserved on read-back via `val… |
| [skip_levels_upcast.il](crates/ilang-cli/tests/programs/09_subtyping/skip_levels_upcast.il) | A grandchild can flow into a slot typed as any ancestor — not just the immediate parent. The vtable still routes calls to the runtime class's override. Per syntax.md §7 "Subtyping… |
| [subclass_optional_wrap.il](crates/ilang-cli/tests/programs/09_subtyping/subclass_optional_wrap.il) | Subclass instance wrapped into `Optional<Parent>` (`T → T?` auto-wrap where the source is a subclass of the Optional's inner type). The `T → T?` wrap was hard-coded to fire only o… |
| [super_method_chain.il](crates/ilang-cli/tests/programs/09_subtyping/super_method_chain.il) | `super.method(...)` resolves to the immediate parent's version (statically up the chain). A grandchild calling `super.label()` hits the parent's override, which in turn can call `… |
| [tuple_literal_subclass.il](crates/ilang-cli/tests/programs/09_subtyping/tuple_literal_subclass.il) | Tuple literal element subclass — `(Parent, Parent) ← (Child, Child)` (cd61814  32111f1). |
| [weak_parent_from_child.il](crates/ilang-cli/tests/programs/09_subtyping/weak_parent_from_child.il) | A `Parent.weak` slot can hold a value that started life as a `Child` instance — the auto-downgrade combines the upcast and the weak demotion in one step. The underlying allocation… |

## 10_closures_arc (27)

| fixture | pin している内容 |
|---|---|
| [break_aliased_array.il](crates/ilang-cli/tests/programs/10_closures_arc/break_aliased_array.il) | `loop { break <aliased var> }` retains the value past the surrounding scope's release (98cd0db). Without the fix the JIT hands the loop's slot a borrowed pointer the binding is ab… |
| [cell_capture_release.il](crates/ilang-cli/tests/programs/10_closures_arc/cell_capture_release.il) | Mutating captures (closure assigns a captured var) go through a shared 1-element heap cell. The ownership model: the scope owns the creation 1, each capturing closure retains its … |
| [closure_captures_closure_sharing_cell.il](crates/ilang-cli/tests/programs/10_closures_arc/closure_captures_closure_sharing_cell.il) | A closure that captures another closure that itself mutates a shared cell — the outer closure sees the mutation through its own capture of the same name, because the mutable bindi… |
| [closure_captures_this.il](crates/ilang-cli/tests/programs/10_closures_arc/closure_captures_this.il) | Closure built inside a method body captures the lexical `this` (e416943  9475956: interpreter and JIT both restore it; the codegen path also retains/releases the captured pointer … |
| [closure_dispatches_overloaded_method.il](crates/ilang-cli/tests/programs/10_closures_arc/closure_dispatches_overloaded_method.il) | A closure built inside a method body captures `this` and calls an overloaded method on it. Overload resolution happens at the call site (closure-construction time) using the param… |
| [closure_fresh_arg_release.il](crates/ilang-cli/tests/programs/10_closures_arc/closure_fresh_arg_release.il) | Fresh heap args passed to a CLOSURE-typed callee follow the same transfer rule as named-fn calls: params are borrows, so the caller's 1 must be released after the call. The indire… |
| [closure_self_recursion_fn_body.il](crates/ilang-cli/tests/programs/10_closures_arc/closure_self_recursion_fn_body.il) | Self-recursive closures inside fn bodies. The body's reference to its own binding name resolves to the running closure itself (Inst::ClosureSelf — the hidden env param), not a cap… |
| [closure_self_recursion_unannotated_error.il](crates/ilang-cli/tests/programs/10_closures_arc/closure_self_recursion_unannotated_error.il) | A self-recursive closure without a fn-type annotation can't be checked (the recursive type isn't inferrable). This used to surface as a bare "undefined function" — it's a targeted… |
| [closure_super_call.il](crates/ilang-cli/tests/programs/10_closures_arc/closure_super_call.il) | `super.method(...)` inside a closure built in a child method (9475956). Both interp and JIT restore the lexical class so the parent's method is reachable. |
| [closure_tail_bare_var_field.il](crates/ilang-cli/tests/programs/10_closures_arc/closure_tail_bare_var_field.il) | Closure whose tail is `inner` — a bare-var implicit field reference of the enclosing method's `this`. Two pre-existing pieces of machinery had to compose: collect_free_vars_expr n… |
| [closures_array_share_cell.il](crates/ilang-cli/tests/programs/10_closures_arc/closures_array_share_cell.il) | Two closures stored in the same array share a single mutable capture — `push`-ing each closure separately doesn't snapshot the cell, so the reader closure sees the writer closure'… |
| [early_return_keeps_shared_cell_alive.il](crates/ilang-cli/tests/programs/10_closures_arc/early_return_keeps_shared_cell_alive.il) | Returning a closure mid-body (`if cond { return closure }`) still keeps the mutable cell alive — the closure carries an ARC handle on the cell, so the early-return path doesn't re… |
| [enum_ctor_refine_closure_return.il](crates/ilang-cli/tests/programs/10_closures_arc/enum_ctor_refine_closure_return.il) | Completes the enum-ctor type-arg refinement (第48 / 第49): a CLOSURE body must also refine a generic enum ctor in its tail / early returns from the closure's declared return type, t… |
| [independent_counters_separate_cells.il](crates/ilang-cli/tests/programs/10_closures_arc/independent_counters_separate_cells.il) | Two separate `makeCounter()` calls each get their own shared cell — incrementing one does not bleed into the other. Per syntax.md §6 "Independent `let` bindings (e.g. two separate… |
| [local_shadows_top_level_fn.il](crates/ilang-cli/tests/programs/10_closures_arc/local_shadows_top_level_fn.il) | A `let f = closure` inside a fn body shadows a top-level `fn f` of the same name — the inner closure that references `f` resolves the local binding, not the global one. Per syntax… |
| [loop_per_iteration_capture.il](crates/ilang-cli/tests/programs/10_closures_arc/loop_per_iteration_capture.il) | A `let` declared inside a loop body is a FRESH binding each iteration, so a closure that captures it snapshots that iteration's value — the three closures return 0, 1, 2, not all … |
| [map_fresh_receiver.il](crates/ilang-cli/tests/programs/10_closures_arc/map_fresh_receiver.il) | Map methods on a fresh receiver release the map after dispatch (126f84c  33e12d5 for the string-key release). |
| [map_mutable_capture.il](crates/ilang-cli/tests/programs/10_closures_arc/map_mutable_capture.il) | A closure capturing a Map and mutating it across calls — the captured Map lives in the closure's heap-allocated env, ARC keeps it alive while the closure is alive, and writes pers… |
| [match_fresh_scrutinee.il](crates/ilang-cli/tests/programs/10_closures_arc/match_fresh_scrutinee.il) | `match make()` on a fresh heap enum scrutinee releases the scrutinee at the merge so we don't leak (e3bb440). |
| [method_chain_fresh_receiver.il](crates/ilang-cli/tests/programs/10_closures_arc/method_chain_fresh_receiver.il) | `(new Foo()).method()` and similar transient-receiver patterns don't leak (03bb407  beee631  46ae19a). Bound-loop check — running this 1k times stays steady on RC. |
| [nested_closure_this.il](crates/ilang-cli/tests/programs/10_closures_arc/nested_closure_this.il) | Nested closure where only the innermost mentions `this` — every wrapper along the chain still has to capture it (9475956: `block_uses_this_directly` recurses into FnExpr). |
| [param_shadows_capture.il](crates/ilang-cli/tests/programs/10_closures_arc/param_shadows_capture.il) | A parameter with the same name as an outer binding shadows it inside the closure body — capture vs. parameter resolution is decided at the closure's call site, not at construction… |
| [readonly_capture_snapshot.il](crates/ilang-cli/tests/programs/10_closures_arc/readonly_capture_snapshot.il) | Read-only capture is a value snapshot — assigning to the outer variable AFTER the closure is built doesn't leak into the closure. Per syntax.md §6 "Read-only captures are value sn… |
| [readonly_capture_string.il](crates/ilang-cli/tests/programs/10_closures_arc/readonly_capture_string.il) | Read-only capture is a value snapshot for STRING (heap, ARC) — reassigning the outer string after the closure is built doesn't bleed into the closure. The captured pointer is reta… |
| [some_aliased_inner.il](crates/ilang-cli/tests/programs/10_closures_arc/some_aliased_inner.il) | `some(<aliased heap>)` retains the inner so the Optional owns its own 1 (3be5dcc). Without retain the JIT used the value after its source binding had released it. |
| [trampoline_topfn_as_value.il](crates/ilang-cli/tests/programs/10_closures_arc/trampoline_topfn_as_value.il) | Referring to a top-level `fn` as a function value (`let f = some_fn`) auto-wraps it in a trampoline closure that ignores the env slot and forwards to the target. Per syntax.md §6 … |
| [tuple_array_aliased_elements.il](crates/ilang-cli/tests/programs/10_closures_arc/tuple_array_aliased_elements.il) | Tuple / array literal elements that are aliased heap refs need retain at construction (ca137fc). Pre-fix JIT segfaulted / out-of-bounds-panicked on the second access. |

## 11_capabilities (6)

| fixture | pin している内容 |
|---|---|
| [use_ffi_indirect.il](crates/ilang-cli/tests/programs/11_capabilities/ffi_indirect_denied/use_ffi_indirect.il) | Capability bypass guard: taking the *address* of a C function and calling it indirectly must not slip past the ffi gate. `let f = abs` lowers to a closure over the extern; without… |
| [use_ffi_indirect.il](crates/ilang-cli/tests/programs/11_capabilities/ffi_indirect_granted/use_ffi_indirect.il) | With `ffi` granted the indirect C call is allowed and runs to completion: abs(-7) == 7. |
| [use_fs.il](crates/ilang-cli/tests/programs/11_capabilities/file_denied/use_fs.il) | A std.fs sink with no `file` capability granted (the sibling ilang.toml grants only `os`). The call must be refused: under JIT the cap_require_file gate aborts at runtime, and und… |
| [use_fs_async.il](crates/ilang-cli/tests/programs/11_capabilities/file_denied_async/use_fs_async.il) | The std.fs sink sits past an `await`, so it lands in the second resume-point of the desugared poll fn. The capability gate must still fire there: JIT aborts when the poll runs, AO… |
| [use_fs.il](crates/ilang-cli/tests/programs/11_capabilities/file_granted/use_fs.il) | The granted counterpart to file_denied: with `file` in the sibling manifest the std.fs sink is allowed and runs to completion. |
| [use_os.il](crates/ilang-cli/tests/programs/11_capabilities/os_denied/use_os.il) | A std.os sink with only `file` granted — the `os` capability is withheld, so the errno intrinsic must be refused. Confirms the vocabulary is enforced per-capability, not all-or-no… |

## (root)

| fixture | pin している内容 |
|---|---|
| [_smoke.il](crates/ilang-cli/tests/programs/_smoke.il) | Smoke fixture for the .il-file test harness. Now exercises the `test` built-in too — if these break, every other fixture is suspect. |

---

**合計 867 fixture**(自動生成)。
